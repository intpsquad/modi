"""평가 하네스 — **개발용이며 런타임이 아니다.**

실제 LLM 을 호출해 크레딧을 쓰므로 `pyproject.toml` 의 `testpaths` 밖에 둔다. CI 에서 돌지
않으며, 러너 안의 순수 로직만 `tests/` 가 크레딧 0 으로 잰다.

**지금은 자동 채점을 하지 않는다.** 프로토타입(vLLM + Qwen)에서 채점기를 이식해 지표 셋을
먼저 만들었는데, 실측 끝에 셋 다 무효로 판명됐다(`docs/EXPERIMENTS.md` "다시 하지 말 것").
순서를 바꿨다 — **실제 모델의 출력을 눈으로 보고 무엇이 문제인지 찾은 뒤에 지표를 정의한다.**
"""
