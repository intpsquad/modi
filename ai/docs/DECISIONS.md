# DECISIONS — AI 파트 결정 트래커

> 규칙: AI 구현에 관한 결정이 새로 생기면 여기에 **근거와 함께** 적는다.
> 아키텍처·데이터 모델처럼 **프로젝트 전체에 영향을 주는 결정은 루트 [`specs/OPEN.md`](../../specs/OPEN.md)** 에 적는다 — 여기는 AI 내부 결정만 다룬다.
> 측정으로 뒷받침된 항목은 [`EXPERIMENTS.md`](EXPERIMENTS.md)를 참조로 단다.

---

## 확정

- [x] **LLM 벤더 = OpenAI** — 자체 GPU 서빙(vLLM + Qwen)은 공용 GPU 서버의 상시 실행·외부 포트 개방이 **불가**로 확정되어 폐기(2026-07-24).
  → **~~옛 LLM 게이트웨이 경유~~ → 2026-08-13 `api.openai.com` 직접 호출로 전환.** 프로젝트가 끝나 게이트웨이 키가 **2026-08-14 에 회수**되므로 그 경로 자체가 사라졌다. 벤더 선택(이 항목의 본론)은 그대로 유효하다 — 바뀐 것은 주소와 결제 주체뿐이다(대여받은 크레딧 → 팀의 OpenAI 플랫폼 계정, 달러 청구).
  → **옛 게이트웨이 이름을 딴 식별자도 함께 걷어냈다**: 키 환경변수는 `OPENAI_API_KEY` 로, 자바 클래스 4개는 `OpenAiConfig`·`OpenAiTaggingClient`·`OpenAiSummaryClient`·`OpenAiEmbeddingClient` 로, python 필드는 `openai_api_key` 로 바꿨다. 처음엔 "운영 `.env`·CI 시크릿에 이미 그 이름으로 값이 있으니 유지"로 결정했는데, **더 이상 우리 것이 아닌 조직의 이름을 저장소에서 전부 지우기로 하면서 뒤집혔다.**
  → ⚠️ **개명의 유일한 위험**: 이미지와 `.env` 의 이름이 어긋나면 값이 안 들어와도 **부팅은 성공하고 AI 기능만 조용히 꺼진다**(`OpenAiConfig` 의 `@ConditionalOnProperty` 가 빈 문자열에도 매칭되기 때문 — 그 클래스 주석 참고). 그래서 코드와 운영 `.env` 를 **같은 배포에서** 교체하고, 실제 호출 1회로 확인했다.
  → **환경변수 이름 `OPENAI_API_KEY` 는 일부러 그대로 뒀다.** 운영 서버 `.env`·compose·CI 시크릿에 이미 이 이름으로 값이 들어 있어서, 이름을 바꾸면 값이 안 들어와도 **부팅은 성공하고 AI 기능만 조용히 꺼진다**(`OpenAiConfig` 의 `@ConditionalOnProperty` 가 빈 문자열에도 매칭되기 때문 — 그 클래스 주석 참고). 클래스 이름 `OpenAiConfig` 도 같은 이유로 유지.
  → **이 전환이 크레딧 등급이라는 축을 없앴다** → 바로 아래 태깅 모델 결정이 뒤집힌 이유다.
- [x] ~~**AI 서버(투두 추천) 생성 모델 = `gpt-4.1-mini`**~~ → **2026-07-30 `gpt-5.4-nano`로 교체**(바로 아래 항목). 원래 근거: 추론 모델(`gpt-5-nano`)은 동일 응답에 숨은 추론 토큰 1,344개를 더 쓰고 5배 느렸다 → [EXPERIMENTS #2](EXPERIMENTS.md). 그 판단에는 크레딧 단가 축이 없었고, 그것이 아래 미확정 항목으로 남아 있었다.

- [x] **★ 추천 생성 모델 = `gpt-5.4-nano`** *(2026-07-30 사용자 지시)* — 위 `gpt-4.1-mini` 잠정 확정을 교체한다.
  → **⚠️ 이 결정은 사전 실측 없이 내려졌다.** 사용자가 "이 모델로 가고, 이 모델을 쓰는 이유는 결과를 내며 찾는다"로 확정했다. 이 파일의 통상 기준(모델 교체는 측정 결과를 남긴다, `ai/CLAUDE.md`)과 다른 순서이므로 **없는 근거를 있는 것처럼 적지 않기 위해 그대로 기록한다.**
  → **교체 직후 확인한 것**(단발 관측, [EXPERIMENTS #18](EXPERIMENTS.md)): 구조화 출력(`with_structured_output(include_raw=True)`)이 게이트웨이 프록시 + 이 모델에서 **통한다**(안 통하면 추천이 502로 죽으므로 최우선 확인 대상이었다). 같은 페이로드로 `gpt-4.1-mini` 12.34초 → `gpt-5.4-nano` **3.67초**, 후보 제목 중간값 약 26자 → 약 18자. 제목이 짧아진 것은 [#13](EXPERIMENTS.md)에서 "제목이 길어진다"고 관측한 문제와 반대 방향이다.
  → **사후 검증 결과(2026-07-30 같은 날)**: 실사용 트레이스에서 **시간 3.5배**(10.94초 → 3.13초), **비용 약 절반**($0.0017 → $0.0009, 토큰이 3.2K/3.3K로 사실상 동일하므로 차이는 전부 단가), **품질은 사용자가 출력을 대조해 합격 판정**. 즉 결정이 먼저였지만 세 축 모두 사후에 뒷받침됐다.
  → **남은 것은 둘이다.** ① **게이트웨이 크레딧 차감** — 랭스미스 비용은 OpenAI 공개 단가 기준이고 대시보드 크레딧은 토큰과 선형 대응이 아니다([#7](EXPERIMENTS.md)). ② **고유명사가 많은 자료에서의 원문 충실도** — 합격 판정은 오픽(추상적 학습 자료) 케이스에서 나온 것이고, `nano` 계열이 제목을 중간에서 잘라낸 전례는 장소·상호명 나열형에서 나왔다([#7](EXPERIMENTS.md)).
  → **부수 효과**: 추천 응답이 3초대로 내려와, 미확정 항목이던 "추천 응답 동기 유지 여부"가 사실상 닫혔다(폴링 전환을 고민할 이유가 없다).
  → **기본값이 네 곳에 있다.** `ai/src/modi_ai/config.py` · `ai/.env.example` · `deploy/.env.example` · `deploy/docker-compose.app.yml`(`${OPENAI_MODEL:-...}`). **코드만 바꾸면 운영은 그대로다** — 네 곳을 함께 맞췄다. 각 개발자의 `ai/.env`와 EC2의 `.env`는 로컬 전용 파일이라 **각자 갱신해야 한다.**
- [x] **임베딩 모델 = `text-embedding-3-small` (1536차원)** — 게이트웨이 프록시가 `/v1/embeddings`를 지원함을 실측 확인(2026-07-28). 프로토타입의 BGE-m3(1024차원)에서 교체됨.
- [x] **벡터 저장소 = Postgres `archive_items.embedding real[]`** *(2026-08-01 확정 `V7`)* — Pinecone 등 외부 벡터 DB 미사용. Postgres가 이미 있어 인프라 추가가 없고, **자료 행 자체의 컬럼**이라 자료가 지워지면 임베딩도 같이 없어진다(별도 테이블이 아니므로 `ON DELETE CASCADE`를 논할 것도 없다 — 이 문장의 이전 판이 pgvector 별도 테이블을 전제한 잔재였다).
  → **~~pgvector~~를 뒤집은 결정이다.** 착수해 보니 확정된 설계가 "FastAPI가 메모리에서 선별"이라 **Postgres는 벡터로 검색하지 않는다.** pgvector가 주는 것(전용 타입·거리 연산자 `<=>`·HNSW 인덱스) 중 **실제로 쓰는 것은 저장뿐**인데, 그 대가로 운영·로컬 이미지를 pgvector 빌드로 바꾸고 Testcontainers 테스트를 전부 손봐야 한다(아래 "pgvector 도입 시 확인된 사실" — 순정 `postgres:17-alpine`에 확장이 없다). 쓰지 않는 기능에 그 비용을 낼 이유가 없다.
  → Postgres에서 top-K를 돌릴 근거가 생기면 그때 옮긴다 — `real[]` → `vector`는 컬럼 하나짜리 마이그레이션이다.
  → `real`(4바이트)로 충분하다. 코사인 유사도 판정에 float8 정밀도가 필요하지 않고, `double precision[]`이면 행당 12KB가 된다.
  → **차원 제약은 걸지 않는다** — Postgres 배열의 선언 길이는 검사되지 않고, 모델을 바꾸면 차원이 달라진다. 섞인 차원은 읽는 쪽이 걸러야 한다.
  → Hibernate `float[]` ↔ Postgres `real[]`는 **우회 없이 기본 매핑으로 통과**했다(`SchemaValidationTest`, 실 Postgres + Flyway + `ddl-auto=validate`). 값 왕복(1536개)은 `ArchiveItemServiceTest`가 증명한다.
- [x] **AI 서버 분리(FastAPI)** — 근거는 셋이고, 이 순서로 무게가 있다. *(2026-07-28 재작성)*
  ① **Python 생태계** — LangChain·LangSmith·평가 도구가 Python 우선이다. Spring AI에는 LangSmith 연동도 동등한 평가 도구도 없다.
  ② **프로토타입 자산 재작성 비용** — 자료 15개와 `prompts.py`가 Python이다. *(2026-07-30: `validation.py`의 `STOPWORD_NOUNS`도 여기 있었으나 폐기됐다 — 이 논거는 그만큼 약해졌다.)*
  ③ ~~Kiwi 검증층~~ — **폐기됐다**(2026-07-30). 원래도 약한 논거였고(Kiwi 본체는 C++이고 Java 형태소 분석기도 있다) 이제는 코드 자체가 없다. 분리 근거는 ①·②로 유지된다.
  → **이건 마이크로서비스가 아니라 "언어 런타임 때문에 분리된 전용 워커"** 다. 자체 DB도 도메인 경계도 외부 노출도 없다. 도메인으로 쪼갠 게 아니라 런타임으로 쪼갰다.
  → 정당성의 기준: **함수 하나를 떼면 비용만 늘고, 기능 하나를 통째로 떼면 경계가 생긴다.** 이 서버는 추천의 프롬프트·LLM 호출·임베딩 검색을 전부 갖고 경계를 넘는 건 요청 1회다.
- [x] **★ 태깅은 Spring 유지 · FastAPI는 투두 추천 전용** *(2026-07-28)* — `wip`(`70701d4`)에 이미 구현·머지된 Spring 태깅을 FastAPI로 이관하지 **않는다.**
  ① **검증층의 가치가 두 기능에서 다르다.** 태깅이 틀리면 검색되지 않는 태그 하나가 남지만, 추천이 틀리면 존재하지 않는 가게로 헛걸음한다.
  ② ~~검증층은 원래 Qwen 때문에 필요했다~~ — **이 논거는 폐기됐다.** 검증층(Kiwi 자동 채점)을 2026-07-30에 전면 삭제했다(`EXPERIMENTS.md` "다시 하지 말 것"). 결정 자체는 ①·③으로 유지된다.
  ③ **태깅의 주력 경로는 비동기다.** URL 공유 등록은 `@Async`로 배경 처리되므로 FastAPI가 줄 수 있는 이점(응답 속도)이 값을 하지 않는다. *(2026-07-28 갱신 — 원래는 "모델 교체로 얻으면 되니 이관 불필요"라고 적었으나, 모델을 바꾸지 않기로 하면서 그 논거는 폐기했다.)*
  → 대신 태깅 프롬프트는 Java 문자열 리터럴에서 리소스 파일로 빼고 Python 평가 러너가 같은 파일을 읽게 한다(별도 이슈). 그래야 "프롬프트 변경은 평가셋으로 검증한다"가 언어와 무관하게 유지된다.
- [x] **★ 태깅·요약·추천 = `gpt-5.4-nano` 하나로 통일** *(2026-08-13 사용자 확정 — 아래 "태깅 = `gpt-5-nano` 유지"를 뒤집는다)*
  → **뒤집은 이유는 새 측정이 아니라 전제의 소멸이다.** 태깅만 다른 모델을 쓴 유일한 근거가 게이트웨이 크레딧 등급이었고, 게이트웨이 키 회수로 그 축이 사라졌다(위 벤더 항목). OpenAI 직접 과금에서는 **숨은 추론 토큰이 출력 토큰 요금으로 그대로 청구**되므로, 등급이라는 방패가 없어진 `gpt-5-nano` 는 느린 쪽이면서 **동시에 비싼 쪽**이 된다.
  → 태깅 워크로드 실측(운영 프롬프트·운영 서버·2라운드): `gpt-5-nano` 3.94~15.43초 · completion 478~1,818 tok(그중 **92.7~98.6% 가 숨은 추론**) vs `gpt-5.4-nano` 0.73~1.32초 · completion 25~36 tok(**추론 0/6회**) → [EXPERIMENTS #34](EXPERIMENTS.md). 원출력은 `evals/data/snapshots/2026-08-13_tagging_model_openai_direct.txt` 에 동결했다.
  → **결정적이었던 것은 평균이 아니라 편차다.** 같은 입력이 4.41초 → 15.43초로 튀었다(추론 448 → 1,792). 태깅 실패는 폴백이 삼키므로(태그 없이 등록 진행), 꼬리를 예측할 수 없는 모델은 "가끔 태그가 안 달린다"를 원인 없이 남긴다.
  → **관측된 유일한 회귀**: `gpt-5.4-nano` 가 `먹스타그램`·`인스타그램` 같은 플랫폼 이름 태그를 6회 중 3회 냈다(`gpt-5-nano` 는 0회). 정답 라벨이 없어 **측정이 아니라 눈으로 본 관측**이고, 태그 용도가 검색이라 무해하다고 판단했다. 프롬프트로 막을 수 있지만 이번에는 **모델만 바꿨다** — 두 변수를 같이 바꾸면 다음 측정에서 원인을 갈라 볼 수 없다.
  → 프로퍼티는 셋 그대로 둔다(`spring.ai.openai.chat.options.model` · `modi.archive.summary-model` · AI 서버 `OPENAI_MODEL`). 값만 같다 — 한 기능만 모델을 바꿔 재보는 일이 다시 생긴다.
- [x] ~~**★ Spring 태깅 모델 = `gpt-5-nano` 유지**~~ *(2026-07-28 · 같은 날 내린 `gpt-4.1-mini` 교체 결정을 뒤집음)* → **2026-08-13 폐기**(바로 위 항목). 아래 근거는 전부 게이트웨이 크레딧 등급에 매달려 있었고, 게이트웨이와 함께 무효가 됐다.
  뒤집은 이유는 **판단에 크레딧 축이 빠져 있었기 때문**이다. 게이트웨이 대시보드 기준 `gpt-5-nano` 1회 평균 3.0 크레딧(약 35,087회) vs `gpt-4.1-mini` 10.0 크레딧(약 10,278회).
  → **속도 근거도 무너졌다.** "태깅은 사용자가 스피너를 보며 대기하는 화면"이라는 전제가 틀렸다 — 실제 사용 경로인 URL 공유 등록은 `@Async`라 사용자를 붙잡지 않는다(`ArchiveCrawlProcessor:43`, 즉시 `PENDING` 저장 후 배경 처리). 동기인 경로는 앱 내 등록과 텍스트 공유뿐이다.
  → 단, 실측상 `nano`가 **총 토큰은 오히려 1.2~1.6배 더 쓴다**(reasoning 토큰 때문). 대시보드의 3.3배 차이가 우리 워크로드(입력 거대·출력 극소)에 그대로 적용되지는 않는다 → [EXPERIMENTS #7](EXPERIMENTS.md).
  → 정확한 단가는 대시보드의 크레딧 차감으로 확인 가능하다. **그 수치가 나오면 이 항목을 재검토한다.**
- [x] **본문 추출은 Spring이 담당** — Jsoup 크롤링(SSRF 방어 포함)이 `wip`에 이미 있고, 태깅이 Spring에 남으므로 그대로 둔다.
- [x] **★ 본문 정제는 시맨틱 태그·ARIA 역할만 본다 — 사이트별 클래스명은 넣지 않는다** *(2026-07-29)* — 노이즈 제거(`script`/`nav`/`footer`/`aside` 등) 후 `main`/`[role=main]`/`article` 중 **텍스트가 가장 긴 것**을 본문 루트로 쓰고, 그것이 body의 50% 미만이면 body로 폴백한다 → [EXPERIMENTS #12](EXPERIMENTS.md).
  → **`selectFirst("article")`은 쓰지 않는다.** 목록 카드마다 `<article>`을 쓰는 사이트에서 92자짜리 홍보 카드가 본문으로 선택돼 본문 3,384자가 통째로 날아갔다.
  → **`header`/`footer`는 `article`/`main` 바깥의 것만 지운다.** 위치를 안 보고 지우면 `<article><header><h1>글 제목</h1></header>`이 사라지는데, 태깅 프롬프트에 들어가는 건 `bodyText`뿐이라 **문서에서 정보 밀도가 가장 높은 한 줄이 AI 입력에서 통째로 빠진다**(리뷰에서 지적, 같은 날 수정).
  → **네이버 전용 셀렉터(`.se-main-container`)를 넣자는 안은 보류**다. 실사용 워크로드에서 잡음이 확실히 줄지만, 사이트별 추측을 코드에 박으면 네이버가 DOM을 바꿀 때 조용히 깨진다. 필요해지면 이 항목을 다시 연다.
  → **이 변경의 가치는 토큰이 아니다.** 모바일 네이버 기준 감소는 3~5%에 그친다. 실질 이득은 한 줄로 뭉개져 있던 텍스트가 **문단 구조를 갖게 된 것**이다.
- [x] **자료 제목은 AI로 생성하지 않는다** *(2026-07-28 · 2026-07-29 서버 수정 완료)* — 크롤러의 `og:title`이 AI 생성 결과와 **문자열까지 동일**했다 → [EXPERIMENTS #9](EXPERIMENTS.md). 공유 등록에서 제목이 URL로 남는 현상은 AI 문제가 아니라 `ArchiveCrawlProcessor`가 `crawled.title()`을 반영하지 않는 **서버 버그**였다.
  → **해결됨**: 에서 `ArchiveItem.markCrawlDone`이 제목을 받도록 고쳤다(저장된 제목이 아직 프리필된 URL 그대로일 때만 교체 — 사용자가 직접 입력한 제목은 유지). 예측대로 **LLM 호출 0회**로 끝났다. 상세는 `specs/0014-외부-공유-등록.md` 데이터 절.
  → 에뮬레이터 실측으로 확인: 같은 DB에서 옛 코드 자료는 제목=URL·개행 0, 새 코드 자료는 실제 글 제목·개행 25~125.
- [x] **★ 자료 요약(summary) 도입** *(2026-07-28 · AI 파트 결정)* — ~~태깅 호출에 **합쳐서** 받는다. 별도 LLM 호출을 추가하지 않는다.~~ → **2026-07-30 분리로 변경**(바로 아래 항목).
  ① **목적은 UI가 아니라 추천 비용 절감이다.** 추천은 아카이브 전체를 프롬프트에 넣으므로, 본문 대신 요약을 넣으면 자료 20건 기준 약 117,000 tok → 약 2,600 tok으로 **45분의 1**이 된다 → [EXPERIMENTS #8](EXPERIMENTS.md).
  ② **통합 비용이 사실상 0이다.** 태그만 받던 호출에 요약을 얹는 값이 prompt +47 tok / completion +16 tok. 호출 횟수는 1회 그대로다.
  ③ **본문을 자르지 않는다.** 4,000자로 자른 쪽보다 전체(8,823자)를 넣은 쪽이 결과가 깨끗했고 시간 차이는 0.5초였다([#8](EXPERIMENTS.md) 결론 2).
  → **환각 대응 방법은 미정.** 요약은 원문의 재진술이라 원문 문자열 대조로 검증할 수 없다([#8](EXPERIMENTS.md) 주의). 짧은 본문에 요약을 만들지 않을 임계값도 미정.
  → **★ 추천 프롬프트 튜닝은 요약 도입 뒤에 한다** *(2026-07-30 확정)* — 현재 러너는 본문을 그대로 넣는데, 그 상태로 프롬프트를 고치면 **곧 사라질 입력에 맞춰 튜닝하는 것**이 된다. 실제로 후보 제목이 길어지는 원인(중간값 22.5자·최대 40자)이 **원문에 병기된 주소·날짜·기관명을 베껴 넣는 것**이었고(`부산 부산진구 히떼 로스터리 커피 메뉴 및 운영시간 알아보기`), 요약이 들어가면 그 원인 자체가 사라진다. 그래서 프롬프트 규칙 5(제목 길이)를 손대지 않았다. 전/후 비교용으로 요약 도입 **전** 출력을 `ai/evals/data/snapshots/`에 동결해뒀다.
  → **AI 파트 밖의 작업이 딸려 있다** — `archive_items.summary` 컬럼(**`V5`** — `V4`는 2026-07-29 FCM 토큰이 가져갔다), 앱 표시, `specs/0010` 개정. 스키마·앱은 프로젝트 전체 영향이므로 **루트 [`specs/OPEN.md`](../../specs/OPEN.md)에 올려 팀 합의를 받는다**(이 파일은 AI 내부 결정만 다룬다). ~~Spring 태깅 프롬프트의 JSON화~~는 아래 분리 결정으로 **불필요해졌다.**
- [x] **★ 요약은 태깅과 합치지 않고 별도 호출로 받는다** *(2026-07-30 사용자 확정 — 위 "합쳐서 받는다"를 뒤집음)*
  → **뒤집은 이유는 토큰이 아니라 결합도다.** 합치려면 이미 잘 돌고 있는 태깅 프롬프트를 쉼표 구분에서 JSON으로 바꿔야 하고, 그러면 **JSON 파싱이 깨질 때 태그까지 함께 날아간다.** 분리하면 태깅 코드를 한 줄도 건드리지 않고 요약과 태그가 독립적으로 실패한다.
  → **대가**: 등록 시 본문을 한 번 더 보낸다(통합이 prompt +47 tok이었던 것에 비해 자료당 약 5,834 tok, [#8](EXPERIMENTS.md) 기준). 다만 그건 **자료당 1회**이고, 요약을 쓰는 진짜 이득(추천 프롬프트 45분의 1)은 추천을 누를 때마다 반복되므로 그대로 남는다.
  → 구현: `domain/archive/client/AiSummaryClient` + `OpenAiSummaryClient`. 호출부는 `ArchiveItemService`(동기 2경로)와 `ArchiveCrawlProcessor`(비동기 URL 경로) 셋. 실패는 태깅 실패와 같은 폴백 — 요약 없이 등록을 진행한다.
- [x] **★ 요약 모델 = `gpt-5.4-nano`** *(2026-07-30 사용자 확정)* — 당시엔 태깅(`gpt-5-nano`)과 **일부러 달랐다**(2026-08-13 태깅이 이 값으로 합쳐져 이제는 같다 — 위 통일 항목). `modi.archive.summary-model` 프로퍼티, `ARCHIVE_SUMMARY_MODEL`로 덮을 수 있다.
  → 근거: `gpt-5-nano`로 요약 프롬프트를 재보니 **completion의 94~97%가 숨은 추론 토큰**이었고(150자 요약에 추론 3,520 tok) 12.9~23.9초가 걸렸다. `gpt-4.1-mini`는 1.5~2.6초·completion 52~101 tok으로 **품질이 동등**했다 → [EXPERIMENTS #14](EXPERIMENTS.md).
  → `gpt-5.4-nano`를 고른 이유는 **nano 등급 크레딧을 유지하면서** 그 추론 낭비를 피할 수 있어 보이기 때문이다(사소한 프롬프트에서 completion 4 tok vs `gpt-5-nano` 74 tok). 크레딧 축을 재지 않고 모델을 정하는 것이 [#2](EXPERIMENTS.md)를 뒤집은 실수였으므로, 이번엔 등급을 안 내리는 쪽을 골랐다.
  → **⚠️ 미측정으로 남은 것 = 크레딧 축 하나뿐이다.** `gpt-5.4-nano`로 실제 요약 프롬프트를 재는 것은 [#14](EXPERIMENTS.md)에 실측 열로 채웠다(1.3~2.8초 · completion 76~144 · 추론 0). 크레딧 차감은 게이트웨이 대시보드로만 확인 가능해 아직 안 했다 — 등록이 느리거나 크레딧이 예상보다 빠지면 여기부터 의심한다.
  → ~~**태깅도 `gpt-5.4-nano`가 나을 수 있다** — 별도 확인 대상.~~ → **2026-08-13 닫혔다**: 태깅도 `gpt-5.4-nano` 로 통일했고, 태깅 워크로드로 재보니 추론 비중 92.7~98.6% 였다 → [#34](EXPERIMENTS.md). 이제 **요약과 태깅은 같은 모델**이므로 이 항목의 "일부러 다르다"는 더 이상 사실이 아니다.
- [x] **★ 요약 프롬프트 = To-do 중심 · 평서문 · 계정명/날짜 제외 · 고유명사 핵심만** *(2026-07-30, v3 — 사용자 작성)* — 세 바퀴 돌려 확정했다. 상세는 [EXPERIMENTS #15](EXPERIMENTS.md).
  → **교훈: 규칙을 늘리는 게 아니라 구체적으로 쓰는 것이 효과였다.** v2는 "과거 경험·감상·배경 제외"라고 추상적으로 써서 계정명(`Instagram의 …님이`)을 못 걸렀고, "To-do 중심"이 오히려 상호명 16개 나열을 유도해 231자(스스로 요구한 200자 위반)가 나왔다. v3는 `작성자 계정명(인스타그램 등)`·`핵심 3~4개만`처럼 **이름을 대고** 지시해서 프롬프트가 v2보다 짧아졌는데도 3건 모두 200자 이내로 들어왔다.
  → 각 규칙이 살아 있는지 `OpenAiSummaryClientTest.promptCarriesEveryRuleThatAMeasurementJustified()`가 지킨다. 규칙을 지우면 그것이 막던 문제가 되살아난다.
  → **⚠️ `temperature`를 고정하지 않고 잰 값이다.** #15·#32 표의 자릿수를 품질 지표로 쓰지 말고 "길이 상한을 넘겼는가 / 계정명이 남았는가" 같은 이진 관측만 근거로 쓴다. *(2026-08-04: 원래 "200자를 넘겼는가"였는데 상한이 300자로 바뀌어 문장이 틀리게 됐다 — 숫자를 박지 않는 쪽으로 고쳤다.)*
- [x] **★ 요약 프롬프트 v4 = `4~6문장 · 300자 이내`** *(2026-08-04 사용자 요청 "지금의 두 배")* — v3 의 `2~3문장 · 200자`를 교체한다. 규칙(제외·평서문·고유명사)은 **그대로**이고 길이 지정만 바꿨다. 근거는 [EXPERIMENTS #32](EXPERIMENTS.md) — 픽스처 15건을 네 조합으로 재봤다.
  → **요청은 두 배였지만 실측 1.43배가 한계다**(평균 107.0 → 152.9자). **200자 상한은 원래 안 걸리고 있었다**(기준선 최대 167자) — 길이를 정하던 것은 상한이 아니라 문장 수였다. 그래서 문장 수를 함께 올렸고 증가분의 대부분이 거기서 왔다.
  → 🔴 **하한을 넣으면 두 배가 되지만 `제외` 규칙을 깬다.** `내용이 충분하면 250자 이상`으로 평균 181.0자(1.69배)까지 갔는데, 원문이 얇은 자료(`009`)에서 모델이 분량을 채우려고 **계정명을 집어넣었다** — v3 가 가장 먼저 잡은 문제다. "분량을 채우려고 제외 항목을 넣지 마라"를 명시해도 **그대로 유출했다.** 알려진 회귀를 안고 숫자를 맞추지 않았다. **하한을 되살리지 말 것.**
  → **아직 안 재본 지렛대 = 고유명사 상한 `핵심 3~4개`.** 이 숫자는 200자 기준으로 정해졌다(#15 — 16개 나열로 231자). 300자면 여유가 있으므로 이걸 늘리면 `제외` 규칙을 안 건드리고 상호가 많은 자료만 길어질 수 있다.
  → **⚠️ 추천 품질로는 안 쟀다.** 요약은 추천 프롬프트의 입력인데 #32 는 요약 자체만 봤다. 방 평가를 안 돌렸으므로 후보 품질 변화는 **모른다.** 프롬프트 예산(12,000자)에서 자료당 비용이 약 1.4배가 되는 것도 미측정이다 — 자료 50건쯤에서 순위 뒤쪽이 버려지기 시작한다.
  → 컬럼 폭은 안 건드렸다(`VARCHAR(500)`, 실측 최대 231자). 400자로 올릴 때는 마이그레이션이 필요하다.
- [x] **★ 요약 서식(줄바꿈·강조)은 프롬프트로 받지 않는다 — 화면에서 나눈다** *(2026-08-08, 사용자 확정. `v4` 프롬프트 유지)* — 자료 상세의 요약이 통짜라 읽기 어렵다는 QA에 대해 프론트가 ①문단 `\n` ②마크다운 `**강조**` 중 택일을 요청했다(`docs/backend/archive-summary-formatting-handoff.md`). **재보고 둘 다 기각했다** — 근거는 [EXPERIMENTS #33](EXPERIMENTS.md), 크레딧 45회.
  → 🔴 **모델이 줄바꿈 규칙을 절반만 지킨다** — `7/15`, `8/15`로 두 번 재현됐다. 그리고 **어길 때는 예외 없이 통째로 한 줄**이다(부분 준수가 없다). **자료마다 다르게 보이므로 QA 문제가 절반만 해결된다.**
  → 🔴 **강조 규칙은 `고유명사 제어`·`제외`와 정면으로 싸운다.** `핵심 3~4개` 범위를 지킨 것이 **2/15**뿐이고(한 자료는 11개), **계정명을 강조까지 했다**(`**COMBINE**님의`, `**꿈꾸는라이언**`). v4 동결본은 계정명 0건이다 — #15가 가장 먼저 잡은 문제가 되살아났다. [#32 ②](EXPERIMENTS.md)의 "규칙끼리 충돌하면 말로 우선순위를 정해도 안 먹는다"가 **다른 규칙 쌍에서 재현된 것**이다.
  → 🔴 **어투도 무너진다** — `가져보세요`·`따라가라`·`확인하라`. `평서문(~한다, ~다)` 규칙을 그대로 뒀는데도 그렇고, 기준선은 **0/15**다. 그리고 300자 초과가 1건(310자) 실제로 났다.
  → **그래서 저장값을 한 글자도 바꾸지 않고 앱이 문장 경계에서 줄을 바꾼다**(`app/lib/features/archive/summary_text.dart`). 프롬프트가 평서문을 못 박고 있으니 문장 경계는 이미 문법으로 정해져 있다 — 개행 **100%**, 어투·계정명 회귀 **0**, 추천·임베딩 입력 **완전 동일**(그래서 방 평가가 필요 없다), 이미 등록된 자료도 **재요약 없이** 즉시 고쳐진다.
  → ⚠️ **볼드는 없앤 게 아니라 보류다.** 앱은 볼드를 만들 수 없다(어떤 단어가 핵심 상호명인지는 모델만 안다). 다시 볼 때의 출발점은 #32 ④와 같은 자리 — 개수를 강조와 **같은 규칙 안에서** 다루는 것. 루트 `specs/OPEN.md`에 기록했다.
  → ⚠️ **측정 도구가 한 번 거짓말을 했다**(#33 ⑤). 문장을 `~다.`로 세다가 어투 이탈 때문에 4문장을 1문장으로 세어 **개행 미준수를 준수로 집계**했다. **측정 대상이 흔들릴 수 있는 축을 계측기의 전제로 쓰지 말 것.**
- [x] **AI 태깅 실패 시 태그 없이 등록 진행** — `wip` 구현의 폴백 정책을 유지한다(루트 `specs/OPEN.md` 2026-07-27 확정).
- [x] ~~**LangGraph 미도입**~~ → **2026-08-01 도입으로 뒤집힘**(바로 아래 항목). 원래 근거: 두 기능 모두 LLM 호출 1회 구조라 그래프가 불필요. time travel은 체크포인터 영속화를 요구해 "AI 서버는 도메인 DB를 직접 조회하지 않는다"는 원칙과 충돌한다.

- [x] **★ LangGraph 도입 — 추천 파이프라인을 `StateGraph` 로** *(2026-08-01 사용자 결정)*
  → **⚠️ 오늘 기준 기능 이득은 0이다.** 직선 파이프라인을 노드 4개로 나눈 것뿐이고 동작이 하나도 안 바뀐다. **`langchain-core` 채택 때와 같은 근거**(팀 역량·포트폴리오)로 사용자가 결정했고, 그 판단을 감추지 않으려고 여기 적는다. 이 문서가 `langchain-core` 항목에 "기술적 필요가 아니라 팀 역량·포트폴리오 판단이다"라고 적어둔 것과 같은 성격이다.
  → **다만 금지 근거였던 "LLM 호출 1회 구조"는 이미 사실이 아니었다.** 이후 외부 호출이 **2회**(LLM + 임베딩)이고 폴백도 **둘**(파싱 실패 → 502, 임베딩 실패 → 그대로 통과)이다. 옛 항목이 적어둔 재검토 조건("재시도·폴백·조건 분기가 실제로 필요해지면")에 닿아 있었다.
  → **합격 기준은 "계약이 한 글자도 안 바뀐다"였다.** 입력·출력·예외(502)·로그·폴백 동작 전부. **기존 테스트가 하나도 안 바뀌고 통과**하는 것이 그 증거다(전환 시점 187개).
  → 실서버 왕복도 했다 — 기존 카테고리 `공부` 하나뿐인 방에 강릉 맛집 + 정처기 자료를 넣어 `POST /v1/todo-suggestions` 를 부르니 `[여행] 강릉에서 식사 3번 맛집 투어하기` · `[여행] 강릉 카페 3곳 일정대로 방문하기` · `[자격증] 정처기 실기 기출 반복 풀이하기` · `[자격증] 정처기 실기 오답노트 작성하기` 가 나왔다(전환 전이었다면 넷 다 `[공부]`).
  → **그래프가 표현하지 않는 것**: 폴백 둘은 여전히 `try/except` 다. 성공·실패가 **같은 다음 노드로 가서** 조건부 엣지로 옮기면 분기처럼 보이지만 실제로 갈리는 곳이 없다. 진짜 분기는 `_has_candidates`(후보가 없으면 임베딩을 건너뛴다) 하나뿐이다.
  → **체크포인터는 안 쓴다** — 옛 항목의 "도메인 DB를 직접 조회하지 않는다" 원칙은 그대로 유효하다. 메모리 상태만 쓰는 `compile()` 이다.
  → 비용: 패키지 53 → 58(`langgraph`·`langgraph-checkpoint`·`langgraph-prebuilt`·`langgraph-sdk`·`ormsgpack`). `uv.lock` 을 함께 커밋해야 CI(`uv sync --frozen`)가 산다.
- [x] **LangSmith 도입 · 개발 환경 전용** *(2026-07-28 도입 확정)* — 프롬프트를 깎는 과정을 트레이스로 보고, 평가셋을 `langsmith.evaluate()`로 돌리기 위해 쓴다. `LANGSMITH_TRACING=false`가 **운영 기본값**이다. 켜면 사용자 자료 본문이 제3자 서버에 저장되며, 개인정보 처리위탁 명시와 Play Console 데이터 보안 신고 대상이 된다.
  → **2026-07-29 구현 완료** (`modi_ai/tracing.py`). 배선이 별도로 필요했다 — pydantic-settings는 `.env`를 `Settings` 객체에만 채우고 `os.environ`에는 넣지 않는데 LangSmith SDK는 `os.environ`만 본다. 실측으로 확인한 함정 셋: ① 이 간극 자체 ② `langsmith.utils.get_env_var`가 `lru_cache`라 환경변수를 나중에 바꿔도 첫 읽기 값을 물고 있다 ③ SDK가 `LANGCHAIN_TRACING_V2`를 `LANGSMITH_TRACING`보다 먼저 보므로 셸에 남은 구세대 값이 설정을 이긴다. 키가 비어 있으면 `true`여도 켜지 않고 경고만 남긴다.
  → **더 단순한 대안이 있었다(리뷰 지적)**: `uv run --env-file .env ...`를 쓰면 값이 import 전에 프로세스 env에 들어가 ②·③이 애초에 생기지 않는다. 그래도 모듈을 유지하는 이유는 남는 가치 둘이다 — **끌 때 `false`를 명시적으로 박는 것**(셸에 남은 값이 설정을 이기지 못하게)과 **키가 없으면 안 켜는 것**. 실행 방법에 의존하지 않는 안전장치가 낫다고 판단했다.
  → **운영 방어는 두 겹이다** *(2026-07-30 dev 머지로 완성)*. ⓐ 코드 기본값이 `false` ⓑ `deploy/docker-compose.app.yml`의 `ai` 서비스가 `LANGSMITH_TRACING: "false"`를 **하드코딩**한다. pydantic-settings가 `.env`보다 `os.environ`을 먼저 보므로 컨테이너 env로 못 박는 것이 가장 확실하다. 개발용 `ai/.env`는 이미지에 들어가지 않는다(`ai/Dockerfile`이 `pyproject.toml`·`uv.lock`·`src`만 복사한다). 기동 로그에 `LangSmith 추적 켜짐`이 보이면 즉시 끈다.
  → **`kiwipiepy` 같은 dev 의존성도 운영 이미지에서 빠진다** — `ai/Dockerfile`이 `uv sync --frozen --no-dev`로 빌드한다. 이전에 "지켜야 할 조건"으로 적어뒀던 것이 실제로 충족됐다.
  → **⚠️ import 시점에 배선하면 안 된다 (2026-07-30, 리뷰 발견 후 수정).** 원래 `main.py`가 모듈 최상단에서 `configure_tracing()`을 불렀는데, 그러면 **이 모듈을 import하는 모든 것**(pytest 수집 포함)이 개발자 `.env`의 실제 키로 추적을 켜버린다. 실제로 pytest가 그 상태로 돌고 있었고, 업로드가 없던 이유는 `FakeChatModel`이 langchain Runnable이 아니어서일 뿐이었다.
    → **FastAPI `lifespan`(기동 시점)으로 옮겼다.** LLM 클라이언트는 요청마다 `Depends(get_llm)`로 만들어지므로 기동 시점이면 충분히 이르다. 실측: import 직후 `tracing_is_enabled() == False`, `TestClient` 기동 후 `True`.
    → 여기에 `ai/tests/conftest.py`의 세션 autouse 픽스처가 이중으로 끈다. 그 픽스처는 env를 지우는 것만으로 부족해서(`langsmith.utils.get_env_var`가 `lru_cache`라 이미 읽힌 값이 남는다) 캐시를 비우고 **실제로 꺼졌는지 단언**한다 — 주장과 구현을 일치시켰다.
  → **Prompt Hub를 프롬프트의 진실로 삼지 않는다.** 원본은 git에 둔다. LangSmith UI에서 편집하고 런타임이 `hub.pull()`로 당겨가면 평가셋 게이트·코드 리뷰·롤백이 전부 무력화된다. Playground는 **후보를 탐색하는 용도**로만 쓰고, 채택된 프롬프트는 git에 커밋한 뒤 평가셋을 돌린다.

- [x] **`langchain-core` + `langchain-openai` 도입** *(2026-07-28)* — **기술적 필요가 아니라 팀 역량·포트폴리오 판단이다.** 현재 범위(LLM 호출 1회)에서 능력 이득은 사실상 0이고, 구조화 출력은 `openai` SDK의 `client.chat.completions.parse()`로 이미 된다(실측: openai 2.49.0). 다만 비용이 감당 가능하고(패키지 28 → 46), 리트리벌·리랭킹이 붙으면 값을 하기 시작한다.
  → **`langchain` 풀 패키지는 여전히 쓰지 않는다.** 다만 사유가 바뀌었다 — 원래는 `langchain==1.3.14`가 `langgraph`를 하드 의존성으로 끌고 오는 것이 LangGraph 미도입 결정과 충돌해서였는데, **2026-08-01 `langgraph`를 의도적으로 넣었다**(위 항목). 지금 남은 사유는 **필요한 것만 명시적으로 넣는다**는 것 하나다 — 풀 패키지는 그 밖의 것까지 끌고 온다.
  → LangSmith 연동은 LangChain의 전제가 **아니다** — `wrap_openai()`·`@traceable`로 순수 Python에서도 동일하게 된다. 둘은 독립적으로 판단했다.
- [x] **패키지 관리 = uv** — Docker/Jenkins 재현성(lock)과 설치 속도. Python 버전도 uv가 관리해 로컬/CI 드리프트를 없앤다.
- [x] **★ 검증층(Kiwi 자동 채점) = 폐기** *(2026-07-28 도입 → 2026-07-30 전면 삭제)* — 프로토타입에서 이식했다가 지표 셋이 전부 무효로 판명돼 코드·라벨·문서를 다 지웠다. 상세는 [`EXPERIMENTS.md`](EXPERIMENTS.md) "다시 하지 말 것".
  → 요지: 원문 포함 검사는 **구조적으로** 헛경보를 낸다(추천은 문장이고 원문에는 그 문장이 없다). 고유명사 손상 탐지는 **잡을 게 없었다**(Qwen의 실패이고 우리 모델은 0건). `has_actionable_content` 라벨은 **태깅용**인데 추천 판정에 갖다 써서 잘못된 결론을 냈다.
  → **교훈: 지표를 먼저 만들고 데이터를 맞추지 않는다.** 실제 모델의 출력을 눈으로 보고 무엇이 문제인지 찾은 뒤에 지표를 정의한다. 반대로 해서 이틀을 썼다.
  → **런타임 게이트가 필요 없다는 판단은 그대로 유효하다** — `full_spec.md` §S-16-B가 "AI 로딩 → 후보 목록 → Add"이므로 사용자가 후보를 보고 고른다. 환각은 사용자가 거절하는 후보 한 줄로 끝난다.
  → 새 지표를 만들 때도 **`kiwipiepy`를 되살리려면 위 "다시 하지 말 것"을 먼저 읽을 것.**
- [x] **★ 옛 LLM 게이트웨이가 구조화 출력을 지원한다 → 수동 JSON 파싱 폴백 미구현** *(2026-07-29 실측)* — `with_structured_output(..., include_raw=True)`이 프록시를 그대로 통과했다 → [EXPERIMENTS #10](EXPERIMENTS.md). 착수 전 최대 리스크였고, 안 됐다면 프롬프트로 JSON을 강제하고 손파싱 + 재시도를 만들어야 했다. **만들지 않는다.**
  → `include_raw=True`를 쓰는 이유는 둘이다: 토큰 사용량(`usage_metadata`)을 로깅할 수 있고, 파싱 실패가 예외가 아니라 값(`parsing_error`)으로 돌아와 502로 정직하게 내보낼 수 있다.
- [x] **★ `INTERNAL_API_KEY`가 비어 있으면 검증을 건너뛴다** *(2026-07-29)* — `config.py` 주석이 가리키던 "키가 필요한 엔드포인트를 만들 때 그 시점"이 왔다. `FirebaseConfig`/`OpenAiConfig`의 "키가 없으면 그 기능만 비활성, 나머지는 그대로 동작" 패턴과 같은 방향이다.
  → 근거: 이 서버는 외부에 포트를 열지 않고(Docker 내부망), 로컬에서 `curl`로 확인할 수 있어야 하며, CI에도 키가 없다. 운영 compose에서는 반드시 주입한다.
  → 비교는 `secrets.compare_digest`로 한다(타이밍 공격 회피).
- [x] **★ 추천 1단계는 자료 개수·본문 길이 상한을 두지 않는다. 대신 토큰 수를 로깅한다** *(2026-07-29)* — 상한값을 지금 박으면 근거 없는 숫자가 코드에 남는다(아래 미확정 "전체 투입 ↔ top-K 전환 임계값"과 같은 문제). 실측을 먼저 쌓고 2단계에서 정한다.
  → 자료 2건 기준 prompt 1,078 tok([EXPERIMENTS #10](EXPERIMENTS.md)). 프로토타입 방은 자료가 적어 폭주 위험이 없다.
- [x] **★ 후보 검증은 스키마 제약으로 한다 — `source_item_id` 필수화** *(2026-07-29)* — 각 후보가 근거 자료의 id를 직접 밝히게 하고, 입력에 없는 id를 가리키면 코드가 버린다(`suggest.filter_candidates`). 프롬프트로 "지어내지 마라"라고 애원하는 대신 출력 형식으로 강제한다.
  → 자료가 0건인 방에서는 이 검사를 건너뛴다(근거를 요구할 수 없다). ~~중복 제거는 문자열 정규화(공백 제거+소문자)까지만 — 의미 단위 중복은 임베딩이 붙는 2단계.~~ → **2026-07-30 구현됨**(바로 아래 항목).

- [x] **★ "2단계"에 뭉쳐 있던 두 작업을 분리한다** *(2026-07-30, 에서 발견)* — 이 문서가 여러 곳에서 "의미 단위 중복은 임베딩이 붙는 **2단계**"라고 적어 **임베딩을 쓴다는 공통점만으로 서로 다른 작업 둘을 한 덩어리로 묶어놨다.** 실제로는 임베딩 **대상이 다르고 의존 관계도 없다.**

  | | 임베딩 대상 | 해결하는 것 | 저장 필요? |
  |---|---|---|---|
  | **(B) 후보 의미 중복 제거** | 후보 제목 + `excluded_todos` | 반복 노출되는 비슷한 후보 | **아니오** — 요청 시점의 짧은 문자열 |
  | **(A) 자료 하이브리드 선별** | 아카이브 자료 + 방 목표 | 프롬프트 비용·잡음(자료가 많아질 때) | 예 — 컬럼 필요 |

  → **(B)가 (A)에 의존하지 않는다.** 이걸 몰라서 "RAG 2단계를 해야 의미 중복이 고쳐진다"고 오판했다. (B)는 스키마 변경 0·Spring 변경 0·새 의존성 0으로 끝났다.
  → **우선순위도 이 발견으로 뒤집혔다**: (B)는 실측으로 관측된 증상을 고치고([#17](EXPERIMENTS.md)), (A)는 자료가 8건인 지금 이득이 없는 예방이다. 그래서 (B)를 먼저 했다.

- [x] **★ 후보 의미 중복 제거 = 임베딩 코사인, 임계값 0.70** *(2026-07-30 구현)* — 문자열 정규화로 24/24 전부 고유로 판정된 실제 후보들이 눈으로 보면 같은 할 일이었다(유튜브 모의고사 3회 반복) → [EXPERIMENTS #17](EXPERIMENTS.md).
  → **임계값은 재고 정했다.** 실제 후보 24개로 쌍별 코사인을 측정: 중복이어야 하는 쌍 `min 0.527`, 아니어야 하는 쌍 `max 0.648`. **구간이 겹쳐 단일 임계값으로 라벨 전부를 가르지 못한다** — 제목이 짧고 전부 같은 도메인이라 코사인이 넓게 퍼지지 않는다.
  → **그래서 "완벽한 분리"를 목표로 삼지 않고 `오탐 0`을 기준으로 잡았다.** 오탐은 쓸 만한 새 후보를 **조용히** 지우고(사용자는 있었는지도 모른다), 미탐은 중복이 한 번 더 보이는 것 = 고치기 전 상태다. 게다가 "회당 8개 vs 제외 창 50개라 6~7번이면 후보가 마른다"가 이미 관측돼 있어 과하게 거르면 빈 화면이 더 빨리 온다. 재현율은 7/11이고 **놓치는 4쌍을 #17에 명시**했다.
  → **저장하지 않는다.** 비교 대상이 요청 시점에 손에 있는 짧은 문자열(제외 창 최대 50 + 후보 최대 8)이라 배치 1회로 끝난다(실측 24개 2.73초). 자료 임베딩과 달리 컬럼이 필요 없다.
  → **`filter_candidates`는 손대지 않았다.** 순수 함수로 유지하고 뒤에 층을 하나 더 붙였다(`drop_semantic_duplicates`가 `embed`를 주입받는다) — 이 파일의 "크레딧 0으로 로직 전체를 테스트한다"는 성질을 깨지 않기 위해서다.
  → **실패는 삼키되 `error`로 남긴다.** 임베딩이 죽으면 문자열 중복 제거 결과를 그대로 반환한다(후보는 이미 만들어졌고 사용자가 봐야 한다). `warning`으로 두면 "추천은 되는데 비슷한 게 계속 나온다"로 조용히 되돌아가는데, 그 조용함이 정확히 을 필요하게 만든 원인이었다.
  → **`OpenAIEmbeddings`는 이미 설치돼 있었다** — `pyproject.toml`·`uv.lock` 무변경. `config.py`의 `openai_embedding_model` 상수도 원래 있었고 쓰는 코드가 없었을 뿐이다.
- [x] **`langchain-core` 1.5.x + `langchain-openai` 1.4.x 설치 실측** *(2026-07-29)* — 패키지 53개로 늘었고 **`langgraph`는 들어오지 않았다**(확인 완료). "풀 패키지 금지, `langchain-core`만" 결정이 실제로 유효함을 재확인. `langsmith`는 `langchain-core`의 전이 의존성으로 들어오지만 `LANGSMITH_TRACING=false`가 기본이라 추적은 꺼져 있다.
- [x] **이미지 OCR 미지원** — 스크린샷은 참고자료로만 저장.
- [x] **투두 추천은 매번 전체 재생성** — 증분(새 자료만) 아님. 자료 간 맥락이 필요하기 때문.
- [x] **AI 후보가 투두로 채택될 때 담당자는 미지정** — 루트 `specs/0002-data-model.md`와 이미 일치.
- [x] **★ `has_actionable_content` 판정 주체 = ⓐ Spring 태깅 프롬프트에 필드 추가** *(2026-07-29 사용자 확정)* — `archive_items` 컬럼으로 영속화한다.
  → 요약 도입으로 이미 확정된 태깅 JSON화·`V5` 마이그레이션에 필드 하나만 얹는다 — 추가 LLM 호출 0회(요약 통합과 같은 논리, [EXPERIMENTS #8](EXPERIMENTS.md)).
  → 자료당 1회 판정·영속화되므로 추천 프롬프트에서 '빈 껍데기' 자료를 **보내기 전에** 제외할 수 있다. 기각한 ⓑ(FastAPI 추천 시점 판정)는 이미 프롬프트에 실어 보낸 뒤라 입력 토큰 절감이 없다 — 실제 본문이 자료당 약 6.8k tok임이 실측되어([EXPERIMENTS #11](EXPERIMENTS.md)) 사전 제외의 가치가 커졌다.
  → **⚠️ 2026-07-30 철회.** 하루 전 "빈껍데기 9건 중 8건에서 후보를 냈다(억제율 11.1%)"를 실측 근거로 적었으나 **잘못된 결론이었다.** 그 판정에 쓴 라벨이 바로 이 `has_actionable_content`인데, 라벨의 뜻은 "태그로 달 **구체적 장소명**이 있나"이지 "추천이 침묵해야 하나"가 아니다. 후보를 직접 확인하니 **전부 원문에 근거가 있고 쓸 만한 할 일**이었다(`"더 많은 후기는 네이버 이기적 스터디 카페"` → `"이기적 스터디 카페에서 후기 찾아보기"`). 상세는 [`EXPERIMENTS.md`](EXPERIMENTS.md) "다시 하지 말 것".
  → **오히려 반대 방향의 주의가 필요하다** — 이 플래그로 자료를 사전 제외하면 위 같은 **좋은 추천을 버리게 된다.** 목적이 토큰 절감인 것은 그대로지만, **무엇을 `false`로 볼지의 정의를 추천 기준으로 다시 잡아야 한다.** 태깅용 정의를 그대로 쓰면 안 된다.
- [x] **★ RAG 2단계 — 임베딩 top-K 검색의 주체 = ⓑ FastAPI가 메모리에서 선별** *(2026-07-29)* — Spring이 자료의 **요약 + 벡터**를 요청에 실어 보내고, 유사도 계산·선별·리랭킹은 FastAPI가 받은 것 안에서 한다.
  → 기각한 대안 ⓐ는 "Spring이 pgvector로 top-K를 뽑아 그것만 보낸다"였다. 그러면 리트리벌 로직이 Spring에 흩어져 평가셋으로 튜닝할 수 없고, 프롬프트와 검색을 한 몸으로 재는 일이 불가능해진다.
  → 이 안은 **"도메인 DB 직접 접근 금지"**(`CLAUDE.md`)를 지키면서 리트리벌·리랭킹을 AI 서버에 남기는 유일한 조합이다. 대신 요청 크기가 커지므로 전송량 상한은 아래 미확정 항목과 함께 정한다.
- [x] **★ RAG 2단계 — 선별 기준 = 하이브리드(유사도 + 최근성 + 핀/좋아요), 융합은 RRF(k=60)** *(2026-07-29)*
  → **순수 top-K를 기각한 이유**: 목표와 의미적으로 덜 붙는 자료가 **영구히** 후보에서 빠진다. 방금 담은 자료나 팀이 좋아요를 누른 자료가 코사인 유사도 하나로 밀려나는 것은 S-16-B의 의도와 다르다.
  → **RRF를 쓰는 이유**: 세 축의 스케일이 전혀 다르다(코사인 0~1 · 경과일수 · 정수 카운트). 점수를 직접 가중합하면 정규화 상수가 근거 없는 숫자로 코드에 남는다. 순위만 쓰는 RRF는 그 문제를 아예 없앤다.
  → **`k=60`은 원 논문의 관례값이지 우리 데이터로 잰 값이 아니다.** 평가셋으로 튜닝한다.
  → **★ 축이 넷으로 갈리고 가중치가 정해졌다** *(2026-08-01 사용자 확정)* — 원래 `핀/좋아요`를 한 덩어리로 적었는데 **둘은 다른 신호**다. 좋아요는 "팀이 좋다고 한 것", 핀은 "계속 보겠다는 것"이다.

    | 축 | 가중치 |
    |---|---|
    | 좋아요 | **3.0** |
    | 핀 | **2.5** |
    | 유사도 | **2.0** |
    | 최근성 | 1.0 |

    ~~즉 `점수 = Σ wᵢ / (k + 등수ᵢ)` 의 가중 RRF다.~~ 사용자 의도는 **"팀이 명시적으로 표시한 것을 유사도보다 위에 둔다"**. (가중치는 유효하고, **융합 방식만 아래에서 뒤집혔다**.)
  → **⚠️ 동점은 반드시 같은 등수를 줘야 한다(competition ranking).** 실서버 자료 17건 중 **핀 0건 · 좋아요 1건**이다. 16건이 전부 0으로 동점인데 이걸 억지로 1·2·3등으로 줄 세우면 **그 순서는 정렬 방식이 우연히 정한 것**이고, 거기에 최대 가중치(3.0)를 주면 **아무 의미 없는 순서가 결과를 지배한다.** 동점을 같은 등수로 묶으면 그 축의 기여가 모두에게 동일해져 **신호가 없을 때 조용히 빠지고**, 좋아요가 쌓일수록 자동으로 세게 먹는다. **이 처리 없이 가중치를 크게 주면 안 된다.** → 구현됨(`ranking.competition_ranks`).
- [x] **★★ 융합은 RRF가 아니라 「순위 정규화」다 — 위 RRF 확정을 실측으로 기각했다** *(2026-08-02 사용자 확정 구현 완료, [EXPERIMENTS #22](EXPERIMENTS.md))*

      점수 = Σ wᵢ × (꼴찌등수ᵢ − 등수ᵢ) / (꼴찌등수ᵢ − 1등수ᵢ)

  → **기각 사유: RRF가 위에서 확정한 가중치 순서를 정반대로 만든다.** 실서버 분포(자료 17건 · 핀 0 · 좋아요 1)에서 가장 오래된 자료에 **좋아요를 눌러도 17등 → 16등**이었다. 축별 변별력이 유사도 `0.0068` > 최근성 `0.0034` > 좋아요 `0.0008` > 핀 `0.0007` 로 뒤집힌다.
  → **원인은 k 튜닝이 아니라 구조다.** RRF의 변별력은 `w × [1/(k+1) − 1/(k+n)]` 이라 **가중치뿐 아니라 그 축의 단계 수 `n`에 비례**한다. 좋아요·핀은 사실상 2단계(1/0)고 유사도·최근성은 17단계라, **`k`를 0까지 낮춰도 못 이긴다**(k=0에서도 유사도 1.88 > 좋아요 1.50). 대수적으로 좋아요가 최근성만이라도 이기려면 `k < 1.46`이 필요하다.
  → **RRF를 골랐던 원래 근거는 그대로 지켜진다.** "스케일이 달라 정규화 상수가 근거 없이 남는다"가 이유였는데, 순위 정규화도 **등수만 쓰고** 나누는 값은 임의 상수가 아니라 **그 축에서 관측된 등수 범위**다. 전원 동점이면 축이 통째로 빠지는 성질(핀 0건 → 자동 제외)도 동일하다.
  → **라이브 검증**: 자료 18건(좋아요 5 · 핀 1)에서 **좋아요 5건이 상위 5위를 독점**했고, 좋아요가 없는 것들 사이에서는 유사도가 순서를 정했다. 제목이 URL뿐인 자료 3건이 하위로 밀렸다.
  → 그래서 **`k=60` 항목은 소멸했다** — 평활 상수 자체가 없어졌으므로 튜닝할 것도 없다.
- [x] **★ 선별의 이득은 토큰이 아니라 "관련 없는 자료가 앞자리를 차지하지 않는 것"이다** *(2026-08-02)*
  → 원래 명분이 토큰 절감이었는데 [#20 ⑥](EXPERIMENTS.md)이 뒤집었다 — 프롬프트의 약 80%가 고정비라 자료를 절반으로 줄여도 25%밖에 안 준다.
  → 라이브에서 실제로 관측된 이득은 다른 것이다: 본문이 32~72자뿐인 URL 자료 3건이 **하위로 밀렸다.** 그 자료들이 프롬프트 앞자리에 있으면 모델이 근거로 삼을 것이 없는 항목을 먼저 본다.
- [x] **★ RAG 2단계 — 임베딩 생성 시점 = 태깅 `@Async` 흐름에 이어서** *(2026-07-29 · 2026-08-01 구현 완료)* — `ArchiveCrawlProcessor`가 태그·요약을 받은 직후 임베딩 호출 1회를 덧붙인다. 별도 배치도 별도 트리거도 만들지 않는다.
  → 이미 배경 처리라 사용자를 붙잡지 않고, **본문이 손에 있는 유일한 시점**이라 다시 읽어올 필요가 없다.
  → **임베딩 실패는 무해하다** — NULL로 두면 그 자료는 유사도 축에서만 빠지고 최근성·핀/좋아요 축으로 후보에 남는다. 하이브리드를 고른 덕에 생긴 부수 효과다.
  → **구현하며 정정된 것: 붙는 자리는 하나가 아니라 셋이다.** 이 항목이 `ArchiveCrawlProcessor`(외부 공유 URL)만 적어 뒀는데, 실제 등록 경로는 **동기 URL·동기 텍스트·크롤링 비동기** 셋이고 요약도 이미 셋 다에 붙어 있었다. 크롤링 경로만 채웠다면 앱에서 등록한 자료는 영영 벡터가 없었을 것이다.
  → **★ 무엇을 임베딩하는가 = 요약 우선, 없으면 본문(6,000자로 자름)** *(2026-08-01 확정)* — 추천 프롬프트가 쓰는 `pickContent`("요약·본문 중 **짧은 쪽**")와 **일부러 다르다.** 그쪽 기준은 프롬프트 토큰 절약이고 여기 기준은 **검색 품질**이다: 질의가 짧은 방 목표라서, 7,000토큰짜리 본문을 통째로 임베딩하면 벡터가 평균화돼 무엇과도 어중간하게 닮는다. 요약(실측 115~236자)은 "이 자료가 무엇에 관한 것인가"로 이미 압축돼 있다. 즉 **짧아서 고르는 게 아니라 요약이라서 고른다.**
  → **자르는 지점 6,000자는 모델 상한에서 역산했다** — 실측 한국어 토큰/문자 비 0.910~0.988에 여유를 얹어 1.3으로 잡고 8,191 ÷ 1.3 ≈ 6,300 → 6,000([#19](EXPERIMENTS.md)).
  → ⚠️ **부작용: 벡터의 기준이 자료마다 다르다.** 요약 없는 자료는 본문이 입력이라 벡터가 평균화돼 짧은 방 목표와의 유사도가 **구조적으로 낮다** — `NULL`이 아닌데도 유사도 축에서 불리하다(실서버 12건 중 4건). **다음 티켓에서 정할 것**: ① 백필 때 요약부터 만들어 기준 통일(크레딧 2배) ② 읽는 쪽이 기준 구분 ③ 감수.
  → ⚠️ **동기 등록 2경로는 사용자가 +1.4초를 기다린다.** 비동기인 것은 크롤링 경로 하나뿐이다 — 파생 데이터를 전부 뒤로 미루면 `ArchiveItemDetailResponse.summary`가 항상 `null`이 되어 응답 계약이 바뀐다. **이 자리가 공짜라고 믿고 네 번째 호출을 얹지 말 것**([#19](EXPERIMENTS.md)에 경로별 표).
  → **기존 자료 백필은 기본 꺼짐**(`ARCHIVE_BACKFILL_EMBEDDINGS`). 자료 1건당 LLM 호출 1회라 크레딧을 쓰고, 한 번만 필요한 작업이라 부팅마다 돌 이유가 없다.
  → **임베딩 모델은 AI 서버와 같은 값이어야 한다** — Spring은 `ARCHIVE_EMBEDDING_MODEL`, FastAPI는 `OPENAI_EMBEDDING_MODEL`, 둘 다 `text-embedding-3-small`. 다른 모델로 만든 벡터끼리는 코사인 유사도가 의미를 갖지 않는다(같은 좌표계가 아니다). 지금은 쓰는 쪽이 갈라져 있어 어긋나도 아무도 안 터지지만, 선별을 FastAPI가 하기로 확정된 이상 언젠가 같은 공간에서 비교하게 된다.
- [x] **★ `excluded_todos` = 방별 최근 ~~50개~~ → 16개 상한** *(2026-07-29 · 2026-07-30 구현 완료 · **2026-08-03 50→16**)* — ⚠️ **이름이 오해를 부른다. "사용자가 거절한 후보"가 아니라 "이미 노출한 후보 전부"다.** `full_spec.md:130`에 거절 버튼이 없고 "중복 후보 재노출 안 함"만 있기 때문이다. 채택하지 않고 시트를 닫은 후보도 전부 여기 들어간다.
  → 이 오해가 그대로 스키마를 틀리게 만든다 — 거절로 읽으면 `rejected` 플래그를 만들게 되는데, 우리에겐 그것을 채울 사용자 입력 자체가 없다. **실제로 이 경고가 값을 했다**: 스키마에 `rejected`도 `user_id`도 넣지 않았다.
  → **구현 = Spring 쪽 저장소 하나뿐이었다.** 이 파일의 AI 서버 쪽은 처음부터 다 돼 있었다(`suggest.py`가 프롬프트에 싣고 `filter_candidates`가 코드로도 거른다). `TodoSuggestionService`가 `List.of()`를 보내고 있었을 뿐이다. `todo_suggestion_exposures` 테이블(`V6`) + `TodoSuggestionExposureStore`로 채웠다. **AI 서버 코드는 한 줄도 바뀌지 않았다.**
  → **★ 기록 시점 = 후보를 생성해 반환할 때** *(2026-07-30 사용자 확정)*. 대안 ⓑ(앱이 "실제로 보여줬다"를 되보고)는 기각했다 — 엔드포인트와 앱 변경이 늘고, **그 보고가 실패하면 막으려던 중복 노출이 그대로 재발한다.** ⓐ의 대가는 사용자가 응답을 받기 전에 이탈하면 못 본 후보도 제외되는 것인데, 상한이 롤오버되면서 되살아나므로 영구 손실이 아니다(2026-08-03 창이 16으로 줄어 **더 빨리** 되살아난다).
  → **상한은 조회로만 지킨다** — 오래된 행을 지우지 않는다. 상한의 목적이 LLM 프롬프트 크기이기 때문이고, 누르기당 **최대 8행**(`prompts.py` 규칙 6)이라 방 삭제 시 `CASCADE`로 정리되는 것으로 충분하다.
  → ~~**⚠️ 이 조합의 부작용: 후보가 마른다.**~~ → **2026-08-03 해결.** 예측이 아니라 **실사용에서 실제로 터졌다** — 후보가 8개에서 1~2개로 떨어지고 후보 1개당 비용이 5~10배가 됐다.
  → **원인은 역할 중복이었다.** 채택한 후보는 이미 `existing_todos`(방의 투두 **전체**, 상한 없음)로 영구 제외되는데, 노출 기록이 같은 일을 한 번 더 하면서 **채택하지 않은 후보까지 영구히 막았다.** 관측된 방은 채택 2개 vs 제외 50개(채택률 4%)였고, 그 50개가 입력 토큰의 33%를 먹었다.
  → **제외 창을 50 → 16 으로 좁혔다**(회당 상한 8개 × 직전 2회차). 같은 방 진짜 데이터로 창 크기만 바꿔 3회씩 재보니 후보 평균이 **50→2.0 · 24→5.3 · 16→6.0 · 8→7.3 · 0→7.0** 이었다([#24](EXPERIMENTS.md)). 8이 더 낫지만 반복 방지가 한 회차뿐이라 이 기능의 존재 이유에 너무 가까워진다. 실사용에서 반복 불만이 나오면 24로 올린다.
  → **앱 빈 상태 문구도 고쳤다** — `자료를 더 담거나, 잠시 후 다시 시도해 주세요`. 빈 결과의 원인이 둘(자료가 없다 / 이미 추천한 것과 겹친다)인데 시트는 어느 쪽인지 모르므로 **원인을 단정하지 않는다.**
  → **"제외 창을 비우는 수단"은 만들지 않았다** — 창이 줄면서 자연히 풀리므로 앱·서버 양쪽에 기능을 더할 이유가 없었다.
  → ⚠️ **대가**: 창이 줄어 `drop_semantic_duplicates`(임베딩 층)가 보는 범위도 같이 줄었다. 채택한 지 두 회차가 지난 투두와 **의미만 같고 표현이 다른** 후보는 다시 나올 수 있다. 문자열이 같으면 `filter_candidates`가 여전히 잡는다.
  → **`UNIQUE(room_id, title)`을 두지 않은 것이 의도다** — 동시 요청이 겹치면 INSERT가 터져 **사용자의 추천 자체가 실패한다.** 중복 행 하나보다 나쁘다. 중복 제거는 `TodoSuggestionExposureStore`가 `_normalize`(공백 제거+소문자)를 미러링해 코드로 한다.
  → **부수로 결함 하나가 고쳐졌다**: `TodoSuggestionService.suggest()`가 `@Transactional(readOnly = true)`였고 **LLM 호출이 그 안에 있었다** — 실측 7~9초([#10](EXPERIMENTS.md)) 동안 DB 커넥션을 점유했다. `readOnly`만 떼면 쓰기 커넥션을 9초 잡아 더 나빠지므로, 읽기·LLM·쓰기를 셋으로 쪼갰다(`ai/docs` 밖의 변경이지만 이 티켓이 원인이라 여기 남긴다).

- [x] **★ 추천 응답은 동기 유지 — 폴링으로 바꾸지 않는다** *(2026-07-29 제기 → 2026-08-01 실측으로 확정)*
  → 제기 당시 조건은 "요약 도입 후 재실측해 결정"이었다. **결정을 떠받치는 값은 [#18](EXPERIMENTS.md)의 실사용 트레이스 3.13초**다 — 제기 시점의 7~9초([#10](EXPERIMENTS.md))에서 요약 도입으로 내려왔다.
  → [#20](EXPERIMENTS.md)의 135회(1.1~2.8초, 3초 초과 2건·최대 6.1초)는 **그것과 모순되지 않는다는 확인**이지 결정의 근거가 아니다 — 평가는 자료 **1건**짜리 호출(≈900 tok)이고 운영은 8~12건(2,400~2,900 tok)이라 같은 값이 아니다.
  → 앱은 이미 스피너+문구로 대응한다. 3초짜리 응답에 폴링 엔드포인트·상태 저장·앱 재진입 로직을 붙이면 **얻는 것 없이 실패 지점만 늘어난다.**
  → **되살릴 조건**: 자료가 늘어 응답이 10초를 넘기면 다시 본다. 다만 그때도 폴링보다 **자료 선별(RRF)이 먼저다** — 그게 응답 시간의 원인이기 때문이다.

- [x] **★ 평가 러너 입력 = 요약(기본값)** *(2026-08-01, [EXPERIMENTS #20](EXPERIMENTS.md))* — 운영에서 추천 프롬프트에 들어가는 것이 요약(`V5`)인데 러너만 원문 캡션을 넣고 있었다. `evals/data/summaries/`에 **Spring과 같은 프롬프트·같은 모델로 만든 요약을 동결해 커밋**하고 러너가 그걸 읽는다. `--input body`는 [#13](EXPERIMENTS.md) 기준선 재현용으로만 남긴다.
  → **동결하는 이유**: 호출할 때마다 요약이 달라지면 조건 간 델타가 프롬프트 때문인지 입력이 달라져서인지 갈리지 않는다.
  → **요약 프롬프트의 진실은 Spring**(`OpenAiSummaryClient.SYSTEM_PROMPT`)이고 Python 쪽은 복제본이다. 어긋나면 `tests/test_summary_prompt_drift.py`가 죽는다 — 틀려도 요약은 그럴듯하게 나오므로 다른 데서는 안 걸린다.

- [x] **⛔ 카테고리 배정 자체를 걷어냈다** *(2026-08-01 도입 → **2026-08-04 제거** → -243)*
  → **AI 는 카테고리를 정하지 않는다.** 후보는 카테고리 없이 나가고(= 기타/미분류) 사용자가 투두 탭에서 직접 분류한다. 제품 결정이다.
  → **세 방법을 만들어보고 셋 다 실패했다**: ① 프롬프트로 시키기([#21](EXPERIMENTS.md)) — 새 카테고리 0개, 100% 한 곳으로 몰림 ② 임베딩 코사인 — 실사용 10/10 실패, 게이트를 뒤집어도 중앙값 차 ±0.03 ③ LLM 별도 호출([#31](EXPERIMENTS.md)) — 후보당 카테고리 종수가 **0.81 → 0.96 으로 악화**.
  → ⚠️ **③ 은 비싸서 기각이 아니다.** LLM 호출이 3회가 됐는데도 10초 초과 0/30 이었다([#29](EXPERIMENTS.md) 와 사유가 다르다). **효과가 없어서**다.
  → **받는 쪽은 한 줄도 안 고쳤다** — Spring `AiCandidate` 는 record 라 키가 없으면 `null`, 앱 `_resolveCategoryId` 는 빈 이름을 기타로 해석한다. 양쪽 다 이미 null 을 정상 경로로 처리하고 있었다.
  → **되살리려면 [`RESTORE-category-assignment.md`](RESTORE-category-assignment.md) 를 먼저 읽을 것.** 제거를 단일 커밋으로 묶어 `git revert` 한 번이면 돌아오지만, **그냥 되돌리면 실패한 상태로 돌아간다.** 다시 시도한다면 배정이 아니라 **생성**을 고쳐야 한다([#27](EXPERIMENTS.md) 의 진단).
  <details><summary>제거 전 기록</summary>

- [x] **★ 카테고리 판단은 모델이 아니라 코드가 한다 — 임베딩 코사인 0.50** *(2026-08-01 해결)*
  → 사용자가 원한 동작 그대로다: *"유사도가 높으면 그 카테고리에, 아니면 새로 만든다."* **그 판단을 모델에게 시키는 것이 실패의 원인이었다.**
  → 바꾼 것 셋: ① 규칙 4 를 "이 후보가 속할 **분야**를 한두 단어로" 로 ② `build_payload` 에서 **기존 카테고리 목록을 뺐다**(목록을 보여주면 거기 맞추려 한다) ③ `suggest.map_categories` 가 임베딩으로 매칭한다.
  → **프롬프트 문구로는 안 됐다** — 판정 기준 제시·스키마 동시 수정·구체적 예시까지 **4가지 후보안 전부 새 카테고리 0개**, 후보의 100%가 `공부` 였다([EXPERIMENTS #21](EXPERIMENTS.md)). 눈으로는 그럴듯했고 평가셋이 막았다.
  → **임계값 0.50 = 오탐 0 을 지키는 값.** 접어야 하는 것의 최솟값 0.202 < 두어야 하는 것의 최댓값 0.341 로 **구간이 겹치므로**(#17 과 같은 구조) 완벽히 못 가른다. 오탐(여행 → `공부`)이 곧 고치려던 버그라 그쪽을 0으로 잡고 미탐 7/13 을 감수했다.
  → **⚠️ 그래서 매칭 층은 지금 거의 아무것도 안 접는다.** 실제 개선은 ①②가 냈다. ③은 "엉뚱한 데 밀어 넣지 않는다"는 안전장치이자 사용자가 요구한 메커니즘이다.
  → **앱·서버는 안 고쳤다** — `ai_suggest_sheet.dart` 가 이미 `"(새 카테고리)"` 로 표시하고, 채택 시 `todos_screen.dart` 의 `_resolveCategoryId` 가 이름으로 찾아 없으면 만든다(**앱이 한다** — Spring 이 아니다). **처음부터 준비돼 있었고 AI 만 못 채우고 있었다.** API 계약도 그대로다.
  → **왕복은 안 늘었다** — `suggest._prewarm` 이 중복 제거와 카테고리 매칭이 쓸 문자열을 한 번에 임베딩한다(테스트가 호출 1회를 단언).
  → 남은 것: 미탐 7/13(짧은 한국어 이름은 뜻이 같아도 코사인이 낮다 — 문맥을 붙인 임베딩이 나을 수 있으나 미측정).
  → **카테고리 증식은 걱정한 것보다 작다** — 평가 러너 기준으로는 11종이 나오지만 그건 자료를 1건씩 따로 호출하는 러너 설계 탓이고, **운영 모양(자료 전부를 한 번에)으로 재니 4종**이었다([EXPERIMENTS #21](EXPERIMENTS.md) "허상" 절). 회차 간 근접 중복(`여행`↔`여행 일정` 0.713)은 자동으로 접힌다. **2026-08-01 사용자 판단: 지금은 두고 에뮬레이터 실사용에서 보며 판단한다.**
  </details>

---

- [x] **★ 후보 자기 검수(critique) 도입 + `critique_max_attempts = 1`** *(2026-08-03)* — 생성 뒤에 LLM 을 **한 번 더** 불러 "바로 실행할 수 없는 후보"를 반려한다. 사용자 불만(`동선 짜기`·`코스로 묶기`·`10곳 중 1곳 고르기`)을 프롬프트 규칙으로 잡으려 하지 않은 이유는 [#6](EXPERIMENTS.md)·[#21](EXPERIMENTS.md)이다 — **판단을 생성 모델에게 시키면 안 통한다는 것을 두 번 실증했다.**
  → **효과**: 부산 여행 방의 메타 행위 비율 **40% → 0~6%**(두 실행). 두 실행 차이(6%p)가 효과(34%p)보다 훨씬 작아 결론이 선다([#28 ③](EXPERIMENTS.md)).
  → **대가 ①**: 후보가 절반 이하(부산 총 88 → 41개), 3회차는 **최소 0**. 나쁜 후보를 보여주느니 빈 화면이 낫다는 판단이고, 앱에는 이미 빈 화면 문구가 있다.
  → **대가 ② — 응답이 사용자가 정한 7초 상한을 가끔 깬다.** 라운드 평균은 5.30~6.79초로 상한 안이지만 **라운드 단위로는 8/60(13%)이 7초를 넘고 최악이 10.88초**다(기준선은 0/30 · 최악 5.05초). ⚠️ **처음에 "5.8초로 7초 상한 안"이라고 적었는데 평균만 본 것이었다**(2026-08-03 리뷰). **사용자가 두 대가를 다 알고 검수 유지를 확정했다**(2026-08-03). 검수 호출은 +1,247 tok · +1.82초이고 **더 빠른 모델이 없다** — 재본 셋 중 `gpt-5.4-nano` 가 이미 제일 빠르다.
  → **`2`(재생성 1회)를 안 쓰는 이유는 "2 가 나빠서"가 아니라 "구분이 안 돼서"다.** 처음엔 "2 는 일관성이 없고 지연만 더 든다"고 적었는데 **틀렸다** — 오픽 메타 행위·지연 평균·후보 수 **3개 축에서 2 가 같거나 낫다**(지연은 오히려 2 가 빠르다). 1 vs 2 는 #28 이 스스로 세운 기준(군간 차 > 군내 흔들림)을 통과하지 못한다. 그래서 **싼 쪽(라운드당 LLM 2회, 2 는 최악 4회)** 을 고른다. ⚠️ **결과적으로 `critique → generate` 되돌아가는 엣지는 운영에서 안 탄다.** 이 티켓의 명분이 그 루프였다는 점을 감추지 않는다. 코드는 남긴다 — 설정 하나로 다시 잴 수 있어야 하기 때문이다.
  → **⚠️ 그래서 이 티켓 뒤에도 "LangGraph 가 기능 이득을 준다"고 말할 수 없다.** 초안에 *"A/B 를 싸게 만든 것이 그래프의 값"* 이라고 적었다가 지웠다 — **그것도 과장이다.** 설정 분기는 함수에 `if` 하나 넣어도 똑같이 된다. 냉정하게 따지면 검수 노드·`critique → END` 분기·`attempts`/`rejected` 상태 전달 **전부 직선 코드로 된다.** LangGraph 여야만 하는 것은 ① 되돌아가는 루프(안 씀) ② Studio 시각화(개발 도구)뿐이다. 품질을 올린 것은 **검수라는 아이디어**이지 그래프가 아니다.
  → **학습 방 보호 단서가 필수다.** 회화 스터디 후보(`완료시제 넣어 말하기 연습하기`)는 물리적 대상이 없어 규칙 1에 전부 걸린다. 프롬프트에 "행위 자체가 대상인 경우는 통과"를 명시했고, 실측에서 그 방은 안 나빠졌다(12% → 7~8%). ⚠️ **"없었다면 통째로 날아갔을 것"은 추정이다** — 그 단서를 빼고 돌려본 절제 측정은 없다. 문구 존재만 `tests/test_critique.py::TestTheCritiquePromptItself` 가 고정한다.
  → **오류는 fail-open** — LLM 예외·판정 개수 불일치·번호 불일치면 전부 통과시킨다(`_map_categories_safely` 와 같은 정책). 검수 층 고장이 추천 장애가 되면 안 된다.
  → ✅ **그 fail-open 은 자주 터졌고, 고쳤다** *(2026-08-03 발견 → 같은 날 해결, [#30](EXPERIMENTS.md))*. **원인은 후보를 `0.` 부터 번호 매긴 것이었다.** 검수만 떼어 60회를 재보니 첫 번호가 `0` 이면 30회 중 **10회(33%)**가 판정을 하나 빠뜨렸고, `1` 이면 **30/30** 이 맞았다(`evals/probe_critique.py`, `2026-08-03_critique_index_probe.json`). 고친 것은 `suggest.CRITIQUE_INDEX_BASE = 1` 한 줄이다 — **프롬프트와 스키마 설명은 안 건드렸다**(측정이 그것들을 그대로 둔 채 얻은 값이라, 함께 고치면 재지 않은 조건을 배포하게 된다).
  → ⚠️ **개수만 모자란 것이 아니었다 — 번호가 밀렸다.** 반려 사유를 후보와 대조하면 `0` 에서 밀린 판정이 **12건**, `1` 에서는 **0건**이다(예: `opic_8` 의 `index=6` 사유가 7번 후보 `시험 40분 동안 12~15개 콤보 …` 를 설명). **개수가 우연히 맞았다면 fail-open 이 안 걸리고 엉뚱한 후보가 반려됐을 것이다** — 위 "오류는 fail-open" 정책의 개수 검사가 그걸 막고 있었다. **처방이 고친 것은 개수가 아니라 짝이다.**
  → ⚠️ **"꼬리 판정이 빠진다"고 쓰면 안 된다**(2026-08-03 리뷰 P0-1). 응답이 `0..n-2` 로 연속이라 빠진 **번호 라벨**은 산수로 `n-1` 이지만 후보와는 다른 양이다 — `opic_8` 에서 꼬리 후보는 판정을 받았다. 밀림 시작점이 묶음마다 달라(`opic_8` `index=6`, `busan_8c` `index=4`) **어느 후보가 건너뛰어졌는지는 확정 불가**다.
  → **방 평가 2회 확인**: fail-open **0/30 · 0/30**, 후보 수 유지(41·38 / 58·64), 상한 초과 1/30(기준선과 같음). ⚠️ **품질 개선은 주장할 수 없다** — 방마다 기준선의 최선보다 나쁜 실행이 하나씩 있어 "구분되지 않는다"까지다. ⚠️ 최악 지연은 커졌다(10.88 → 14.31초, 단발 스파이크로 보이나 확인 안 됨).
  → ⚠️ **위 ②의 `40% → 0~6%` 은 여전히 "검수가 매번 걸렸다"를 전제한다.** 그 스냅샷들에는 계수기 필드가 아예 없어 사후 확인이 불가능하다. 다만 부산 방 후보 수에 8이 하나도 없어(최대 5·6) 오염 가능성은 낮다 — **대리지표이므로 단정하지 않는다.**
  → ⚠️ **왜 0-based 일 때만 이러는지는 모른다.** 증상을 없앤 것이지 기전을 안 것이 아니다. **모델을 바꿀 때 러너 계수기를 반드시 볼 것.**
  → **⚠️ 이 티켓에서 지표가 한 번 무너졌다**([#28 ①③](EXPERIMENTS.md)). #26 의 "좋은 동사 비율"로는 효과가 재현되지 않았고(22% → 9%), 원인은 **좋은 동사를 열거하려 한 것**이었다. 조건 비교에는 **나쁜 모양을 세는** 메타 행위 비율을 쓴다.

- [x] **★ 부분 반려분 재생성은 쓰지 않는다 — `regenerate_on_partial_reject = False`** *(2026-08-03)* — 만들어서 재고 **기각했다**([#29](EXPERIMENTS.md)). 위 -235 의 되돌아가는 엣지가 "전부 반려"에만 걸려 운영에서 안 타길래, **부분 반려("3개 통과 · 4개 반려")에도 타게** 만들어 후보 마름을 고치려 했다.
  → **목적 자체는 달성됐다.** 부산 총 후보 41·42 → **109·88개**, 오픽 55·63 → **93·103개**.
  → **기각 사유는 지연이다.** 라운드당 LLM 이 2회 → 4회가 된다. **10초 이상이 0/30·1/30 → 11/30·12/30(37%·40%)**, 최악 10.88 → **15.46초**. 두 실행 차가 3%p 라 변동이 아니라 구조다. ⚠️ **10초는 사용자가 이 티켓 착수 시점에 7초에서 올려준 값이다** — 올려주고도 깨졌다. 그리고 이 수치는 오히려 관대하다(fail-open 라운드는 재생성을 안 해서 싸다).
  → **⚠️ 방마다 효과가 반대다 — 이게 더 중요한 관측이다.** 회화 스터디는 메타 행위가 **6.6~7.7% → 3.0~4.3%** 로 좋아졌는데 부산은 **0~5.9% → 7.6~12.7%** 로 나빠졌다. **하필 부산이 사용자가 불만을 제기한 방이다.** 처음엔 "fail-open 오염 탓"으로 물렀다가 2차가 뒤집었다 — 2차는 fail-open 이 **더 적은데**(4회 vs 6회) 메타는 **더 나빴다**. **왜 부산에서만 나빠지는지는 모른다.**
  → **코드는 스위치 OFF 로 남긴다** — 조건 하나로 다시 잴 수 있어야 한다. ✅ 전제였던 fail-open 은 2026-08-03 에 고쳤으므로(위) **이제 다시 재도 된다.** 다만 기각 사유였던 지연은 그 수정과 무관하다 — 되살리려면 LLM 4회를 줄일 방법이 함께 있어야 한다.
  → **안 재본 변형이 하나 있다**: 재생성분을 **재검수하지 않으면** LLM 4회가 3회가 된다(검수 1회 = 1.82초). 오픽 평균 9.17 → 약 7.4초로 **추정**된다(실측 아님). 다만 검수를 뺀 재생성분은 [#6](EXPERIMENTS.md)·[#21](EXPERIMENTS.md)이 실패를 확인한 바로 그 경로다.
  → **재시도 프롬프트에 "통과분 목록"을 넣은 것이 같은 조건에 묶여 있다.** 안 알려주면 모델이 회당 8칸을 1회차 승자로 다시 채워 재생성이 허수아비가 된다 — 분리 불가라 위 수치는 **루프 + 프롬프트 변경의 합**이다.

- [x] **LangGraph Studio 를 개발 도구로 붙인다** *(2026-08-03)* — `PYTHONUTF8=1 uv run --group studio langgraph dev`. 되돌아가는 엣지가 생긴 뒤로 실행마다 경로가 달라져 트레이스 목록보다 그림이 빠르다.
  → **`dev` 가 아니라 별도 `studio` 그룹**이다 — 패키지가 34개 늘어난다(grpcio-tools·opentelemetry 일습). CI 는 `dev` 만, 운영 이미지는 `--no-dev` 라 어느 쪽도 안 들어간다.
  → Studio 가 보는 것은 운영 `SUGGEST_GRAPH` 가 아니라 **`prepare` 노드를 앞에 붙인 사본**이다(`llm`/`embed` 를 JSON 으로 못 넣는다). 배선은 `_build_graph` 를 재사용하고 동일성은 `tests/test_studio_graph.py` 가 지킨다 — **그림과 실제가 갈리면 Studio 로 한 디버깅이 거짓말이 된다.**
  → ⚠️ **`PYTHONUTF8=1` 없으면 안 뜬다.** cp949 윈도우에서 세 군데가 각각 `UnicodeDecodeError` 로 죽는다 — `langgraph.json` · `ai/.env` · **`langgraph_api` 가 자기 패키지에 번들한 `openapi.json`**(라이브러리 쪽 문제). 세 번 연달아 겪고 찾았다.

## 미확정 (임의로 채우지 말 것)

- [x] **✅ 추천 생성 모델 — 세 축 모두 닫혔다** *(2026-07-28 신규 → 2026-07-30 축소 → 2026-08-01 ② 해소 → **2026-08-02 비용 축 종료**)* — 모델은 `gpt-5.4-nano`로 확정되고 시간·비용·품질 세 축이 사후에 뒷받침됐다(위 확정 기록, [#18](EXPERIMENTS.md)).
  → **비용은 게이트웨이 대시보드가 아니라 LangSmith로 본다** *(2026-08-02 사용자 확정)*. 운영 모양(자료 18건) 추천 1회가 **약 $0.0009**(input 2,982 · output 275 tok), 1,000회 환산 **약 $0.90**이다. 평가 러너처럼 자료 1건짜리 호출은 $0.00023~0.00038로 그보다 훨씬 싸다.
  → **⚠️ 이 값은 게이트웨이 크레딧이 아니다.** LangSmith가 **OpenAI 공개 단가로 추정한 값**이고, 게이트웨이 크레딧은 토큰과 선형 대응이 아니다([#7](EXPERIMENTS.md)). 크레딧 정확한 차감이 필요해지면 그때 대시보드를 본다 — **지금은 "비용이 문제가 되는 규모가 아니다"를 아는 것으로 충분하다**는 판단이다.
  → ~~② 고유명사가 많은 자료에서의 원문 충실도~~ → **2026-08-01 해소.** 장소·상호명이 많은 6건(강릉·부산·서울)의 후보를 요약·원문과 눈으로 대조했고 **손상이 없었다**(`모치플레지르`·`밤산책방`·`히떼 로스터리`·`별미청 돼지국밥`·`공원 6ongone` 전부 원문 표기 그대로) → [EXPERIMENTS #20 ④](EXPERIMENTS.md). `nano` 계열이 제목을 중간에서 잘라내던 전례([#7](EXPERIMENTS.md))는 재현되지 않았다.
  → **다만 이 측정이 보는 것은 "요약→후보" 구간이다.** 요약이 이미 고유명사를 3~4개로 줄인 뒤이므로, "원문→요약" 구간에서 버려진 상호명은 여기서 안 잡힌다.
  → **왜 원문 충실도가 최우선 축인가**(2026-07-28 작성, 그대로 유효): 추천이 틀리면 존재하지 않는 가게로 헛걸음한다. 그리고 `nano` 계열은 이 워크로드에서 **고유명사 보존을 측정한 적이 없다** — [#7](EXPERIMENTS.md)에서는 오히려 `nano`가 제목을 중간에서 잘라내는 것이 관측됐다. 자동 채점층을 폐기했으므로(위 폐기 기록) 검증은 **출력을 눈으로 대조**하는 방식이어야 한다.
  → **예산 위험의 대부분이 추천에 있다는 점도 그대로다** — 아카이브 전체를 프롬프트에 넣으므로 호출당 토큰이 태깅과 비교가 안 된다. 다만 요약 도입(223) 이후 실측은 자료 8건에 input 2,398~2,923 tok으로, 착수 전 추정(자료 1건 5,834 tok)보다 훨씬 낮다.
  → 재검토 방법: `evals/`의 15케이스 러너로 **모델만 바꿔 나란히** 돌리고 출력을 눈으로 대조한다.

- [x] **✅ 요약의 환각 — 별도 대응을 만들지 않는다** *(2026-07-28 신규 → 2026-07-30 ② 해소 → **2026-08-02 ① 결정으로 종료**, 사용자 확정)*
  ① 요약은 원문의 재진술이라 **원문 문자열 대조로 검증할 수 없다**([#8](EXPERIMENTS.md)).
  → **결정: 검증 층을 만들지 않는다.** 근거 셋이다.
    1. **조용히 오염되는 경로가 없다.** 사용자가 S-25-B에서 요약을 직접 읽고, 추천 후보도 사용자가 골라 채택한다(`full_spec.md` §S-16-B). 잘못된 요약이 사람 눈을 거치지 않고 데이터가 되는 길이 없다.
    2. **인접 구간을 실제로 대조해 손상이 없었다.** 고유명사가 많은 6건(강릉·부산·서울)의 후보를 요약·원문과 눈으로 맞춰봤고 `모치플레지르`·`밤산책방`·`히떼 로스터리`·`별미청 돼지국밥`·`공원 6ongone` 전부 원문 표기 그대로였다([#20 ④](EXPERIMENTS.md)).
    3. **지표를 먼저 만들면 안 된다.** 그렇게 해서 이틀을 버렸다([EXPERIMENTS](EXPERIMENTS.md) "다시 하지 말 것"). 재현되는 문제를 눈으로 본 적이 없는데 지표부터 만드는 것이 정확히 그 전례다.
  → **되살릴 조건**: 사용자가 "요약에 없는 얘기가 있다"고 신고하거나, 요약을 사람 눈을 거치지 않고 쓰는 경로가 생기면(예: 요약만으로 자동 투두 생성). 그때는 **실제 사례를 먼저 모으고** 지표를 정의한다.
  → ⚠️ **남는 사각지대는 그대로다**: [#20 ④]가 본 것은 "요약 → 후보" 구간이고, "원문 → 요약" 구간에서 버려진 상호명은 여기서 안 잡힌다.
  ② ~~짧은 본문에는 요약을 만들지 않는 편이 낫지만 **임계값 미정**.~~ → **2026-07-30 해소: 임계값을 두지 않는다.** 항상 요약을 만들고, 추천 시점에 **요약·본문 중 짧은 쪽**을 고른다(`TodoSuggestionService.pickContent`). "N자 미만" 상수를 만들면 근거 없는 숫자가 코드에 남는 반면, 짧은 쪽 규칙은 요약이 없는 세 경우(`V5` 이전 등록분·`PENDING`·요약 실패)와 "본문이 요약보다 짧은 경우"를 한꺼번에 처리한다. 태깅 호출에 얹는 비용이 prompt +47 tok 수준이라 항상 만들어도 아까울 게 거의 없다는 것도 근거다([#8](EXPERIMENTS.md)).

- [x] **✅ 추천 후보의 제목 길이 — 지금 표본으로는 잴 수 없어 손대지 않는다** *(2026-08-01 신규 → **2026-08-02 종료**, [EXPERIMENTS #20](EXPERIMENTS.md))*
  → **결정: 프롬프트 규칙 5를 고치지 않는다.** 같은 조건 재실행에서 후보 수가 31↔35 · 28↔25로 갈리고 20자 초과 비율도 함께 흔들린다. **이 표본으로는 규칙 변경의 효과와 실행 간 잡음을 구분할 수 없다** — 고쳐도 좋아졌는지 알 방법이 없다.
  → **되살릴 조건**: 반복 실행 평균을 내는 러너가 생기면(추정 2~3시간). 그 전에 규칙을 건드리는 것은 [#6](EXPERIMENTS.md)의 재현이다 — 규칙 하나를 더해 정확도가 14/15 → 10/15로 후퇴한 전례.
  → **지금 사용자에게 실제 피해는 제한적이다**: 50자를 넘는 제목은 `filter_candidates`가 버리고(`MAX_TITLE_LENGTH`), 남는 것은 "20자보다 길다"는 미관 문제다.
  → 원래 기록(그대로 유효):
  [#13](EXPERIMENTS.md)은 제목이 긴 원인을 "원문의 주소·날짜·기관명을 그대로 베끼는 것"으로 보고 요약이 들어가면 사라진다고 예측했다. **주소 병기는 사라졌지만 길이 자체는 나아지지 않았다** — 20자 초과 비율이 60% → 58.3~74.2%로 같은 대역이고, 최대 길이도 실행별로 30~40자(아티팩트 1건 제외 시 30~35)라 #13의 40자와 겹친다. 짧은 제목(8자)이 사라져 분포가 가운데로 몰렸을 뿐이다.
  → **지금 프롬프트 규칙 5를 손대지 않는다.** 같은 조건 재실행에서 후보 수가 31↔35 · 28↔25로 갈리고 20자 초과 비율도 함께 흔들려(자료 15건·후보 25~42개) **이 표본으로는 규칙 변경의 효과를 잴 수 없다.** 규칙 5를 고치려면 표본을 늘리거나 반복 실행 평균을 쓰는 러너가 먼저 필요하다.
  → 함께 관측된 것: 어미 이탈이 실행당 1~5건(`… 반복학습` 같은 명사형 종결), 그리고 한 자료 안에서 접두어가 반복된다(`을지로·성수 맛집 …` / `… 카페 …` / `… 디저트 …`).

- [x] **✅ 전체 투입 ↔ top-K 전환 임계값 — 「전환」을 없앴다. 항상 정렬하고 예산 초과분만 버린다** *(2026-08-02 사용자 확정 구현 완료)*
  → **원안("임계값을 넘으면 top-K로 전환")을 기각한 이유**: 실서버 자료 17~18건에서 **한 번도 발동하지 않아** 에뮬레이터·데모에서 동작을 확인할 수 없다. 합성 데이터 테스트로만 존재하는 코드가 된다.
  → **대신**: 자료를 **항상 순위대로** 넣고(정렬은 상수가 필요 없다 — 가중치는 확정값), 누적 렌더 길이가 예산을 넘을 때만 뒤쪽을 버린다. 지금 규모에서도 **프롬프트 내 순서**로 효과가 나고, 규모가 커지면 **같은 코드가 자동으로 선별로 바뀐다.**
  → **예산값 `12,000자`는 튜닝값이 아니라 안전밸브다**(`config.archive_prompt_budget_chars`). 실측: 자료 18건 렌더 합계 **3,709자**(평균 206자/건) → 12,000자는 **약 58건 · 지금의 3.2배**라 발동하지 않는다(테스트로 고정). **"자료가 몇 건일 때 추천 품질이 나빠지는가"는 아직 잰 적이 없고, 그 데이터 없이 근거 있는 값을 정할 수 없다** — 그래서 발동하지 않는 크기로 두고 무한정 커지는 것만 막는다. 발동하면 로그가 남고 그때 정할 데이터가 생긴다.
  → 토큰 기준이 아니라 **글자 기준**인 이유: 예산을 세는 문자열이 **실제로 프롬프트에 들어가는 문자열과 같아야** 하고(`render_archive_item`을 그대로 주입), 토크나이저를 끌어오면 인코딩 파일을 네트워크로 받아야 해 CI가 외부에 의존하게 된다.
  → **⚠️ 2026-08-01 실측이 이 항목의 전제를 흔든다**([EXPERIMENTS #20 ⑥](EXPERIMENTS.md)). 자료 1건짜리 프롬프트가 869~948 tok인데 그 안의 요약은 ≈100 tok이다 — **고정 비용(시스템 프롬프트 + 방 정보 + JSON 스키마)이 약 800 tok으로 대부분이다.** 자료를 절반으로 줄여도 고정비가 그대로라 전체의 25% 남짓밖에 안 준다.
  → **다만 이 모형은 운영을 과소추정한다**: 같은 모형으로 자료 8건을 넣으면 1,600 tok인데 위 실측(2,398~2,923 tok)은 그보다 800~1,300 높다. 추천 요청에는 `excluded_todos`(방별 최근 50개)·`existing_todos`가 더 실리기 때문이다. **운영 고정비는 평가에서 잰 것보다 크므로 결론은 오히려 강해진다.**
  → 즉 **선별의 이득을 토큰에서 찾으면 안 된다.** 임계값을 정하기 전에 "선별이 무엇을 좋게 하는가"(관련 없는 자료가 후보를 흐리는 것)를 먼저 정의할 것.

- [x] **✅ 예산 12,000 → 3,000자 · 선별 기준을 순위에서 「최신순」으로** *(2026-08-09 사용자 확정, 시연 대비)* — 위 항목의 후속이고 **그 전제를 뒤집는다.**
  → **예산이 더는 안전밸브가 아니다.** 상시 발동하는 선별기가 됐다. 206자/건 기준 약 14~15건에서 잘린다. 위 항목이 "발동하지 않는 크기로 둔다"고 적어둔 근거는 **이 결정으로 폐기됐다**(문장 자체는 이력으로 남긴다).
  → **근거는 품질이 아니라 지연이다.** 운영 방(자료 35건) 앱 실측이 **평균 28.2초**였고 목표는 10초였다.
  → **선별 기준을 최신순으로 바꾼 이유**: 예산이 상시 발동하면 "무엇이 잘리는가"가 처음으로 결과를 바꾸는데, 시연에서는 **직전에 추가한 자료가 반드시 들어가야** 한다. 순위로 고르면 방금 넣은 자료가 좋아요 0·핀 없음이라 밀려난다. 대가로 **핀·좋아요·목표 유사도가 선별에서 빠진다**(`archive_select_by_recency=False`로 즉시 복원).
  → ⚠️ **위 302~304행이 이 결정의 반대 근거였다 — 지우지 않고 남긴다.** 고정 비용(시스템 프롬프트 + 스키마 + `excluded_todos`)이 약 800 tok으로 프롬프트의 대부분이라 자료를 줄여도 전체 토큰은 크게 안 준다는 것이었다. **실측은 그 예측을 기각했다.**
  → ✅ **실측: 28.2초 → 11.8초**(같은 방, 자료는 오히려 35 → 46건으로 늘어난 상태). 구간이 겹치지 않는다. **예산 축소는 지연 레버가 맞다.**
  → ⚠️ **운영 측정 전에 배포 반영을 반드시 확인할 것.** `deploy.sh` 가 찍는 `==> 배포 대상 커밋` 과 `docker logs maramodi-ai | grep 예산` 두 가지로 본다. 배포가 안 걸린 상태에서 재면 "효과 없음"이라는 거짓 결론이 나온다.
  → 품질은 **재지 않았다.** `ai/CLAUDE.md`의 "프롬프트 변경은 평가셋으로 검증한다"를 이번엔 지키지 못했다(시연 일정). 시연 후 방 평가로 확인할 것.

- [ ] **파인튜닝** — 현재 계획 없음.

---

## 폐기된 결정 (되살리지 말 것)

- ~~자체 GPU 서빙(vLLM + Qwen2.5-7B)~~ — 공용 GPU 서버 제약으로 불가(2026-07-24). 관련 실험 데이터는 전부 Qwen 기준이라 OpenAI에는 그대로 적용되지 않는다.
- ~~임베딩 모델 BGE-m3~~ — GPU 의존. `text-embedding-3-small`로 교체.
- ~~Pinecone~~ — Postgres 컬럼으로 확정.
- ~~pgvector~~ — **2026-08-01 뒤집힘.** 쓰는 것이 저장뿐이라 `real[]`로 갔다(위 "벡터 저장소" 항목). 되살리려면 먼저 "Postgres에서 top-K를 돌려야 하는 이유"를 만들 것 — 그게 없으면 이미지 교체 + Testcontainers 테스트 수정 비용만 낸다.
- ~~Claude API 사용~~ — 초기 스펙에 그렇게 적혀 있었으나 옛 LLM 게이트웨이 경유 OpenAI로 확정.

### 2026-07-28 검토 후 기각된 대안

- ~~Kiwi 전용 마이크로서비스 (Spring → FastAPI(Kiwi) → Spring → SpringAI → OpenAI)~~
  상태도 도메인도 없는 순수 함수 하나를 위해 배포 단위·헬스체크·내부 인증·네트워크 홉을 늘리는 비용이 이득보다 크다. *(2026-07-30: 검증층 자체가 폐기돼 이 대안은 논의 대상이 아니게 됐다.)*

- ~~태깅을 로컬 LLM으로 처리~~
  공용 GPU 서버 제약(2026-07-24)은 그대로이고 배포 대상은 단일 EC2(CPU)다. 무엇보다 Qwen은 상호명을 4/4 손상시켰는데(`카페툇마루` → `카페tep마루`) **태깅은 상호명이 곧 태그**다.

- ~~`langchain` 풀 패키지~~ — `langgraph`를 하드 의존성으로 끌고 온다. `langchain-core`만 도입하는 것으로 결정(위 확정 항목).

- ~~입력 전처리(`preprocess.py`) 이관~~ *(2026-07-28 폐기)* — 프로토타입은 "입력 전처리가 가장 효과적"이라고 결론냈지만 그건 **Qwen 기준**이었다. OpenAI에서는 측정이 한 방향으로 나온다 — 전처리를 켜면 결과가 악화되고, 여러 어절 상호명을 쪼개 없는 가게를 만들어낸다(`삶은 감자` → `삶은`/`감자`). 상세는 `EXPERIMENTS.md` "다시 하지 말 것".
  → **끄는 게 최선이라고 이미 측정된 컴포넌트이므로 버그를 고치지 않고 버린다.** 한때 이관·수정 계획에 올라 있었으나 취소했다.
  → *(2026-07-30: 검증층도 함께 폐기됐다 — `EXPERIMENTS.md` "다시 하지 말 것".)*

> **검토 후 채택된 것**: LangChain 도입 자체는 처음에 기각안으로 올렸다가 **재검토 끝에 `langchain-core` 한정으로 채택**했다. 기각 사유로 들었던 두 가지가 실제로는 성립하지 않았다 — ① 프롬프트 브레이스 이스케이프는 `SystemMessage` 객체를 쓰면 회피된다 ② "LangGraph와 일관성" 은 개념적으로 다른 패키지를 묶은 약한 논거였다(단, 풀 패키지에는 실제 의존성이 있어 결과적으로 유효했다).
