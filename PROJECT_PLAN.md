# 프로젝트 계획서 — 팀 목표 협업 앱 (MVP)
*1인 개발 + Claude Code · Flutter + Spring Boot + Firebase(인증·푸시) · 2026년 7월*

---

## 1. 확정된 결정

| 항목 | 결정 |
|------|------|
| 앱 성격 | 팀 목표 협업 앱 (방 단위 투두·일정·아카이브 공유, 진행률, 콕찌르기, AI 추천/태깅) |
| 모바일 | **Flutter** (자체 디자인 시스템, 커스텀 UI·애니메이션 중심) |
| 백엔드 | **Spring Boot** (자체 API + DB) |
| 인증 | **Firebase Authentication** (소셜 4종 + 자체가입 전부 Firebase로 → ID 토큰(JWT)을 Spring Boot가 Firebase Admin SDK로 검증) |
| DB | PostgreSQL (관계형 — 방·멤버·투두·담당자 다대다에 적합) |
| AI | 투두 추천 + 아카이브 자동 태깅 → **서버에서 LLM 호출**(옛 LLM 게이트웨이 경유 OpenAI, 키 보호). 태깅은 Spring이 직접, 투두 추천은 전용 **FastAPI AI 서버**를 거친다 — `ai/docs/DECISIONS.md` |
| 타깃 | iOS + Android 동시 |
| MVP 범위 | 설계서 전체 (AI·후순위 S-05 포함) |
| 나중 확장 | 위젯 / Live Activity / 배경화면 자동화 (네이티브 확장, 지금은 구조만 열어둠) |

**확정 완료 (초기 결정 전부)**: Spring Boot=**Java, 3.5.x LTS 고정(4.x 업그레이드 금지)** · 자체가입 아이디=**이메일** · 투두 **마감일 메타데이터 지원(날짜·위치 자동 알림은 미구현, 2026-08-06)** · 실시간=**폴링** · 다크모드=**라이트만** · 수익모델=**무료** · 스플래시=**인터랙티브 로고** · 폰트=**Pretendard**(앱 전역 번들, 2026-08-01 시스템폰트에서 전환) · 상태관리 Riverpod · 라우팅 go_router · 앱 패키지/번들 ID=**com.intpsquad.modi**

---

## 2. 시스템 아키텍처 (요약)

```
┌───────────────┐   Bearer ID Token   ┌──────────────────┐
│  Flutter 앱    │ ──────────────────▶ │  Spring Boot API  │
│ (iOS/Android)  │                     │  + PostgreSQL     │
└──────┬────────┘                      └────────┬─────────┘
       │ 로그인(소셜/이메일)                       │ Firebase Admin SDK로 검증
       ▼                                         │
┌───────────────┐                                │
│Firebase Auth   │◀── Admin SDK ─────────────────┘
└───────────────┘

  AI 경로 (앱은 LLM을 직접 부르지 않는다)
  ├ 자동 태깅(S-25-C)   Spring ─────────────────────▶ 옛 LLM 게이트웨이(OpenAI)
  └ 투두 추천(S-16-B)   Spring ─▶ FastAPI AI 서버 ─▶ 옛 LLM 게이트웨이(OpenAI)
```

- **인증 흐름**: 앱이 Firebase Authentication으로 로그인 → Firebase가 ID 토큰(JWT) 발급 → 앱이 API 호출 시 `Authorization: Bearer <id-token>` → Spring Boot가 Firebase Admin SDK로 서명 검증 후 `uid`(유저 UID)로 인가.
- **데이터**: 앱의 모든 도메인 데이터(방/투두/일정/아카이브 등)는 Spring Boot의 PostgreSQL에 저장. Firebase는 **인증·푸시(FCM) 전용**.
- **AI**: 앱이 직접 LLM을 부르지 않음(키 노출 방지, 프롬프트 인젝션·비용 통제). 아카이브 자동 태깅은 Spring이 옛 LLM 게이트웨이로 직접 호출하고, 투두 추천은 Spring이 **FastAPI AI 서버**(`ai/`)를 거친다. AI 서버는 외부에 포트를 열지 않고 도메인 DB도 보지 않는다 — 결정 근거는 `ai/docs/DECISIONS.md`.
- 상세는 `specs/0001-architecture.md`.

---

## 3. 리포 구조 (모노레포)

```
repo/
├── CLAUDE.md                 # 하네스 헌법 + specs 인덱스
├── PROJECT_PLAN.md           # 이 문서
├── specs/                    # 단일 진실 (설계 스펙)
│   ├── full_spec.md           # 전체 IA 원본 — 최상위 진실
│   ├── design.md              # 디자인 시스템 — 디자인에 관한 유일한 진실 (v4, Airbnb 철학)
│   ├── 0001-architecture.md   # 시스템 아키텍처·인증
│   ├── 0002-data-model.md     # 관계형 스키마
│   ├── 0003-navigation.md     # 라우트 트리 + 전이표
│   └── NNNN-<기능>.md          # 화면별 스펙 S-xx (개발하며 /spec으로 추가)
├── .claude/
│   ├── agents/               # explorer, reviewer
│   ├── skills/               # /spec 등 커스텀 명령
│   └── hooks/                # pre-commit 게이트
├── app/                      # Flutter (Dart)
│   ├── lib/
│   └── CLAUDE.md             # (선택) 앱 전용 규칙
├── server/                   # Spring Boot (+ springdoc-openapi → API 명세 자동생성)
│   ├── src/
│   └── CLAUDE.md             # (선택) 서버 전용 규칙
├── ai/                       # FastAPI AI 서버 (투두 추천 S-16-B 전용, uv + Python 3.12)
│   ├── src/modi_ai/
│   ├── docs/                 # DECISIONS.md(결정 트래커) · EXPERIMENTS.md(측정 기록)
│   └── CLAUDE.md             # ai/ 작업 규칙 + "하지 말 것" 목록
├── docs/
│   └── api/                  # openapi.json (CI 가 테스트 중 자동 재생성) + 기능별 요약
├── deploy/                   # 운영 배포 (단일 오라클 A1, ARM)
│   ├── ci/flutter/Dockerfile      # app 스테이지 에이전트 — Flutter 버전 고정(기성 이미지에 3.44.7 없음)
│   ├── ci/server/Dockerfile       # server 스테이지 에이전트 — temurin+git(기성 이미지에 git 없어 CI 실패)
│   ├── docker-compose.infra.yml   # 인프라 층: Caddy(TLS) 하나 — 수동 기동만
│   ├── docker-compose.app.yml     # 앱 층: spring + ai + minio + postgres + redis — dev 머지마다 교체
│   ├── Caddyfile                  # api./storage.maramodi.cloud 라우팅, /actuator 차단
│   ├── deploy.sh                  # 빌드→교체→헬스체크. :previous 태깅으로 롤백 가능
│   └── .env.example               # 운영 환경변수 템플릿 (실제 .env는 서버에만)
└── .github/workflows/       # GitHub Actions CI/CD (ci.yml 한 파일 — 테스트 게이트 + dev push 배포)
```

> **문서 자동화 하네스**: 기능 하나가 끝날 때마다 API 명세(springdoc-openapi 자동생성)와 ERD(`specs/0002` Mermaid)를 함께 갱신하는 것이 완료 정의(DoD)에 포함됨. `/feature-done` 스킬이 이를 강제. 상세는 `CLAUDE.md`.

---

## 4. 개발 로드맵 (전체 범위, 단계적)

전체를 목표로 하되 의존성 순서로 쌓는다. 각 Phase 안에서 `spec→plan→implement→verify→review→commit` 루프.

> **갱신 시점**: 2026-07-23 (커밋 `18f2eda`까지 반영) + 2026-07-27 스펙 재정립. 각 항목은 실제 커밋된 코드 기준으로 체크한다 — "구현 중" 상태는 체크하지 않는다.
> **2026-07-27**: 사용자가 원본 IA 문서 `specs/full_spec.md`/`specs/design_full_spec.md`를 뒤늦게 추가했다. 이 문서들을 `specs/`의 최상위 진실로 승격하고, 기존 파생 스펙(`design.md`, `0005`, `0006`)의 충돌(디자인 팔레트, 홈 오늘투두 "방 전체 보충" 로직)을 제거했다. 미구현 영역(온보딩 상세/방전환/일정/아카이브/멤버투두·콕찌르기/설정/종료된 방)은 `0007`~`0013` 스펙으로 문서화만 완료 — 아래 로드맵 체크 상태는 변경 없음(스펙만 생겼을 뿐 구현은 그대로).
> **2026-07-27 (2차) — 디자인 파일 단일화**: `specs/design_full_spec.md`와 `specs/DESIGN-airbnb.md`를 삭제하고 **`specs/design.md` v4 하나로 통합**했다. 디자인 철학·팔레트를 Airbnb 기준으로 전면 교체(흰 캔버스 + Rausch `#FF385C` + 굵기 완화 + 소프트 라운드 + 단일 그림자 티어)하고, 앱 코드(`app/lib/design/tokens.dart`·`theme.dart` + 화면 17개)까지 함께 반영했다. 삭제된 화면 상세(S-01~S-40-D)는 `0004`~`0013`이 이미 커버하며, 그 문서들이 해당 화면의 진실이 됐다. 로드맵 체크 상태 변경 없음(리디자인이지 기능 추가가 아님).

### Phase 0 — 하네스 + 기반 (데-리스킹 우선)
- [x] 모노레포 + CLAUDE.md + .claude/(agents·hooks) + CI(**GitHub Actions**, GitHub 저장소 — 2026-08-13 Jenkins/GitLab 에서 이관)
- [x] Flutter 앱 부트스트랩(go_router), 디자인 토큰(design.md) → 테마 코드화 — *Riverpod는 아직 실사용 없음(ProviderScope만 존재), 화면은 StatefulWidget+setState 패턴*. **2026-08-05 빈 상태 통일**: 공용 `EmptyState` 위젯(`app/lib/design/empty_state.dart`) 신설 — 은은한 라인 아이콘 + 문구 + primary(`#FF385C`) 텍스트 버튼. 홈(내 투두·모아보기)·일정·모아보기(폴더/폴더 내 자료)·멤버 투두·검색 결과의 아웃라인 버튼/좌측정렬 빈 상태를 이 컴포넌트로 교체(문구도 "＋ ○○ 추가하기"로 간소화). "진행 중인 방이 없어요" 온보딩 게이트·라벨 카드 내부 인라인 빈 텍스트는 예외.
- [x] Spring Boot 부트스트랩 + PostgreSQL + 마이그레이션(Flyway) 세팅
- [x] 전역 예외 처리 인프라 — `global/exception/`(`ApiException` + 상태코드별 6개 서브클래스 + `GlobalExceptionHandler`) 도입, 기존 13개 서비스/클라이언트 파일의 산발적 `ResponseStatusException`을 도메인별 타입 예외(`domain/{feature}/exception/`)로 전량 교체. API 응답 스키마(OpenAPI)엔 변화 없음(`docs/api/openapi.json` diff 없음 확인)
- [x] **★ 데-리스킹 스파이크: Firebase Authentication ↔ Spring Boot ID 토큰 검증 왕복** — 구글 소셜 1종, `44f6c72`+`c0f49bc`. Kakao는 에서 카카오 SDK → Spring Custom Token → Firebase 세션 교환을 추가했고, Apple은 Developer 계정 준비 후 별도 구현한다.
- [x] 데이터 모델(0002) 확정 → `V1__init.sql` 마이그레이션 적용(rooms/room_members/users/todos/todo_assignees/categories/schedules/archive_*/notification_settings/pokes 전 테이블), `invite_codes`는 이후 Redis 단독 저장으로 전환(`V2__drop_invite_codes.sql`)

### Phase 1 — 인증 · 온보딩 · 방
- [x] 로그인(S-01~02) — Google 기존 구현 + Kakao SDK/Firebase Custom Token 교환 구현. Apple은 Developer 계정 준비 후 별도 구현. 스펙 `specs/0007-온보딩.md`(약관 동의 미결 포함). **2026-07-31**: 자체가입(이메일) 3단계 폼과 서버 인증코드 발송/검증(`POST /auth/email/code`, `POST /auth/email/code/verify`)이 `dev`에 병합됐다. 앱의 디버그 전용 인증 우회도 제거해 모든 빌드가 같은 API 계약을 사용한다. Firebase 계정 생성 자체(이메일/비밀번호)는 앱이 Firebase SDK로 직접 한다. 실제 SMTP 수신까지의 실환경 E2E는 별도 확인이 남아 있다.
- [x] 방 만들기/초대코드/참여(S-03, S-10, S-10-A, S-11), 온보딩 게이팅(스킵 불가) — `specs/0004-방-생성-참여.md`, 백엔드 `f49e265` + 앱 화면/라우팅 게이팅 `59d185c`. 초대코드는 Redis 저장(TTL 1일). **2026-08-01**: 대표 이미지 업로드 UI를 방 생성·방 설정(S-40-A) 두 곳에 구현 — 갤러리/카메라 피커 + 2단계 업로드(`POST /rooms/cover-image` → `coverImage` 문자열). 앱에 이미지 파일 업로드 인프라(image_picker + `AuthenticatedHttpClient.sendMultipart`)를 처음 도입. **백엔드 업로드 엔드포인트 구현 완료**(같은 날) — `RoomCoverImageService`가 매직바이트로 이미지 형식 판별 후 MinIO `rooms/cover/`에 저장, 계약 `docs/api/room-cover-image.md`. **2026-08-05**: S-10-A 초대 코드 공유 화면 리디자인 — 완료 체크 그래픽 + 코드 카드(코드는 Jalnan2 폰트 · [코드 복사]=클립보드) + 외부 공유 3버튼(카카오톡·인스타 브랜드 SVG·더보기). 카카오·인스타는 "클립보드 복사 + 앱 딥링크(실패 시 OS 공유 시트)" 인터림, **풀 Kakao Share SDK 템플릿은 후속**(`kakao_flutter_sdk_share` 미설치). **2026-08-05**: S-11 초대 코드 입력 리디자인 — 6칸 PIN 박스 + 아바타형 확인 모달. **2026-08-07**: 멤버 수·기간 메타를 위해 `GET /invite-codes/{code}` 응답에 `memberCount`/`startDate`/`endDate` 추가 완료.
- [x] **2026-08-05 초대 공유 완성**: `kakao_flutter_sdk_share` 기본 FeedTemplate로 방 커버·이름·"참여하기" 카드를 카카오톡 채팅방 선택 화면에 전송하고, Android/iOS custom URL scheme을 Kakao SDK deep-link callback→Flutter router로 연결해 받은 6자리 코드를 S-11에 프리필한다(인증/온보딩 중에는 `AppSession`에 일시 보관). 인스타는 코드만 복사해 `instagram://`을 열고, 더보기는 OS Native Share Sheet다. 방 멤버 관리(S-40-B)도 동일한 세 채널 선택지를 제공한다.
- [ ] 앱 재진입 우선순위 로직(4-2) — **부분**: 로그인 여부·방 소속 유무 게이팅(redirect)은 구현됨. "마지막 본 방" 우선순위는 미구현 — 현재 `active_room_resolver.dart`가 첫 번째 ACTIVE 방으로 임시 대체 중(주석에 명시, `specs/OPEN.md` 참고)
- [ ] 방 없음 상태(S-06) — 전용 빈 상태 화면 없음(홈이 활성 방 없을 때 데이터만 비움)
- [x] 방 전환 바텀시트(S-07) — `RoomSession`(`app/lib/features/room/room_session.dart`, 방 목록 캐시 + "마지막 본 방" 영속화는 shared_preferences) + `RoomSwitcherSheet`. 스펙 `specs/0008-방-전환.md`(2026-07-27). **2026-07-30 추가**: 보조 트리거로 **하단 네비 홈 버튼 롱프레스**(어느 탭에서든, 탭 이동 없음 — `full_spec.md`의 "홈 화면 롱프레스"를 대체) + 최초 사용자용 **스포트라이트 코치마크**(`room_switch_hint.dart`, 최초 1회 + ACTIVE 방 2개 이상 시 1회, 롱프레스 성공하면 영구 미노출). 시트 열기 로직은 `showRoomSwitcher()`로 추출해 상단 "방이름▾"와 공유

### Phase 2 — 핵심 협업 루프
- [x] 홈 대시보드(S-04): D-day·아바타줄·주간캘린더(읽기전용)·오늘투두·아카이브미리보기 — `specs/0005-홈-대시보드.md`, `58048e5`. **2026-07-27 교정**: 멤버 아바타 정렬을 진행률 내림차순으로, 오늘투두의 "방 전체 보충" 로직을 제거(원본 스펙 재정합). **2026-08-01 재디자인**: 에지투에지 접히는 히어로(커버 이미지/그라데이션 + D-day 57px + 진행률 바)·64px 아바타·#F7F7F7 주간달력/오늘투두 박스·아카이브 가로 스냅 캐러셀 + 앱 전역 Pretendard(`home_hero.dart`/`week_calendar.dart`/`archive_carousel.dart`). 리디자인이라 체크 상태는 유지. 백엔드 요구(`room.coverImage` 노출, `todoDone`/`todoTotal` 집계)는 같은 날 구현 완료(`DashboardService`가 `TodoRepository`로 방 단위 직접 집계 — 멤버별 합산의 다중 담당자 중복을 피함), 폴백은 안전망으로 유지. **2026-08-01 타이포 조정**: D-day 56px, '오늘의 현황' 헤더 신규, 섹션 라벨 '이번주'/'투두 미리보기'/'모아보기'(16/600)로 변경 + 헤더를 #F7F7F7 박스 안으로 이동 + 플로팅 하단 네비/흰 바디 30px 라운드. **2026-08-02 다듬기**: 히어로 딤 그라데이션·D-day 스크롤 스케일·lip 불투명(갭 제거), 주간달력 선택일 일정 카드, '투두 미리보기'→'내 투두' + 체크 시 목록에서 제거 **2026-08-05 UX 개선**: 탭을 다시 켜면 조용히 재조회(전체 스피너는 첫 로드만) · 탭 전환 슬라이드 애니메이션(`StatefulShellRoute` + `AnimatedBranchContainer`, 탭 상태는 그대로 보존) · 완료 체크는 화면만 즉시/서버는 2초 뒤(그 사이 다시 누르면 취소, `pending_completion.dart` — 홈·투두 공용) · 바텀시트 전부 `useRootNavigator: true`(하단 네비 위) · 커버 미설정 방은 기본 배경 5종(`roomId % 5`, 표시 전용) · 홈 오늘투두는 행 전체가 완료 토글이고 탭 이동은 "더보기"만 · 섹션 라벨 '이번주'→'이번주 일정'. **2026-08-06 활동 캡슐 배너(`ActivityCapsuleBanner`) 추가 + 활동 피드 백엔드 구현 완료**: 홈 상단에 스터디원 활동을 4초 간격 롤링하는 배너 신설(프론트, `home/activity_banner.dart`) — 처음엔 팀 진행률·D-day만 채웠으나 같은 날 백엔드가 이어서 완료: `activities` 이벤트 테이블(`V20`) + `todos`/`schedules.created_by`(`V19`) + `GET /rooms/{roomId}/dashboard` 응답에 `activities[]` 추가, 9종 적재형(`TODO_COMPLETED` 등) + 4종 파생형(`SCHEDULE_SOON` 등) 이벤트를 `home_screen.dart`의 `homeActivityMessages`가 문구로 조립. 상세는 `docs/backend/home-activity-feed.md`, `specs/OPEN.md`. **2026-08-06 아카이브 미리보기 핀 우선배치**: 대시보드 응답에 `previewArchives`(핀 우선→최신순, 최대 4개) 필드 추가 — 프론트는 이날 먼저 `previewArchives ?? recentArchives` 폴백 게터를 심어둔 상태였어서 서버 필드만 붙이면 바로 연결됐다. 기존 `recentArchives`(순수 최신순)는 그대로 유지. **2026-08-07 별도 백엔드 요청으로 `pinnedArchives` 추가**(순수 `pinned=true` 필터, 핀 0개면 빈 배열 — `previewArchives`의 패딩과 다름) — 이름·의미가 겹치지 않아 기존 필드를 안 건드리고 셋(recentArchives/previewArchives/pinnedArchives)이 공존한다. 아직 앱은 `pinnedArchives`를 쓰지 않음(`specs/OPEN.md`).
- [x] 투두 탭(S-15~18): 카테고리/독립 ToDo, 담당자 다대다, todo/done 탭, 내투두/전체보기 토글, 카테고리 펼침/접힘, 미지정 처리 — `specs/0006-투두-탭.md`, `18f2eda`. **2026-07-27 교정**: todo/done 세그먼트·내투두·전체보기 토글·카테고리 접기·"독립 ToDo" 용어·직접추가 시 담당자 미선택 자동 본인지정 추가(원본 IA 문서 뒤늦게 반영). AI 추천(S-16-B)은 스펙에서 명시적으로 범위 밖. **2026-07-29 보정(FR-39)**: 완료 체크 API(`PATCH .../todos/{todoId}`)가 방 멤버면 누구나 남의 담당 투두를 체크할 수 있던 인가 누락을 수정 — `TodoService.setCompleted`가 이제 담당자 본인만 허용(미지정 투두는 예외로 누구나 허용, 사용자 확정). **2026-08-03 UI 리디자인**: 세그먼티드 컨트롤(내투두/전체보기), 완료 인라인+토글(done 탭 폐기), 상단 ⋯ 더보기 메뉴(항목선택/카테고리관리/완료보기/정렬 수동·생성일·제목), 일괄선택 모드, 하단 점선 추가 어포던스, "독립 ToDo"→"기타" 개명. **2026-08-03 백엔드 후속 완료**: 투두 `createdAt` 노출(`TodoResponse`) + 카테고리 `position` 순서변경(`categories.position` `V10`, `PATCH /rooms/{roomId}/categories/order`) — 프론트 드래그 UI는 별도 후속(`specs/OPEN.md`). **2026-08-04 행 제스처·정밀 레이아웃(`b970ac6`, `f118f42`)**: 단일 탭=완료 토글 / 더블탭=수정 시트 / 롱프레스=드래그 재정렬(드래그 핸들 폐기, 전체를 flat `ReorderableListView`로 렌더해 카테고리 넘나드는 이동 지원, 같은 목록 내 순서는 백엔드 `position`이 없어 기기 로컬 `TodoOrderStore`에 저장) / 좌측 스와이프=삭제. **2026-08-05 QA 반영**: 스와이프 액션이 버튼 2종으로 — 투두 `[수정][삭제]`(50×28, 간격 4, 히트박스는 행 높이) / 카테고리 헤더 `[삭제]`만(확인 모달 유지, 전체 스와이프 즉시삭제 차단), **열린 액션은 화면에 하나만**(다른 슬라이드·탭·스크롤 시 닫히고 탭한 동작은 그대로 실행), 행 박스 좌우 20px 여백 + 눌림 배경을 그 안으로 한정(`InkWell`→직접 그리는 pressed 배경이라 슬라이드 중엔 투명), 카테고리 헤더(48) > 투두 행(44). 같은 커밋에서 직전 두 커밋에 red로 남아 있던 위젯 테스트 6건(탭 토글·스와이프 삭제 구동 방식)도 복구. **2026-08-04 투두 드래그 순서변경 — 백엔드 완료 / 프론트는 로컬 순서 유지(2026-08-05 병합 정리)**: 서버는 `todos.position`(`V12`, `(room_id, category_id)` 단위) + `PATCH /rooms/{roomId}/todos/order`로 완성돼 있다(이동 대상은 나에게 배정된 투두뿐 — FR-39의 "미지정은 누구나" 예외를 두면 항상 400이라 2026-08-04 리뷰로 정정, 남의 투두는 자리 불변). **앱은 아직 이 엔드포인트를 쓰지 않는다** — dev의 카테고리별 드래그핸들 UI 대신 이 브랜치의 flat 드래그(카테고리 넘나듦 + 기기 로컬 `TodoOrderStore`) UI를 택했기 때문(2026-08-05 dev 병합 시 사용자 확정). 순서를 기기 간 동기화하려면 flat 드래그를 `reorderTodos`에 배선하는 후속이 필요하다(`specs/OPEN.md`). **2026-08-06 S-18 전체화면 전환(사용자 확정)**: 기존 항목 수정만 전체화면(`/todos/item/:id`)으로 바뀌었다. 서버에 신규 `GET /rooms/{roomId}/todos/{todoId}`(단일 상세 조회)를 추가해 최신값을 다시 받아 채우고, 저장은 기존 `PUT`(전체 교체)을 그대로 쓴다. 전체화면에는 삭제 버튼이 없다(삭제는 목록 행 좌측 스와이프로 유지). **2026-08-07 인라인 작성기 롤백 후에도 이 전환은 유지**되며, 본문 위젯만 `TodoFormSheet`로 바뀌었고 생성(S-16)은 바텀시트로 되돌아갔다. **2026-08-09 이미지 첨부 저장 연결(QA)**: 백엔드가 `todos.image_url`/`image_pinned`/`image_attached_at`(`V25`)·`POST /rooms/{roomId}/todos/image/upload-url`·`GET/PATCH .../archive/todo-images*`를 먼저 구현해 dev에 올렸고(`docs/backend/todo-image-archive-handoff.md`), 프론트가 그 계약에 맞춰 `TodoFormSheet`의 이미지 선택 UI를 실제 저장에 배선했다 — `TodosApi.uploadTodoImage`(presigned PUT 2단계, `SettingsApi.uploadProfilePhoto`와 동일 패턴) + `createTodo`/`updateTodo`의 `imageUrl` 파라미터, 수정 모드에서 새로 고르지 않으면 기존 이미지를 보존. 모아보기 "이미지" 탭(`archive_folder_items_screen.dart`)은 이미 그 계약대로 선구현돼 있어 추가 변경 없이 그대로 연결된다. 🔴 **2026-08-09 중요 표시(`important`) 백엔드 복원**(`docs/backend/todo-form-handoff.md`) — `todos.important BOOLEAN NOT NULL DEFAULT FALSE`(`V26`, V16의 `flagged`와는 별개 신규 컬럼) + `TodoResponse.important` + 생성/수정 API 바디. 정렬·필터엔 반영 안 함(요청서가 저장·반환까지로 범위 한정). 앱 폼의 "중요" 토글 실제 저장 연결은 후속.
- [x] 일정 탭(S-20): 월간 캘린더 + 일정 CRUD(생성/수정/삭제, 시간 미지정 우선 정렬) — 서버 `schedule` 패키지 + 앱 `features/schedule/`. 스펙 `specs/0009-일정-탭.md`(2026-07-27). **2026-08-02 리디자인(/ 184·185·187·188)**: 목업 맞춤 — 상단바 `+`, **일요일 시작** 월간 그리드(선택일 primary 채운 원·연분홍 이벤트 점), 오늘 일정 카드(좌측 컬러 바 + 제목 + "시간 · 장소"), 생성/수정 시트 재구성(제목/메모/시간 종일기본/장소 그룹박스, CTA 추가하기·저장 + "일정 삭제" 링크). 장소(`place`)는 `ScheduleItem`/API에 nullable로 추가. 리디자인이라 체크 유지. `schedules.place` 백엔드(컬럼+DTO+마이그레이션)는 2026-08-03 완료. 앱 내 주 시작 요일 불일치(홈=월/일정=일)는 `OPEN.md`. **2026-08-04 백엔드 후속(사용자 요청)**: 종료 시간·다중일(기간) 지원 — `schedules.end_date`/`end_time`(`V11`) + 엔티티/DTO/검증 + 조회를 구간 겹침 기준으로 전환 완료. **2026-08-05 프론트 반영 완료**: 폼 시트에 기간(시작·종료 날짜 편집)·종료 시간 피커 추가, 월간 그리드·홈 주간캘린더(`week_calendar.dart`) 둘 다 다중일 일정을 구간 전체에 점으로 렌더, 카드 부제 시간 범위 표시(초 미표시). 홈 대시보드 `ScheduleBrief`에 `endTime`도 추가(서버). **2026-08-07 일요일·공휴일 빨간색(사용자 요청)**: 월간 그리드의 요일 헤더 `일` 라벨과 **일요일·공휴일 날짜 숫자**를 `accent-danger`(#C13515)로 칠한다(종전엔 헤더 라벨만 primary였고 날짜에는 요일 색 개념이 아예 없었다). **토요일은 색 없음**, 색 우선순위는 선택일 > 오늘 > 휴일 > 평일. 공휴일 판정은 신규 `app/lib/features/schedule/korean_holidays.dart`의 **연도별 하드코딩 표**(2026·2027) — 음력·대체공휴일 때문에 규칙 계산이 불가능해 확정 날짜를 박았고, 표에 없는 연도는 공휴일을 안 칠한다(일요일 빨강은 계속 동작). ⚠️ 조사 중 **2026년에 「공휴일에 관한 법률」이 두 번(02-10, 04-09) 개정돼 제헌절이 18년 만에 부활하고 노동절이 관공서 공휴일로 추가**된 것을 확인했다 — 표는 매년 월력요항 발표 후 갱신해야 한다(`specs/OPEN.md`). 홈 주간캘린더(`week_calendar.dart`)는 이번 범위 밖이라 두 캘린더 표기가 다르다. **2026-08-07 일정 카드 치수를 Figma 오토레이아웃에 맞춤**: 패딩 가로 12/세로 10, 바↔텍스트 간격 10, 좌측 바 4px 풀 하이트 → **3 × 34 · 반경 2 세로중앙**, 부제 `body-small`(14) → `caption`(13), 카드 사이 간격 12 → **8**(마지막 카드 뒤 여백 제거). 카드 높이 58은 설정값이 아니라 `패딩 10 + max(콘텐츠, 바 34) + 패딩 10` 파생값이라 위젯 테스트로 58·바 3×34·바 x12·간격 10·카드 간격 8을 잠갔다. **Figma의 “내부” 스트로크는 프레임 크기를 안 늘리는데 Flutter `Border`는 자식을 밀어내서** 패딩에서 테두리 1을 빼야 58·바 x12가 맞는다(안 빼면 60·13) — 다른 화면 이식에도 같은 보정이 필요하다. 10·3·34·2는 design.md 4px 스케일 밖이고 카드 내부 패딩 16·카드 간격 12·부제 body-small도 문서 규정과 어긋나 `specs/OPEN.md`에 정식화 여부 미결로 기록했다.
- [ ] 아카이브 탭(S-25): 폴더 3뎁스, 등록, 좋아요/핀 — **부분**: 폴더 CRUD + S-25 폴더 목록, 폴더 내 항목 목록(S-25-A), 항목 상세(S-25-B), 자료 등록+AI 자동 태깅(S-25-C, 크롤링·SSRF 방어·당시 옛 LLM 게이트웨이 경유 `gpt-5-nano`로 실기기 검증 완료 — **2026-08-13 OpenAI 직접 호출 + `gpt-5.4-nano`로 전환**(`ai/docs/EXPERIMENTS.md` #34)), 외부 공유 등록(S-25-D) **Android·iOS 네이티브 코드** 구현(네이티브 `ShareActivity`/`ShareExtension`, `crawl_status` PENDING/DONE/FAILED, `@Async` 백그라운드 크롤링+태깅) 및 iOS Simulator 빌드·Swift XCTest 검증 완료(**2026-07-31 기준** — 🔴 2026-08-05 에 추가한 iOS 변경분은 아직 실행되지 않았다, 아래 참고) — 실기기 공유 시트 E2E는 출시 검증으로 남음. 서버 `archive` 패키지 + 앱 `features/archive/` + `app/android/.../ShareActivity.kt` + `app/ios/ShareExtension/`. — `specs/0014-외부-공유-등록.md`, `specs/0010-아카이브-탭.md`(2026-07-31). **2026-07-29 크롤링/파싱 보정**: ① 공유 등록 자료의 제목이 URL로 남던 버그 수정(프리필을 안 고친 경우에만 `og:title`로 교체) ② 본문 추출을 `doc.body().text()`에서 시맨틱 태그 기준 정제로 교체(문단 줄바꿈 유지) ③ 비동기 크롤링 경로에 없던 길이 상한을 동기 경로와 공유(`ArchiveTextLimits`) — 측정은 `ai/docs/EXPERIMENTS.md` #12, 미해결로 올린 2건(3xx 무처리·네이버 PC 주소 본문 0자)은 `specs/OPEN.md`. **2026-07-30 해결**: 두 건 다 `JsoupUrlCrawler`가 크롤링 실패(`FAILED`)로 처리하도록 수정 — 3xx는 상태코드 직접 확인(`rejectRedirectStatus`), 네이버 블로그 PC 주소 등 본문이 실제로 빈 페이지는 일반 규칙(`toCrawlResult`, iframe 특화 추종 없음)으로 흡수(`specs/OPEN.md` 확정 기록). **2026-08-02 모아보기 리디자인**: 목업 맞춤 — S-25 폴더 목록을 **2열 그리드**(대표 썸네일 카드 + "저장된 항목 N개", 상단 ···=폴더 추가, 카드 ⋮=이름수정/삭제), S-25-A 정렬 탭 pill·항목 타일(썸네일+태그칩+날짜·출처+좋아요), S-25-B를 **카드 오버레이 히어로**(투명 SliverAppBar 이미지 배경 + 라운드 흰 시트, 좋아요 상단·폴더이동/핀/삭제는 ··· 메뉴)로 리스타일. 태그 칩 **primary 테두리**, AI 요약 카드 **그라데이션 테두리**(`archive_widgets.dart` 신규). `ArchiveFolder.thumbnail`·`ArchiveItemDetail.memo` nullable 추가. **백엔드 요구(미구현·전달)**: 폴더 목록 응답 대표 `thumbnail`(집계), 항목 `memo`(컬럼+상세응답+등록입력, 후속) — `specs/OPEN.md`. **2026-08-03 유튜브·인스타 크롤링**: 둘 다 페이지가 JS 껍데기라 `본문을 찾을 수 없는 링크예요`로 등록조차 안 됐다(유튜브는 `youtu.be`·`/shorts/`·`m.`이 303/302라 SSRF 방어에도 막혔다). `CompositeUrlCrawler`(@Primary)가 호스트로 갈라 전용 크롤러에 넘기고 나머지는 기존 `JsoupUrlCrawler`로 — **일반 크롤러는 한 줄도 안 고쳤다**(블로그 회귀 위험 0). 유튜브는 **리다이렉트 추종 대신 URL 정규화**(id 검증 `[A-Za-z0-9_-]{11}`이 SSRF 방어선), 본문은 `ytInitialPlayerResponse.shortDescription` 전문. 인스타는 **2단계 부트스트랩**(페이지에서 lsd·csrftoken을 매번 새로 딴 뒤 persisted GraphQL), 본문은 캡션. **인스타 썸네일만 MinIO에 받아둔다** — CDN URL이 `oh=`/`oe=` 서명으로 몇 시간이면 만료돼 그대로 저장하면 깨진 이미지가 된다(사용자 확정). 에뮬레이터 실측으로 세 갈래 전부 제목·본문·썸네일·AI 태깅·AI 요약 확인. **곁가지로 잡은 것 셋(전부 서버 로그가 조용한 종류)**: ① 실제 1.28MB 페이지에서 정규식이 `StackOverflowError`(픽스처 28개는 통과 → 선형 스캐너) ② 버킷 정책이 `archive/*`를 안 열어 업로드는 되는데 앱에서 403 ③ `publicUrl`이 서버 접속 주소를 그대로 써서 에뮬레이터가 못 닿음 → `minio.public-base-url` 분리(프로필 사진·방 커버도 같은 경로였다). `doc_id` 갱신 절차와 차단 위험은 `specs/OPEN.md`. **2026-08-04(QA)**: 외부 공유(S-25-D) 폴더 선택 단계에서 폴더가 0개면 종료하던 것을, 공유 화면 안에서 바로 새 폴더를 만들어 등록하도록 Android(`ShareActivity.kt`)·iOS(`ShareViewController.swift`+`ShareAPIClient.createFolder`) 양쪽에 추가(기존 `POST .../archive/folders` 재사용, 서버 무변경). `specs/0014`의 "폴더 신규 생성 안 함" 결정을 개정. flutter test 불가한 네이티브 경로라 폴더 0개/N개 기기 QA가 완료 조건. 🔴 **2026-08-05 유튜브를 Data API v3 로 교체** — 240 의 HTML 스크래핑이 **운영에서 100% 실패**하고 있었다. 원인은 우리 코드가 아니라 **구글의 봇 판정**이다: 같은 URL·같은 UA 로 가정용 IP 는 200(1,283,912 bytes), 운영 EC2(데이터센터 IP)는 `302 → google.com/sorry`(CAPTCHA, 387 bytes). 리다이렉트 추종은 답이 아니라(목적지가 CAPTCHA) `videos.list?part=snippet` 로 갈아탔다 — 제목·채널명·설명 전문·썸네일을 전부 준다. `oEmbed` 는 **설명을 안 줘서 기각**(설명이 요약·태깅·추천의 주 입력). **HTML 폴백을 남기지 않았고 키에 IP 제한도 걸지 않았다**(사용자 확정) — 남기면 로컬은 HTML·운영은 API 를 타서 "로컬에서 통과한 것이 운영에서 죽는" 이번 사고의 구조가 그대로 남는다. 썸네일은 `maxres → standard → high → medium → default` 폴백(`maxres` 는 옛날 영상에 없다). `snippet.tags` 는 받지만 **안 쓴다**(측정 없이 AI 입력을 바꾸지 않는다). ⚠️ **곁가지로 잡은 것 둘**: ① 교체 전 실패 경로(`statusCode() >= 300`)가 **로그를 한 줄도 안 남겨** 원인 규명에 EC2 SSH 가 필요했다 → 실패 경로마다 상태코드·사유를 남긴다 ② 생성자를 둘로 만들면서 `@Autowired` 를 빼먹어 `@SpringBootTest` 249개가 동반 실패했다(전체 빌드가 잡았다 — 대상 테스트만 돌렸으면 놓쳤다). **2026-08-05 네이버 지도 공유 링크 등록(`NaverUrlCrawler`)**: 지도 앱 공유 버튼이 주는 단축 주소(`naver.me/…`)가 3xx 라 SSRF 방어(추종 금지)에 걸려 등록조차 안 됐다 — `youtu.be` 와 같은 형태다. 전용 크롤러가 **호스트를 검증한 한 홉만** 따라가고(`allowed-hosts` 밖이면 요청조차 안 한다), `map.naver.com` 목적지에서 **place id 만 뽑아** `m.place.naver.com/place/{id}/home` 으로 간다(업종에 따라 바로 200 이거나 한 홉 더 — 그래서 업종을 몰라도 된다). **새 시크릿이 없다** — 네이버는 place id 로 조회하는 공식 API 가 없어(검색 API 는 키워드 전용) 페이지 안 `PlaceDetailBase:{id}` 객체에서 가게명·업종·도로명주소·한줄평·편의시설을 읽는다. `og:description` 은 리뷰 수라 못 쓴다. **장소가 아닌 `naver.me` 는 펼쳐서 일반 크롤러에 넘긴다**(사용자 확정) — 블로그·뉴스 단축도 함께 살아난다. 실측: 단축 경로 1.37초 · 직접 주소 0.28~0.75초, 운영 EC2 에서도 200(유튜브와 달리 데이터센터 IP 가 안 막힌다). ⚠️ **모바일 UA 필수**(PC UA 는 첫 요청부터 429), ⚠️ **없는 장소도 200**(제목 유무로만 판정), ⚠️ 인스타 `doc_id` 와 같은 등급으로 깨지기 쉽다(`specs/OPEN.md` 에 갱신 절차). 곁가지: **UA 검사 테스트가 마지막 요청만 보고 있어 단축 주소 요청의 UA 는 아무도 안 보고 있었다** — 변형 테스트로 잡아 세 요청 전부 검사하게 고쳤다. 🔴 **2026-08-05 공유 등록 3건(네이버 크롤러 머지 후 에뮬레이터 검증에서 드러난 별개 문제들)**: ① **공유 값에서 URL 을 뽑는다** — 네이버 지도 공유 버튼은 URL 만 주지 않고 `[네이버지도] 돼지통 역삼2호점 서울 강남구 … https://naver.me/…` 를 준다. 양 네이티브가 **문자열 전체가 URL 인지**를 보고 있어(`Patterns.WEB_URL…matches()` / `URL(string:)?.host`) 텍스트로 등록됐고 **크롤링이 아예 안 일어났다**(실측 id 60: `url=(빈칸)`). 값 안의 http(s) 조각 중 **마지막 것**을 쓰도록 Android(`SharedTextUrl.kt`, JVM 테스트 8건)·iOS(`ShareContent.swift`) 양쪽에 같은 규칙을 넣고 **단일 진실을 `specs/0014` 에 적었다**. ⚠️ **iOS 분은 한 번도 실행되지 않았다**(작업 머신 Windows, Swift 툴체인 없음) — 그리고 **Jenkins 는 네이티브 단위 테스트를 Android·iOS 둘 다 안 돌린다**(`specs/OPEN.md` 신규 항목). ② **텍스트 공유도 비동기로 통일** — 위 ①로 텍스트 경로를 타면서 드러났다: 크롤링이 없어도 LLM 3회라 5초를 넘겨 **서버는 성공했는데 앱은 "등록하지 못했어요"**, 다시 누르면 중복이 쌓였다. 클라이언트 타임아웃을 늘리는 대신 서버를 URL 경로와 같은 모양으로 맞췄다. 🔴 **①을 고치니 서버 쪽 원인이 하나 더 드러났다** — 지도 **앱**의 공유 링크는 웹과 **주소 모양이 다르다**: 웹은 경로형(`map.naver.com/p/entry/place/{id}`)인데 앱은 경로에 장소가 없는 좌표 URL(`map.naver.com/?lat=…&pinId=1340737588&pinType=site&…`)이다. `findPlaceId` 가 경로만 봐서 일반 크롤러로 넘겼고 그 주소가 또 3xx 라 죽었다(실측 `itemId=64` `FAILED`). `pinId` 가 place id 와 같은 값임을 확인하고 쿼리도 보게 고쳤다(변형으로 3건 실패 확인). **교훈: 링크 모양은 같은 서비스 안에서도 진입 경로마다 다르다** — 웹에서 한 번 재보고 "지원한다"고 적으면 안 된다. ④ **에뮬레이터 실측**: 사용자가 신고한 문자열 그대로 `id=65 · DONE · title=돼지통 역삼2호점 · source=naver.com` + 업종·주소·한줄평 본문 + 태그 4개 + 요약 + 임베딩. 긴 텍스트 공유는 **T+1.2초에 PENDING(본문 채워진 채)**, T+13초 이내 `DONE`+태그 5개. ⑤ **저장된 링크가 안 눌렸다**(같은 날 사용자 신고) — S-25-B 상세의 출처 URL 이 `color.primary` 를 입힌 `SelectableText` 라 **링크처럼 보이는데 탭해도 아무 일이 없었다**(어포던스만 있고 동작이 없음). `url_launcher`(이미 의존성에 있고 초대 공유·설정에서 쓰던 것)로 외부 브라우저를 연다. **http(s) 만 연다** — 옛 데이터나 서버 변경으로 다른 스킴이 들어오면 앱이 `intent:`·`javascript:` 를 열어주는 통로가 된다. 위젯 테스트 4건(열림·앱 없음·예외·스킴 거부), 변형으로 `onTap` 제거 시 4건 / 스킴 검사 제거 시 1건 실패 확인. ⚠️ `design.md` 에 **인라인 링크 패턴이 없어** 스타일은 손대지 않았다(`specs/OPEN.md`). ③ **인스타 GraphQL 관측성** — 재발한 인스타 실패의 원인을 못 짚었다(부트스트랩은 통과, GraphQL 만 HTML. **같은 순간 curl 은 200**). `queryPost` 가 상태코드를 확인조차 안 하고 있어(어제 유튜브와 같은 사각지대) 상태코드·`Location`·본문 앞 500자·**보낸 쿠키 이름**(값 제외)을 남기고 로그인 응답이면 1회 재부트스트랩 재시도한다. ⚠️ **이 커밋은 인스타를 고치지 않았다** — 서버 재시작으로 복구됐고 **재시도 코드는 발동조차 안 했다**. 가설 셋(IP 소프트 블록 / JVM 수명 누적 / 시간 경과)을 이 데이터로 못 가른다(`specs/OPEN.md`). 🔴 **2026-08-06 인스타 차단 대응 — 사용자에게 뜨던 "분석 실패"를 없앴다.** 앞선 조사에서 **IP 소프트 블록을 기각했던 것이 틀렸다**: 그 기각의 근거는 "같은 EC2 에서 curl 은 200" 이었는데 오후에 다시 재니 **curl 도 같은 challenge HTML(610KB)로 막혔다** — 아침의 200 은 차단이 아직 안 걸린 시간대였을 뿐이다. 함께 남아 있던 마지막 후보(Java TLS 지문)도 이걸로 배제됐고, 헤더 순서 가설은 운영 로그에서 순서가 그대로 나간 것을 확인하고도 실패해 직접 반증됐다. **차단은 영구가 아니라 10~20분이면 풀린다**(운영 DB 실측: 12:45 실패→12:54 성공, 15:18 실패→15:36 성공). 즉 실패율 58%(인스타 19건 중 11건)의 그 11건은 "안 되는 링크"가 아니라 **"타이밍이 나빴던 링크"** 였다. `test.md` 의 처방 셋 중 둘을 넣었다 — ① **24시간 캐시**(`V14`, 같은 URL 재공유 시 제목·본문·요약·임베딩·태그까지 재사용해 크롤링 1회 + 게이트웨이 3회를 아낀다) ② **자동 재시도**(`V15`, `crawl_retries`·`next_crawl_at`): 실패를 **다시 해볼 만한 것과 아닌 것으로 나누고**(`CrawlException.retryable`, 기본값은 "안 함") 일시적 실패는 `FAILED` 로 굳히지 않고 **`PENDING` 을 유지한 채 20분 뒤로 예약**한 뒤 5분마다 도는 배치(`ArchiveCrawlRetryScheduler`)가 다시 긁는다. 최초 1회 + 재시도 3회로 **최악 60분 안에** 확정된다. 🔴 **앱은 한 줄도 안 고쳤다** — `PENDING` 을 이미 "분석 중"으로 그리고 있어서(`CrawlStatusBadge`) 그 상태를 유지하는 것만으로 실패 배지가 사라진다. 앞서 넣은 **쿨다운**(`RedisCrawlBlockCooldown`, 차단 감지 후 10분 침묵)까지 셋이 겹쳐 재시도가 차단을 되레 키우는 것을 막는다(횟수 상한 3 · 배치 tick 상한 20건 · 쿨다운). ⚠️ **남은 처방은 residential egress 하나**인데 **비용 결정**이라 배포 후 실패율을 다시 재고 판단한다. 검증: 변형 14개(재시도 분류 4 · 처리기 5 · 배치 5) + 실제 Postgres 로 쿼리 의미 검증 + **차단→대기→자동 성공 전 구간 통합 테스트** + CI 와 같은 UTC 전체 빌드. 곁가지로 잡은 것 둘: ① `NaverUrlCrawler` 의 네트워크 실패 2곳이 jsoup 과 똑같은 모양인데 재시도 분류에서 빠져 있었다 ② 내가 쓴 시간 단언이 **두 `Instant.now()` 가 같은 클록 틱에 떨어질 때만 통과하는 플래키**였다(변형 검증 중 드러남). 🔴 **2026-08-06 인앱 등록(S-25-C)도 비동기로 통일** — `createItem` 이 HTTP 응답 전에 외부 호출을 4회(크롤링 1 + LLM 3) 돌아 사용자를 붙잡고 있었다(문서화된 최악 198초, 실측 텍스트 한 줄 13초). 공유 경로가 2026-08-05 에 같은 이유로 비동기가 됐으므로 두 경로를 같은 모양으로 맞췄다 — `PENDING` 저장 후 즉시 응답하고 나머지는 `ArchiveCrawlProcessor` 가 한다. **주소 검증만 동기로 남긴다**(`UrlSafetyValidator` 로 분리) — 오타·사설망 주소는 등록 시점에 400 으로 알려주는 편이 낫다. 🔴 **부수 효과로 `ArchiveItemService` 에서 크롤러·요약기·임베더·태깅 클라이언트가 통째로 빠졌다** — 넷 다 그 동기 경로에서만 쓰이던 것이라, 이제 이 서비스는 크롤링도 AI 도 하지 않는다. ⚠️ **UX 가 하나 바뀐다**: "등록 즉시 제목·태그가 보인다"가 사라진다. 사용자 피드백("분석하는 동안 앱에 아무 화면이 안 떠서 공유가 됐는지 모호")에 따라 등록 직후 **스낵바** + 목록 상단 **"분석 중 N건"** 으로 등록 사실을 알린다(세는 것은 앱 — 목록 응답에 이미 `crawlStatus` 가 있어 서버 무변경). 폴링은 넣지 않기로 했고(사용자 확정) 완료 시점은 **푸시 알림**이 맡는데 그건 아직 미구현이다(`specs/OPEN.md` 신규 항목 — 등록자 본인에게만·성공과 실패 둘 다·여러 건은 묶어서 한 번). 곁가지: 비동기화로 `ArchiveItemServiceTest` 가 조용히 플래키가 됐다(등록 직후 요약·임베딩을 단언하는데 경합이 생겼다) — 동기 실행기로 고정했다. 검증은 UTC 전체 빌드 · `flutter analyze` · 아카이브 위젯 테스트 75개. 🔴 **2026-08-06 자료 분석 완료·실패 푸시 추가** — 등록이 양쪽 경로 모두 비동기가 되면서 "언제 끝났는지"를 알 방법이 앱을 다시 여는 것뿐이 됐다. `V17` + `PushType.ARCHIVE_ANALYSIS_DONE` + 설정 토글, 그리고 Redis 버퍼에 사람별로 모았다가 **마지막 확정 뒤 1분 조용하면** 30초 배치가 한 번만 보낸다(공유로 5건을 연달아 넣어도 알림은 하나). 받는 사람은 등록한 본인뿐이다. 🔴 **실패는 자동 재시도가 끝난 뒤에만 보낸다** — 밀린 것을 실패로 세면 20분 뒤 성공할 것에 대해 "가져오지 못했어요"를 먼저 보내게 된다. 커밋된 뒤에 세는 것(afterCommit)도 같은 이유다 — 롤백된 작업까지 알림에 들어가면 안 된다. 곁가지: 설정 화면의 「재촉 (노크)」 토글이 **끌 수 있는데 아무것도 막지 않는 스위치**였다(콕찌르기와 2026-07-29 에 통일됐는데 UI 만 남아 있었다) — 지웠다. 그리고 `PushType` 테스트가 하나도 없어서 그게 살아남았다는 것을 확인하고, **알림↔스위치 연결을 양방향으로 고정하는 테스트**를 새로 만들었다(알림마다 그것만 끄는 스위치가 정확히 하나 / 스위치마다 그걸 읽는 알림이 존재). 변형으로 노크 버그를 재현해 잡히는 것을 확인했다. 🔴 **2026-08-06 텍스트 자료의 AI 요약을 버튼으로 뺐다** — 유저 테스트에서 "텍스트 등록 시 AI 응답 기다리는 시간이 아쉽다"는 피드백이 나왔다. 사용자가 직접 적은 짧은 메모는 본문이 곧 요약이라 얻는 것이 거의 없으면서 LLM 호출과 기다림만 쓴다. 텍스트 등록의 자동 요약을 끄고, 자료 상세(S-25-B)에 「AI 요약 만들기」 버튼을 뒀다(`POST .../items/{itemId}/summary`). **링크 자료의 자동 요약은 그대로다** — 본문이 길어 추천 프롬프트 비용에서 실제로 값을 한다. 요약이 실패했던 링크 자료도 같은 버튼으로 되살릴 수 있고, "요약이 이미 있으면 400" 규칙이 남용 방지를 겸해 별도 레이트 리밋을 두지 않았다. 백필에만 있던 요약+재임베딩 로직을 `ArchiveSummaryFiller` 로 꺼내 버튼과 공유한다 — 두 경로가 같은 규칙을 써야 벡터 축이 어긋나지 않는다. ⚠️ 텍스트 자료는 요약이 없어 **본문으로** 임베딩되므로 한 방 안에 두 축이 섞인다(`specs/OPEN.md` 신규 항목). 검증: UTC 전체 빌드 · 서버 테스트 5건 + 위젯 테스트 5건 · 아카이브 도메인 368건. 🔴 **2026-08-06 핀 고정 우선배치**: 핀 고정 항목이 정렬에 아무 영향을 안 주던 것을 고쳤다. 폴더 내 목록(S-25-A)은 서버 쿼리를 `pinned desc, created_at desc`로 바꿔 핀이 항상 최상단에 오게 했고(`findByFolderIdOrderByPinnedDescCreatedAtDesc`), 앱도 "좋아요순" 재정렬이 그 우선순위를 깨지 않도록 핀/비핀을 나눠 각자 정렬 후 이어붙인다. 홈 미리보기(`previewArchives`)는 위 항목에서 같은 날 별도로 구현 완료. 🔴 **2026-08-07 방마다 폴더 최소 1개 보장(백엔드 요청)**: 방 생성 시 "기본" 폴더를 즉시 만들고, 폴더 0개인 기존 방은 목록 조회 시점에 보정한다(`ArchiveFolderService.listFolders`). 마지막 남은 폴더는 이름과 무관하게 삭제를 막는다(개수만 본다, `specs/OPEN.md` 근거). 폴더 미지정 등록 엔드포인트(`POST /rooms/{roomId}/archive/items`, "기본"으로 들어감)도 추가했지만 앱은 아직 이 경로를 안 씀. 같은 날 `pinnedArchives`(순수 핀 필터, 홈 대시보드) 필드도 별도 백엔드 요청으로 추가. 🔴 **2026-08-08 자료 상세(S-25-B)를 Figma 리디자인에 맞춤**: 받은 오토레이아웃 좌표가 **전부 간격 12로 떨어져**(사진 0/180 · 제목 192/26 · 메모 230/40 · 구분선 282/1 · 링크 295/20 · 태그 327/20 · 배너 359/40 · 본문 411) 기존 리듬·사진 180·반경 16·구분선·`AiHintBanner`·태그 칩 규격이 이미 시안과 같다는 것이 확인됐다 — **실제로 바뀐 것은 여덟 가지**다: 앱바에 폴더 이름 / 썸네일 폴백을 `primary` 빨강 그라데이션 → **`surface-soft` 면 + 이미지 아이콘 40(`muted-soft`)** / 좋아요를 우하단 → **우상단**(상·우 16, 카운트는 하트 바로 아래 `badge` 11) / 사진 **우하단에 등록자 아바타 24 원**(`surface-strong`) 신설 / 제목 1줄·메모 2줄 말줄임 / 링크 줄에서 **등록 날짜 제거**(URL 없는 텍스트 자료는 줄과 앞뒤 간격이 통째로 사라진다) / 링크 아이콘↔텍스트 간격 4→6 / 본문(AI 요약) 색 `foreground` → **`foreground-soft`**. **서버는 `ArchiveItemDetailResponse`에 `folderName`·`createdBy`(`MemberBriefResponse` 재사용, nullable) 두 필드만 추가**했다 — 라우트가 `/archive/item/:id` 하나뿐이라 앱이 폴더도 등록자도 넘겨받지 못한다. 컬럼은 이미 있어 **마이그레이션 없음**. 🔴 **곁가지로 잠재 장애를 하나 막았다**: `spring.jpa.open-in-view: false`인데 `folder`·`createdBy`가 둘 다 LAZY라, **의도적으로 `@Transactional`이 아닌** `updateUrl`(커밋 전에 비동기 크롤링 스레드가 옛 상태를 읽는 경합을 피하려고) 경로에서 `folder.getName()`을 부르면 `LazyInitializationException`이 난다. 지금까지 무사했던 것은 `getFolder().getId()`처럼 **식별자만** 읽었기 때문이다(프록시는 초기화 없이 id를 내준다). 조회를 join fetch(`findForDetailById`, `createdBy`는 탈퇴자 때문에 `left join`)로 바꾸고 **`getDetail`이 아니라 `updateUrl`을 타는 회귀 테스트**를 따로 뒀다 — 다른 상세 테스트는 전부 `@Transactional`인 `getDetail`을 타서 이 구멍을 못 잡는다. ⚠️ **시안과 다르게 확정한 것 3건**(전부 `specs/OPEN.md`): 앱바 제목은 시안이 자료 제목이지만 **폴더 이름**으로 감(그 결과 폴더 목록 화면과 상세 화면의 앱바 글자가 같아진다) / 시안의 좌우 인셋 20은 토큰 스케일 밖이고 **모든 화면의 여백**이라 16 유지(시안 파일 자체가 제목·본문만 358=인셋 16으로 섞여 있다) / 좋아요 우측 여백 13 → 16. 검증: 위젯 테스트 신규 9건(아바타 위치·등록자 없음·회색 폴백·앱바·1줄/2줄·본문 색·날짜 없음·링크 간격 6·링크 없는 자료) + 옛 동작을 단언하던 2건을 뒤집음, **뮤테이션 3건**(아바타 우하단→우상단 / 메모 maxLines 제거 / 링크 간격 6→4)이 각각 해당 테스트만 실패시키는 것 확인, 앱 전체 543건 통과. **reviewer 반영(같은 날)**: ① `specs/design.md` 갱신이 빠져 있었다 — 폴백·스크림 방향·좋아요 위치·패딩·카운트 타이포 다섯 값과 등록자 아바타가 전부 design.md 250행과 충돌하는 상태였다(CLAUDE.md 는 디자인 값은 design.md 가 이긴다). 사진 카드 절을 다시 쓰고 **링 없는 소형 아바타 24**를 아바타 크기 목록에 추가했다. ② 🔴 **뮤테이션 되돌리는 스크립트가 전역 치환이라 `_kLinkIconGap`(6)이 앱바 우측 여백과 제목↔핀 간격까지 번져 있었다** — Figma 근거는 링크 줄 하나뿐인데 무관한 두 곳의 시각이 바뀌어 있었고 테스트도 그 자리를 안 잠갔다. 원래대로 `AppSpacing.xs` 로 되돌렸다(교훈: 뮤테이션 원복은 전역 replace 로 하지 말 것). ③ 좋아요 하트·카운트 색을 `muted-soft` → **`muted`** 로 고쳤다 — Figma 가 카운트 채우기로 지정한 값이 원래 `muted` 였는데 하트 색(시안이 iOS 시스템 색 `Grey/Medium` 을 물고 있었다)에 묶어 잘못 넣었다. design.md §접근성이 `muted-soft` 를 비활성 텍스트 전용으로 못 박았고, `surface-soft` 위 대비가 약 2.9:1 로 규정값 3.5:1 에도 못 미친다(`muted` 는 약 5.3:1). ④ 등록자 아바타가 **프로필 사진 로드 실패 시 빈 회색 원**으로 남았다(`DecorationImage` 는 에러 폴백이 없다) — 이니셜을 항상 깔고 그 위에 `Image.network(errorBuilder:)` 를 얹는 구조로 바꿔, "등록자가 없어 자리를 비운 것"과 구분되게 했다. ⑤ 테스트 구멍 2건을 닫았다 — 좋아요의 `onPhoto` 분기와 카운트 타이포가 안 잠겨 있었고(reviewer 가 뮤테이션으로 실증), 등록자 null 테스트가 이 화면에 존재하지도 않는 `CircleAvatar` 를 찾고 있어 **항상 통과하는 공허한 단언**이었다(시맨틱 라벨 기준으로 교체). 등록 경로(`createItem`)의 folderName·createdBy 도 함께 못 박았다 — 그쪽이 안전한 이유(새 엔티티라 merge 가 아니라 persist)가 코드에 드러나 있지 않았다. ⑥ 문서 거짓 2건 정정 — 테스트 Javadoc 이 "join fetch 를 빼면 빨개진다"고 적혀 있었는데 실측으로 틀렸고 (실제 방어선은 `buildDetail(item)`), `specs/0010` 진입 경로에 적은 **"알림 딥링크"** 는 구현이 없었다 (딥링크 배선 자체는 있지만 매핑이 `/archive` 탭까지고 상세로는 안 간다). **AI 요약 서식 — 문장마다 줄바꿈(2026-08-08)**: 자료 상세의 요약이 4~6문장 통짜라 읽기 어렵다는 QA. 프론트 요청은 프롬프트가 `\n`이나 `**강조**`를 넣어 달라는 것이었는데, **재보고 둘 다 기각했다** (`ai/docs/EXPERIMENTS.md` #33, 크레딧 45회) — 모델이 줄바꿈 규칙을 **7/15 · 8/15건만** 지켰고(두 번 재현), 강조는 `핵심 3~4개` 범위를 **2/15건**만 지키면서 **계정명을 강조까지 했다**(`**COMBINE**님의`). 어투도 명령형·존댓말로 이탈했다(기준선 0/15). 그래서 **서버 저장값을 한 글자도 바꾸지 않고 앱이 문장 경계에서 줄을 바꾼다**(`app/lib/features/archive/summary_text.dart`) — 개행 100%, 추천 프롬프트·임베딩 입력 무변경(그래서 방 평가 불필요), 이미 등록된 자료도 재요약 없이 즉시 반영. 요약 프롬프트가 평서문(~한다/~다)을 못 박고 있어 문장 경계는 추정이 아니라 문법이다. ⚠️ 실제 데이터의 함정 둘을 테스트로 잠갔다 — 콘텐츠 이름 `드.디.어.`(마침표 뒤 공백이라 단순 분리 시 한 문장이 두 줄로 찢어진다)와 `~보자.`처럼 `다`로 끝나지 않는 문장. **볼드는 보류**(기각 아님)이고 근거·재개 지점은 `specs/OPEN.md`. 회신은 핸드오프 문서 하단. ⚠️ **측정 도구가 한 번 거짓말을 했다** — 문장을 `~다.`로 세다가 어투 이탈 때문에 4문장을 1문장으로 세어 개행 미준수를 준수로 집계했다(#33 ⑤). 측정 대상이 흔들릴 수 있는 축을 계측기의 전제로 쓰지 말 것. 🔴 **2026-08-08 자료 상세 댓글 백엔드(docs/backend/archive-comments-handoff.md)**: `archive_item_comments` 테이블(`V23`, item_id CASCADE·author_user_id는 `archive_items.created_by`와 같은 근거로 SET NULL) + `GET/POST /rooms/{roomId}/archive/items/{itemId}/comments`(오래된 순, 페이지네이션 없음) + `ArchiveItemDetailResponse.commentCount` 추가. 삭제 API는 이번 라운드에 없다(권한 미결, `specs/OPEN.md`). **프론트 UI는 아직 없다** — 데이터만 준비, 바텀시트는 프론트 후속. 🔴 **2026-08-09 댓글 프론트 구현**: 상세 하단 반응 바에 댓글 아이콘+`commentCount`, 댓글 바텀시트(`archive_comments_sheet.dart` — 오래된순 목록·작성·내 아바타 입력바, 탈퇴 작성자 '?' 폴백). 수정·삭제는 사용자 확정("본인 댓글 수정·삭제 필요")으로 백엔드에 PATCH/DELETE 요청(`docs/backend/archive-comments-handoff.md` 🆕절). 🔴 **2026-08-09 이미지 탭 = 투두 첨부 사진 피드(기획 확정) → 같은 날 백엔드 구현 완료**: FE 선구현 — 2열 그리드(사진+핀+담당자 아바타+투두 제목 탭 펼침). 서버는 `todos.image_url`(V16, 2026-08-07 롤백으로 걷어냈던 매핑을 부활) + `image_pinned`·`image_attached_at`(`V25` 신규) + `TodoResponse.imageUrl`/`Create·UpdateTodoRequest.imageUrl`(전체 교체 — 안 보내면 해제) + `POST /rooms/{roomId}/todos/image/upload-url`(투두 id 없이 발급, MinIO presigned PUT) + `GET /rooms/{roomId}/archive/todo-images`(방 전체·폴더 무관·핀 우선→최신 첨부순, `id==todoId`, 대표 담당자는 `userId` 오름차순) + `PATCH .../todo-images/{todoId}/pin`으로 FE 제안 계약을 그대로 구현했다(`TodoImageService`/`TodoImageController`, `domain/todo/`). MinIO 버킷 공개읽기 정책에 `todos/*` 추가(`MinioConfig`, 안 넣으면 업로드는 되고 앱에서만 403 나는 함정 — 과 같은 패턴이라 같은 커밋에 넣음). ~~**앱(Flutter)의 실제 업로드 배선(`todo_form_sheet.dart` → `imageUrl` 실어 보내기)은 이번 스코프 밖**~~ → **2026-08-09 같은 날 프론트도 완료**(위 투두 탭 항목 참고, `TodosApi.uploadTodoImage` + `TodoFormSheet.uploadImage`). 상세는 `docs/backend/todo-image-archive-handoff.md`(갱신 완료), `specs/0010-아카이브-탭.md`, `specs/0006-투두-탭.md`. 🔴 **2026-08-09 외부 공유 등록(S-25-D) 등록 단계 제거 + 커스텀 디자인(QA, 사용자 확정)**: 폴더를 고르거나 새로 만드는 즉시 공유된 값을 제목으로 그대로 등록하도록 바꿔 "제목 입력→등록" 단계를 없앴다(구 `showRegisterDialog`/`showRegisterForm` 제거). 방·폴더 선택·새 폴더 입력 다이얼로그도 시스템 `AlertDialog`/`UIAlertController` 대신 design.md 토큰(`ShareTokens`/iOS `UIConstants`)으로 스타일링한 커스텀 뷰로 교체했다(Android `ShareActivity.kt`, iOS `ShareViewController.swift`) — 네이티브 폰트 번들은 하지 않고 시스템 폰트 + 색·라운드·간격만 맞췄다. `SharedTextUrl`/`ShareContent`(URL 추출 로직)는 무변경(회귀 테스트 10건·기존 Swift XCTest 그대로 통과). `specs/0014-외부-공유-등록.md` 갱신. 🔴 **2026-08-09 후속 — 모아보기 폴더 직접 업로드 이미지 자료 신설(사용자 요청, 투두 첨부 이미지와 별개 경로)**: 위 "이미지 탭 = 투두 첨부 사진 피드" 확정과 부딪혀("이미지 출처는 폴더 등록이 아니라 투두 첨부") 사용자에게 먼저 확인 — **"별도 이미지 자료 경로 신설"**로 결정, 두 경로가 공존한다. `archive_items.image_url`(`V26`, nullable) 신규 컬럼 — 별도 이미지 테이블 없이 기존 `url`(링크)/`body_text`(텍스트) 관례를 그대로 확장해 `image_url != null`이면 이미지 자료다(셋 중 정확히 하나). `ArchiveItemService.createItem`이 검증을 3-way로 확장하고, 이미지 분기는 크롤링·AI 태깅/요약/임베딩을 아예 안 타고(`dispatchCrawl` 미호출) 곧바로 `DONE`. 제목을 비우면 서버가 "사진" 기본값. 업로드는 `POST /rooms/{roomId}/archive/items/image/upload-url`(`TodoImageService`와 같은 presigned PUT 패턴, 키 `archive-images/{roomId}/{UUID}`) — 신규 `ArchiveItemService.createImageUploadUrl`/`ArchiveImageUploadUrlResponse`. `ArchiveItemResponse`/`ArchiveItemDetailResponse`에 `imageUrl` 필드 추가. 핀·삭제·폴더이동·댓글·태그는 기존 자료 엔드포인트를 그대로 재사용(신규 API 없음). **FE**: `ArchiveItem`/`ArchiveItemDetail`에 `imageUrl`, `ArchiveApi.createItem(imageUrl:, title:)`/`uploadArchiveImage` 신규, `ArchiveItemRegisterSheet`에 `ArchiveRegisterMode.image`(사진 선택 박스 + 선택적 제목, `todo_form_sheet.dart`의 이미지 피커 패턴 재사용), `archive_folder_items_screen.dart`의 ＋ "이미지 추가"가 더 이상 준비 중 스낵바가 아니라 실제 등록 플로우로 연결. **이미지 탭을 두 섹션으로 분리**("이 폴더에 올린 사진" 폴더 스코프 / "투두에 첨부된 사진" 방 전체 — 스코프가 달라 하나로 합쳐 정렬하지 않는다), 텍스트 탭 필터에 `imageUrl == null` 조건을 추가해 이미지 자료가 새지 않게 함. 상세 화면(`archive_item_detail_screen.dart`)의 사진 영역도 `imageUrl ?? thumbnail`로 이미지 자료를 표시. 검증: 서버 전체 746건(신규 5건 포함) + 앱 전체 634건(신규 15건 포함, `archive_api_test.dart`/`archive_item_register_sheet_test.dart` 신규 파일) 통과. `docs/backend/todo-image-archive-handoff.md`(대체 문구 철회 노트 추가)·`docs/backend/archive-image-upload-handoff.md`(구현 완료로 갱신)·`specs/0002-data-model.md`·`specs/0010-아카이브-탭.md` 갱신. 🔴 **2026-08-09 QA 문구 2건**: ① 외부 공유 등록 성공 시 "아카이브에 등록했어요"가 뜨던 것을 "모아보기에 등록했어요"로 통일(Android `ShareActivity.kt`·`AndroidManifest.xml` 공유 타깃 라벨, iOS `ShareViewController.swift`·`Info.plist` 확장 표시 이름 — 다이얼로그 제목·진행 문구·성공 문구 전부). ② 자료 분석 완료 알림(`ArchiveAnalysisNotifier`)의 1건 성공 케이스를 사용자 확정 문구로 교체 — 제목은 고정 "AI로 자료 분석이 끝났어요", 본문은 "{자료 제목}의 내용을 {방 이름} 모아보기에서 확인해보세요"(방 이름은 `ArchiveCrawlProcessor.notifyDone`이 `item.getRoom().getName()`을 `ArchiveAnalysisNotificationBuffer`에 함께 실어 보낸다 — 1건일 때만 의미 있고 여러 건이면 여러 방에 걸칠 수 있어 안 쓴다). 1건 실패·여러 건 문구는 기존 유지. `docs/backend/pending-backend-requests-2026-08-09.md`의 이모지 제안과는 다른 문구로 확정됐음을 표에 반영. `specs/0015-알림-트리거.md` 갱신, 서버 테스트 748건(신규 2건 포함) 통과. 🔴 **2026-08-09 자료 댓글 수정·삭제 백엔드 구현 완료**(`docs/backend/archive-comments-handoff.md`) — `PATCH`/`DELETE .../comments/{commentId}`, 작성자 본인만(아니면 403 `NotCommentAuthorException`, 탈퇴 작성자 댓글은 아무도 못 고침), 다른 자료의 댓글 id 접근은 404(`ArchiveCommentNotFoundException`). `updated_at`/`edited` 플래그는 요청서가 선택으로 명시해 스키마 변경 없이 스코프 제외. 댓글 행의 본인 한정 수정/삭제 UI는 프론트 후속.
- [x] ~~**iPhone 미리 알림식 TODO 인라인 작성·편집(2026-08-06)**~~ → **2026-08-07 롤백(사용자 확정)**. 목록 안 `TodoInlineComposer`로 날짜·위치·태그·담당자·중요 표시·사진을 입력하던 방식을 08-06 이전의 `TodoFormSheet` 바텀시트로 되돌렸다. 인라인 작성기와 "기존 입력 방식" 버튼을 삭제하고, 서버에서도 위치·태그·중요표시·사진과 사진 업로드 API(`POST /rooms/{roomId}/todos/image`)·`todo_tags` 매핑·요청의 `reminder` 객체를 걷어냈다. **`due_date`만 존치** — 협업 캐릭터가 마감 준수율로 `THE_J` 판정·신뢰도를 계산해, 입력 UI가 없으면 그 캐릭터가 영구 도달 불가해지기 때문. 대신 `TodoFormSheet`에 마감일 칩(없음/오늘/내일/이번 주말/직접 선택)을 새로 뒀다. **S-18 전체화면(`/todos/item/:id`)과 `GET /rooms/{roomId}/todos/{todoId}`는 유지**하고 본문 위젯만 폼으로 교체. `V16`의 `location`/`flagged`/`image_url` 컬럼과 `todo_tags` 테이블은 DB에 남기고 매핑만 끊었다(Flyway 과거 파일 불변 + 데이터 보존, `specs/OPEN.md` 후속). 상세는 `specs/0006-투두-탭.md` 2026-08-07 절.
- [x] 멤버 투두 페이지(S-30-M) + 콕찌르기/재촉 — 스펙 `specs/0011-멤버-투두-콕찌르기.md`(2026-07-27). 조회 전용 멤버 투두 + `POST .../pokes`(콕찌르기, 확인 없이 즉시)로 구현. **2026-08-07 마이페이지식 리디자인**: 헤더를 멤버 이름만으로, 중앙 히어로를 가로 프로필 행(아바타 + 이름 + '투두를 N% 완료했어요' + 우측 '찌르기' primary pill 버튼 `icon_light.png`)으로 교체, **멤버 협업 캐릭터 카드**(`MemberTodosApi.fetchCharacter` → `GET /rooms/{roomId}/members/{userId}/character`, `MyActivityCard` 재사용) 추가, 읽기전용 투두에서 체크 동그라미 제거(완료여부는 취소선·muted, 카테고리 접기/펼치기 토글만 유지).

### Phase 3 — AI · 설정 · 마무리
- [x] AI 투두 추천(S-16-B): FastAPI 후보 생성(`0d657cb`) + Spring 프록시·재료 수집(`da8a94c`) + 앱 S-16 [직접추가]/[AI추천추가] 선택·후보 목록·채택(담당자 미지정, 카테고리 이름→id 해석·중복 생성 방지) — `specs/0006-투두-탭.md` 2026-07-29 개정. 추천 모델은 **`gpt-5.4-nano`**(2026-07-30 `gpt-4.1-mini`에서 교체, 사용자 지시). 사전 실측 없이 정한 뒤 사후에 세 축을 확인했다 — 시간 3.5배(10.94→3.13초)·비용 약 절반($0.0017→$0.0009, 토큰 동일)·품질 합격 판정(`ai/docs/EXPERIMENTS.md` #18). 게이트웨이 크레딧 차감은 여전히 미확인(대시보드로만 확인 가능). **고유명사 원문 충실도는 2026-08-01 확인 완료** — 장소·상호명이 많은 6건의 후보를 요약·원문과 대조했고 손상이 없었다(`ai/docs/EXPERIMENTS.md` #20 ④). **요약·임베딩은 별도 스코프로 미착수**(각각 `V5`/`V7`에서 완료 — 아래 항목 참고). **2026-07-30 평가 하네스**: LangSmith 개발 트레이싱 배선 + 추천 러너(`ai/evals/`, 실제 자료 15건으로 후보·토큰·응답시간 관측). **자동 채점은 없다** — 프로토타입(공용 GPU 서버 + Qwen)의 채점기를 이식했다가 지표 셋이 전부 무효로 판명돼 코드·라벨·문서를 전면 삭제했다(`ai/docs/EXPERIMENTS.md` "다시 하지 말 것"). 교훈은 "지표를 먼저 만들고 데이터를 맞추지 않는다"이고, 다음 단계는 **실제 모델 출력을 보고 무엇을 잴지 정하는 것**이다. 아카이브 자동 태깅(S-25-C)은 Phase 2 아카이브 탭 항목에서 이미 구현+검증됐고 **Spring에 그대로 둔다**(`gpt-5-nano`, FastAPI로 이관하지 않음)
- [x] 자료 AI 요약(`archive_items.summary`, `V5`) + 추천 입력을 본문 → 요약으로 교체 (2026-07-30) — 목적은 화면 표시가 아니라 **추천 프롬프트 비용 절감**(자료 20건 기준 약 117,000 tok → 약 2,600 tok, `ai/docs/EXPERIMENTS.md` #8). **태깅 호출에 합치려던 원래 설계를 뒤집고 별도 호출(`AiSummaryClient`)로 분리**했다 — 합치려면 잘 돌고 있는 태깅 프롬프트를 JSON으로 바꿔야 하고 파싱이 깨지면 태그까지 날아간다. 요약 전용 모델은 `gpt-5.4-nano`(태깅은 `gpt-5-nano` 유지) — `gpt-5-nano`로 요약을 시키면 completion의 94~97%가 숨은 추론 토큰이고 12.9~23.9초가 걸린다(`ai/docs/EXPERIMENTS.md` #14). 요약 프롬프트는 To-do 중심·평서문·계정명/날짜 제외로 세 바퀴 돌려 확정(#15). 추천 입력은 **요약·본문 중 짧은 쪽**이라 임계값 상수가 없다. 앱은 S-25-B 본문 위에 표시(`specs/0010`). *(이 시점 기준 임베딩은 미착수였다 — `V7`에서 완료)*
- [x] AI 추천 후보 중복 재노출 방지(`todo_suggestion_exposures`, `V6`) (2026-07-30) — 추천을 두 번 누르면 같은 후보가 또 나왔다. **AI 서버는 원래부터 지원하고 있었다**(프롬프트에 싣고 `filter_candidates`가 코드로도 거른다) — Spring이 `excluded_todos`를 빈 배열로 보내고 있었을 뿐이라 AI 코드는 한 줄도 바뀌지 않았다. 기록 시점은 **후보를 생성해 반환할 때**(사용자 확정, 앱이 되보고하는 대안은 기각 — 그 보고가 실패하면 중복이 그대로 재발한다), 상한은 **방별 최근 N개**를 조회 제한으로만 지킨다(~~50~~ → **2026-08-03 16으로 축소** — 50이 후보를 말렸다. 채택한 것은 이미 `existing_todos`로 영구 제외되는데 노출 기록이 그 역할을 중복으로 하면서 **채택하지 않은 후보까지 영구히 막았다**. 실사용에서 후보가 8개→1~2개로 떨어졌고, 창 크기별 실측으로 16을 골랐다 — `ai/docs/EXPERIMENTS.md` #24. 앱 빈 상태 문구도 원인을 단정하지 않게 함께 고쳤다). **부수로 결함 하나가 고쳐졌다**: `suggest()`가 `@Transactional(readOnly = true)`인 채 실측 7~9초짜리 LLM 호출을 그 안에서 해 DB 커넥션을 붙잡고 있었다 → 읽기·LLM·쓰기를 세 경계로 분리(`TodoSuggestionPayloadLoader`/`TodoSuggestionExposureStore`). *(이 시점 기준 임베딩은 미착수였다 — `V7`에서 완료)*
- [x] AI 추천 후보 **의미** 중복 제거(임베딩 코사인, 임계값 0.70) (2026-07-30) — 177이 막은 것은 **글자가 같은** 재노출까지다. 실측에서 정규화 기준 24/24 전부 고유인데도 `유튜브 모의고사로 주제별 답변 구조 연습하기` 계열이 3회차 내내 반복됐다. AI 서버가 후보 제목과 `excluded_todos`를 임베딩해 유사도로 걸러낸다 — **스키마 변경·Spring 변경·새 의존성 0**(요청 시점에 임베딩하고 버린다). **임계값은 재서 정했다**: 실제 후보 24개 쌍별 코사인에서 중복 최소 0.527 vs 비중복 최대 0.648로 **구간이 겹쳐 단일 값으로 완벽히 갈리지 않으며**, 오탐이 미탐보다 비싸다고 판단해 `오탐 0` 기준으로 0.70을 골랐다(재현율 7/11, 놓치는 4쌍을 `ai/docs/EXPERIMENTS.md` #17에 명시). 이 과정에서 **"2단계"에 뭉쳐 있던 두 작업(후보 중복 제거 vs 자료 하이브리드 선별)이 서로 의존하지 않는다는 것을 발견**해 DECISIONS에서 분리했다.
- [x] **자료 하이브리드 선별 — 가중 순위 정규화** (2026-08-02) — RAG 2단계의 마지막 조각. 자료를 등록 역순 그대로 프롬프트에 넣던 것을, 네 축(좋아요 3.0 > 핀 2.5 > 유사도 2.0 > 최근성 1.0)으로 줄 세워 넣는다. Spring이 `ArchiveInfo`에 `embedding`·`pinned`·`likeCount`·`createdAt`을 실어 보내고, AI 서버 그래프에 `select` 노드가 `generate` 앞에 붙었다. ⚠️ **확정 설계였던 가중 RRF(k=60)를 실측으로 기각했다** — 좋아요·핀은 실서버에서 사실상 2단계 축(1/0)이고 유사도·최근성은 17단계라, RRF의 변별력이 `w × [1/(k+1) − 1/(k+n)]`으로 **단계 수에 비례**해 가중치 순서가 정반대가 됐다(가장 오래된 자료에 좋아요를 눌러도 17등→16등; k=0까지 낮춰도 유사도 1.88 > 좋아요 1.50). 튜닝이 아니라 구조 문제라 **등수를 축마다 [0,1]로 펴서 가중합**하는 방식으로 바꿨다 — 변별력이 곧 가중치가 되고, RRF를 고른 원래 근거(스케일 차이로 정규화 상수가 남는 것)는 나누는 값이 "관측된 등수 범위"라 그대로 지켜진다(사용자 확정). **동점은 같은 등수**(competition ranking)라 신호 없는 축은 조용히 빠진다. 값이 없는 것(임베딩 NULL·목표 임베딩 실패)은 **중립(한가운데)**이다 — ⚠️ 처음엔 꼴찌를 줬다가 리뷰에서 잡혔다: 꼴찌는 중립이 아니라 감점이라, 벡터가 `V7` 이후 등록분에만 있고 백필이 기본 꺼짐인 탓에 **"벡터 있음 ≈ 최신"** 이 되어 유사도 축이 최근성의 복사본이 됐다(실효 최근성 3.0 > 핀 2.5 — RRF를 기각한 것과 같은 실패). **라이브 검증**(자료 18건·좋아요 5·핀 1): 좋아요 5건이 상위 5위 독점, 본문 32~72자짜리 URL 자료 3건이 하위로 밀림, 예산 미발동(3,709자/12,000자), HTTP 200·9.1~9.4초·페이로드 390KB·임베딩 2회. 벡터를 최신 5건만 남긴 혼합 방으로 재현했을 때도 상위 5위는 좋아요 5건(벡터 보유 0건)이었다. 상세는 `ai/docs/EXPERIMENTS.md` #22
- [x] **기존 자료 요약 백필 + 요약 기준 재임베딩** (2026-08-03) — 요약 없이 등록된 자료가 추천 프롬프트를 잠식하고 있었다. `pickContent`가 요약·본문 중 짧은 쪽을 고르는데 요약이 없으면 **본문 전체**가 실린다. 실측(부산 여행 방, 자료 8건): 요약 있는 4건 529자 vs 요약 없는 4건 **34,206자 — 프롬프트의 98%**였고, 그 본문은 크롤링에 딸려온 블로그 UI 텍스트(`본문 폰트 크기 조정 … URL복사 신고`)가 섞인 원문이었다. ⚠️ **요약만 채우면 안 된다** — `ArchiveEmbedder`가 요약 우선으로 임베딩하므로, 요약을 채우면 한 방 안에 "요약 기반 벡터"와 "본문 기반 벡터"가 섞여 유사도 축이 서로 다른 잣대로 비교하게 된다. 그리고 그 어긋남은 기존 `ARCHIVE_BACKFILL_EMBEDDINGS`로 **못 고친다**(벡터가 NULL인 것만 찾는데 이 항목들은 이미 본문 기반 벡터가 있다). 그래서 요약 백필러가 직접 재임베딩한다. 재임베딩이 실패해도 요약은 저장한다(프롬프트가 줄어드는 것이 본론). `ARCHIVE_BACKFILL_SUMMARIES`, **기본 꺼짐**(건당 LLM 호출 2회). 전후 비교는 `ai/docs/EXPERIMENTS.md` #25
- [x] 자료 임베딩 생성·저장(`archive_items.embedding real[]`, `V7`) (2026-08-01) — 추천 자료 선별(RAG 2단계)의 **전제**다. 추천을 누를 때마다 아카이브 전체를 다시 임베딩할 수 없어(건당 약 1.4초) 등록 시점에 만들어 둔다. ⚠️ **이 티켓은 저장까지였다** — 읽어서 선별하는 것은 위 에서 완료됐다. **확정돼 있던 "벡터 저장소 = pgvector"를 뒤집었다**: 설계가 "FastAPI가 메모리에서 선별"이라 Postgres는 벡터로 검색하지 않고, pgvector에서 실제로 쓰는 것은 저장뿐인데 대가로 운영·로컬 이미지 교체 + Testcontainers 테스트 수정이 따라온다. **무엇을 임베딩하나 = 요약 우선, 없으면 본문(6,000자로 자름)** — 추천 프롬프트의 "짧은 쪽" 규칙과 일부러 다르다(그쪽은 토큰 절약, 여기는 검색 품질: 질의가 짧은 방 목표라 긴 본문 벡터는 평균화돼 무엇과도 어중간하게 닮는다). 자르는 지점은 모델 상한 8,191 tok에서 역산했다(실측 한국어 0.910~0.988 tok/char). 실패는 태깅·요약과 같은 폴백으로 `NULL` — 그 자료는 유사도 축에서만 빠지고 최근성·핀 축으로 후보에 남는다. 기존 자료 백필은 **기본 꺼짐**(`ARCHIVE_BACKFILL_EMBEDDINGS`, 크레딧을 쓴다). 실측·검증은 `ai/docs/EXPERIMENTS.md` #19 — 게이트웨이 실호출 200/1536차원, 백필 12/12·16.79초, `float[]`↔`real[]` 우회 없이 통과, 저장된 벡터와 요약 재임베딩의 코사인 1.000000
- [x] 설정 전체(S-40): 프로필·알림·현재방·멤버관리·방나가기·지난방보기·회원탈퇴 — 스펙 `specs/0012-설정.md`. 프로필/알림은 완료(/201). **2026-08-03 앱 상세 화면 완료**: `/settings/*` 6개 화면을 목업에 맞춰 구현하고 프로필 사진 presigned PUT, 방 상세 프리필·대표 이미지 변경, 현재 초대코드 조회(`GET /rooms/{roomId}/invite-code`)·재발급, 멤버별 진행률, 마지막 멤버 나가기 경고, 종료 방→S-05 이동을 연결했다. `GET /rooms` 응답에는 설정 프리필용 `goalDetail`/`coverImage`를 추가했다. Flutter 설정 테스트 12개·반응형 320px/큰 글자·골든 비교, Spring `RoomServiceTest`, iOS Simulator 빌드로 검증. **회원 탈퇴 백엔드**(`DELETE /me`, 2026-08-03 확정 — 소속 방 전체 자동 나가기 후 유저 삭제, 좋아요·콕찌르기·투두담당·알림설정 연쇄 삭제, 다른 멤버가 남은 방의 아카이브 자료는 작성자만 지우고 자료는 유지, Firebase 계정도 완전 삭제. `V9__account_deletion_cascades.sql`이 `users.id` FK 6곳의 삭제 불가 문제도 함께 해소)도 완료. **2026-08-03 앱 회원 탈퇴 연결 완료**: 강한 경고 확인 모달 → 인증된 `DELETE /me`(204) → 방 캐시·온보딩 완료값·Firebase/iOS Share Extension 세션 정리 → S-01 이동. 실패 시 기존 로컬 상태를 유지하고 재시도를 안내한다. **문의하기**는 `mailto:myeonglyeonghajim@gmail.com`으로 OS 메일 작성 화면을 열며, 열 수 없으면 주소를 직접 안내한다. S-05(지난 방 탭 시 이동할 상세 화면)는 `specs/0013-종료된-방.md`에 후순위로 명시돼 별도 스코프. **2026-08-07 마이페이지 리디자인 + '마이' 탭 이관**: 제목 '설정'→'마이페이지', 상단 프로필 헤더("MODI와 함께한 지 N일차" 캡션) + 활동 카드(`MyActivityCard` — 캐릭터/지표 칩, A안 placeholder) 추가, 그룹을 둥근 흰 카드 4개로 재편(제목을 카드 안 16/700, 행 아이콘 제거·구분선 제거, 행 라벨 #6A6A6A 13/medium), '로그인 계정 정보'는 로그인 수단 배지(카카오/구글/애플/이메일) 표기 전용. **라우트 `/settings`→'마이' 탭 `/mypage`로 이관**(하위 `/mypage/*`는 rootNavigatorKey 풀스크린), 홈 상단 햄버거(☰)→알림 벨(🔔, 탭 시 알림 설정)로 교체. 백엔드 요구(`/me/profile`에 `createdAt`·`loginProvider`, 활동 카드=`GET /me/character`)는 `docs/backend/my-page-handoff.md`·`character-handoff.md`. 골든 `settings_flow.png`는 macOS 재생성 필요.
- [ ] 방 종료 자동 전환(4-3) + 종료된 방 요약형 홈(S-05, 후순위였으나 전체범위) — 스펙 `specs/0013-종료된-방.md`(2026-07-27). **부분**: 4-3 자동 전환은 구현 완료(2026-07-30, `feature/4-3-room-auto-end`) — 배치가 아니라 요청 시점 lazy 체크로 확정(AskUserQuestion, `specs/OPEN.md`), `RoomService.refreshStatus`가 초대 미리보기·참여·내 방 목록·지난 방 목록·홈 대시보드 전 진입점에서 `end_date` 경과 시 `ENDED`로 전환한다(만료된 방에도 계속 join 가능했던 실제 버그도 함께 해소). **S-05 종료된 방 요약형 홈 화면은 여전히 미구현**(후순위 스코프, 앱 라우트 없음).
- [x] 푸시 알림(콕찌르기/재촉/일정 전날·디데이/방 입장·퇴장/내 담당 투두 추가) — Firebase Cloud Messaging(FCM). 콕찌르기(=재촉, 동일 기능으로 통일)는 기존 완료. **2026-08-05 나머지 5종 구현 완료**(`specs/0015-알림-트리거.md`) — 공통 게이팅 로직을 `PokeService`에서 `global.notification.PushNotifier`로 추출해 재사용. 일정 전날/디데이는 이 프로젝트 최초의 `@Scheduled` 배치(매일 08:00 KST, `ScheduleReminderService`, Redis로 중복 발송 방지)로 구현했고, 방 입장/퇴장은 `RoomService`에, 투두 담당자 추가(신규 배정만·본인 제외)는 `TodoService`에 각각 트리거를 추가했다. 새 REST 엔드포인트·스키마 변경 없음(설정 API·`V13`이 이미 있었음). **마감임박 알림은 여전히 미착수** — `full_spec.md`가 말하는 게 방 `end_date` 임박인지가 `specs/OPEN.md` 미결이라, 이번에 구현한 "일정 디데이"(schedules.date 기준)와는 다른 개념이다.
- [x] **협업 캐릭터(마이 탭) — 서버만 구현 완료, 백엔드 핸드오프** (2026-08-07) — 프론트/디자인 핸드오프 문서(10종 캐릭터·진화 사다리·confidence)를 서버 판정 로직으로 구현. 이번 스코프는 **백엔드뿐**(문서 자체가 "프론트는 UI만 담당"이라 명시) — 마이 탭 화면은 여전히 빈 화면으로 남고, 후속 프론트 작업이 아래 API를 소비한다. `domain/character/`(신규): `user_activity` 로그 테이블(`V21`, 90일 보존 배치)에 기존 조회 엔드포인트(대시보드/아카이브 상세/투두 상세) 호출 시 자동 적재, `CharacterService`가 완료 타이밍·완료율·스트릭·좋아요 준 횟수로 결정 트리 판정. `GET /me/character`(전역, 방 무관 합산)·`GET /rooms/{roomId}/members/{userId}/character`(방 멤버십으로 접근만 확인, 같은 전역 캐릭터 반환)·`POST /me/activity/app-open`(준비만, 앱 미호출). 임계값·정규화 방식은 문서가 구체화하지 않아 저리스크 기본값으로 정함(`specs/OPEN.md` 근거). 상세는 `specs/0016-협업-캐릭터.md`. **2026-08-07 프론트 연동**: 마이 탭 `MyActivityCard`를 로컬 캐릭터 일러스트(`assets/images/characters/{id}.png`)에 연결 — `characterId→파일` 매핑(에셋 파일명 오타 `procrasinator.png` 우회, `SPRINTER`는 전용 아트 없어 `steady`로 임시 대체), `GET /me/character` 응답 계약대로 `MyActivitySummary.fromJson` 파싱. **실제 API 연결 완료**(`SettingsApi.fetchCharacter`가 인증 호출로 실서버 데이터 표시, `settings_api_test`로 계약 검증). ⚠️ 후속: SPRINTER 아트 제작, `procrasinator.png` 파일명 정정 여부, 진화 바 UI(evolveProgress/Hint 미표시). **2026-08-09 버그 수정(마감 준수 0% 오표시 QA)**: 마감일 있는 완료 투두가 하나도 없는 사용자도 "마감 준수 0%"로 표시돼 "마감을 전혀 안 지킴"처럼 보이던 문제 — `ActivityStatsResponse`에 `dueDateCompletedCount` 필드 추가(서버는 이미 `Confidence` 판정에 이 값을 쓰고 있었는데 응답에만 안 실었음), 프론트 `MyActivitySummary.fromJson`이 이 카운트가 0이면 마감 준수 칩을 숨기도록 게이팅.
- [x] **알림 내역(S-41 QA)** (2026-08-09) — 홈 상단 알림 벨이 알림 **설정**(`/mypage/notifications`)으로 직행하던 것을, 실제로 받은 알림 이력을 보여주는 새 화면으로 교체(설정 메뉴 자체는 마이페이지에 그대로 유지). 신규 `notifications` 테이블(`V24`, 개인 기록·90일 보존·`NotificationRetentionScheduler`) — `global.notification.PushNotifier.notify/notifyEach`의 게이트 통과 지점 **한 곳**에 기록 훅을 추가해 발송 트리거 7종이 자동으로 전부 커버됨(FCM 발송 성공 여부와 무관하게 기록 — "발송 대상이었다"는 사실 기록). `PushNotifier`가 `Room room`(nullable) 파라미터를 새로 받아 5개 호출부(`PokeService`/`TodoService`/`RoomService`/`ScheduleReminderService`/`ArchiveAnalysisNotifier`)가 갱신됨(`ArchiveAnalysisNotifier`만 room=null — 유저 단위로 여러 방 결과를 합칠 수 있어 단일 room이 없음). `domain/notification/`에 `Notification` 엔티티·`NotificationRepository`·`NotificationHistoryService`·`NotificationHistoryController`(`GET /me/notifications`·`GET /me/notifications/unread-count`·`POST /me/notifications/read-all`) 신규. 프론트 `NotificationHistoryScreen`(`/mypage/notification-history`, `PastRoomsScreen`과 같은 마이페이지 톤)이 목록·읽음 처리·탭 시 이동(기존 `notification_router.dart`의 `handleNotificationData` 그대로 재사용)을 담당하고, 홈 벨에 안읽음 개수 배지(9개 초과 시 "9+") 추가. 상세는 `specs/0017-알림-내역.md`. 부수적으로 `PokeServiceTest`의 stale 테스트(2026-08-08 게이트 변경 전 "전체 꺼지면 개별 무시" 동작을 검증하던 것) 하나를 현재 동작(개별 flag만으로 판단)에 맞게 수정.
- [x] **알림 문구 최종 카피 교체 + 푸시 딥링크 `data` 페이로드 + `knock_enabled` 잔재 정리** (2026-08-09, `docs/backend/notification-handoff.md`·`notification-deeplink-handoff.md`) — 위 알림 내역 작업이 이미 건드려둔 5개 호출부(`PokeService`/`TodoService.notifyNewAssignees`/`RoomService` 입장·퇴장/`ScheduleReminderService`/`ArchiveAnalysisNotifier`)를 다시 손대 문구를 최종 카피로 교체(이모지 포함, 예: 콕찌르기 `"{보낸사람}님이 콕! 👀"`, 담당 투두 `"새 투두가 도착했어요 📮"`). **일정 알림 본문 재구성**: 제목이 타입별 고정 카피(D-day/D-1)로 바뀌면서 일정 제목이 본문 1줄(`{제목} · {방}`)로 이동, 2번째 줄에 시간·장소 중 있는 것만 ` · `로 이어 붙이고(`ScheduleReminderService.body`/`formatTime` 신규) 둘 다 없으면 생략. `PushSender.send(...)`에 `Map<String,String> data` 인자 추가(오버로드 없이 시그니처 자체 변경, 구현체 2곳뿐), `FirebaseCloudMessagingPushSender`가 `.putAllData(data)`, `PushNotifier`가 `type`(항상)+`roomId`(방 컨텍스트 있을 때만)를 자동으로 채워 넣는다 — `scheduleId`/`todoId`/`archiveItemId`는 두 문서 모두 "프론트 미소비"라 명시해 스코프 제외. **`knock_enabled` 컬럼·DTO 필드·`PokeType.KNOCK`·`pokes.type` CHECK 제약을 `V27__drop_knock_enabled.sql`로 완전히 제거** — 조사 과정에서 원래 문서가 우려한 위험(요청 DTO `@NotNull`이라 옛 앱이 400)은 Spring 기본 Jackson 설정(초과 필드 무시)상 실제로는 문제없음을 확인했고, **진짜 위험은 반대쪽**이었다: 앱 `NotificationSettings.fromJson`이 `json['knockEnabled'] as bool`로 non-null 캐스팅하고 있어 응답에서 필드를 빼면 구버전 앱이 설정 화면에서 크래시났다. 그래서 앱 모델의 `knockEnabled`도 같은 커밋에서 제거해 서버·앱 배포 순서 문제 자체를 없앴다(단, 서버·앱스토어 배포 사이에 낀 기존 구버전은 여전히 영향받으므로 앱 심사 통과 후 서버 배포 권장). 서버 테스트 750건·Flutter 테스트 600건 전체 통과로 검증.

### Phase 4 — 출시
- [x] **서버 배포(CD) — 완료** *(2026-08-14 확인: GitHub Actions `deploy` 잡 + blue/green 무중단 배포가 배선돼 있고 `BlueGreenDeployContractTest` 가 설정 정합성을 지킨다. 배포 중 1195요청 5xx 0건 실측. 이 항목이 「미완」으로 남아 있던 것은 문서 지연이었다.)* 이전 기록: **서버는 돌고 있고 자동 배포 배선이 미완** (2026-07-29 착수 / 2026-08-13 이관): 아래 두 단락은 **옛 EC2·Jenkins 시절 기록**이고, 지금 사실은 그 아래 "🔴 2026-08-13 갱신"이다. 당시: `maramodi.cloud` 도메인 확보(가비아), `api.`/`jenkins.`/`storage.` A레코드 → 당시 EC2 공인 IP. `deploy/`에 Caddy(자동 TLS) + compose 2층(인프라/앱) + `deploy.sh`(빌드→교체→헬스체크→`:previous` 롤백 태깅) 작성, `Jenkinsfile`에 `dev` 머지 시 배포 스테이지 추가.
  **로컬 검증 완료(증거 있음)**: 앱 층 5개 컨테이너(spring·ai·minio·postgres·redis)를 실제 compose로 띄워 전부 `healthy` 확인 — Flyway 마이그레이션 통과, `Started ServerApplication`, `/v1/health` 200, MinIO 버킷 자동 생성 + 접두사 정책 실측(`profile/`→404 통과 / 그 외→403 차단), spring→ai 내부 호출 성공, 호스트 포트 바인딩 0개, spring·ai 둘 다 비루트. `caddy validate` → `Valid configuration`. **이 과정에서 배포를 막을 결함 2개를 잡았다**: ① `minio.endpoint` 기본값 탓에 MinIO 없으면 서버가 부팅되지 않던 문제(좀비 컨테이너) ② `ACME_EMAIL`이 비면 Caddy가 아예 기동하지 않던 문제 — 둘 다 수정+재검증했다(`specs/OPEN.md`).
  **🔴 2026-08-13 갱신 — 위 서술은 EC2·Jenkins 시절 기록이다.** 원 프로젝트가 끝나 EC2·GitLab·Jenkins·LLM 게이트웨이가 전부 회수됐고, 다음으로 옮겼다:
  - 인프라: **오라클 클라우드 Ampere A1**(ARM aarch64, 4 OCPU/24GB, ap-tokyo-1). 앱 층 5개 컨테이너가 실제로 떠서 돌고 있다 — `/actuator/health/deploy` UP, Flyway 28, 데이터 복원 검증 완료(users 19 · todos 87 · archive_items 90, MinIO 버킷 파일목록 `diff` 일치).
  - CI/CD: **GitHub Actions**(`.github/workflows/ci.yml`). Jenkins·Shorebird 자동화는 제거했다.
  - AI: **OpenAI 직접 호출** + 세 기능 모델 `gpt-5.4-nano` 통일(`ai/docs/EXPERIMENTS.md` #34).
  - **남은 것**: ① GitHub Actions 배선(Secrets 3개 + 서버 read-only deploy key) — **이 파이프라인은 아직 한 번도 돌지 않았다.** ② `dev` 브랜치 보호(필수 체크 `gate`) ③ **DNS 전환** — 가비아 계정 확보 후 `api`·`storage` A레코드를 새 서버로, `jenkins` 레코드는 삭제. 전환 직전 옛 서버에서 마지막 증분 백업을 뜨고, `MINIO_ENDPOINT` 를 `https://storage.maramodi.cloud` 로 되돌린다. 절차는 `README.md` "배포" 절.
- [x] **운영 데이터 백업 — 완료** *(2026-08-14)*: `deploy/backup.sh`(PostgreSQL 덤프 + MinIO 볼륨) + `deploy/restore.sh` + systemd 타이머(매일 04:00 KST, daily 14세대 / weekly 8세대). 그전까지 **백업이 아예 없었다** — 크론탭·systemd 타이머·덤프 파일 전부 확인해 0건이었고, 디스크 한 장이 날아가면 복구 수단이 없는 상태였다. **검증**: 임시 컨테이너에 실제로 복구해 20개 테이블 행 수가 운영과 전부 일치(users 20 · rooms 32 · todos 87 · archive_items 90), 세대 정리·하드링크 동작 확인. 절차는 `README.md` "배포 → 백업·복구". **남은 것**: 백업이 원본과 같은 디스크에 있다 — 서버 밖(오라클 오브젝트 스토리지) 전송은 OCI API 키 발급이 선행돼야 한다.
- [x] **소개·지원·약관 웹페이지 — 완료** *(2026-08-14)*: `https://maramodi.cloud/`(지원 URL) · `/privacy`(개인정보처리방침 URL) · `/terms`. **App Store 는 이 두 URL 이 없으면 제출 버튼 자체를 막는다.** 약관 전문은 앱 안에 있었지만(`legal_content.dart`) 웹 주소가 없었고, DNS 는 서버를 가리키는데 Caddyfile 블록이 없어 **인증서가 발급된 적조차 없었다**(https 연결 실패 상태). 구현: `deploy/site/*.html` + `deploy/Caddyfile` 블록 + `docker-compose.infra.yml` 마운트. **검증**: 세 주소 200 · http→https 308 · Let's Encrypt 인증서 4장 정상 · 인프라 재생성 후 기존 사이트 전부 기대값 유지(`/rooms` 401 · `/actuator/health` 404 · `/docs` 401 · `storage/` 403). **드리프트 방지**: `app/test/features/legal/legal_web_sync_test.dart` 가 앱 본문과 웹 HTML 을 절 단위로 대조해 한쪽만 고치면 CI 를 실패시킨다.
- [ ] 앱 아이콘/스토어 메타데이터 (스플래시 UI는 구현 완료) — ⚠️ **App Store 표시 이름이 `modi-modi` 다**(2026-08-14 확인). 홈 화면 아이콘 이름은 별개이고 이미 `MODI`(`Info.plist`). 제출 전까지만 바꿀 수 있다.
- [ ] 빌드/서명/제출 — **iOS 전용** · **2026-08-14: TestFlight 업로드까지 완료**. `1.0.0 (1)` 을 Xcode Organizer 로 올려 Apple 검증(Validate App)·처리를 통과했고 "제출 준비 완료"가 됐다. Swift 테스트 13건도 이날 처음 실행돼 전부 통과. 앱 레코드(`modi-modi`)·개인정보처리방침/지원 URL·기본 카테고리·개인정보 수집 설문·앱 설명/키워드·수출 규정도 해소됐다.
  - **카카오 해결**(2026-08-14): 옛 팀원이 기존 카카오 앱(ID `1544003`)에 **Editor 로 초대**해 줘서 새 앱 생성이 불필요해졌다. 콘솔에서 번들 ID 등록 + **사용 설정 ON**(아예 꺼져 있었다) + 동의항목 3종을 맞췄다. 🔴 **그런데 그것만으로는 안 됐다** — 빌드의 `KAKAO_NATIVE_APP_KEY` 가 옛 프로젝트 PC 에서 복사돼 온 **다른 카카오 앱의 키**였다. 키 자체는 유효해 카카오톡이 열리고 돌아온 뒤에야 실패하며, SDK 단계에서 끊겨 **서버 로그에 아무것도 안 남는** 탓에 원인 추적이 오래 걸렸다. 키 교체 후 `1.0.0 (2)` 재빌드(IPA 내부 `Info.plist` 실측으로 `kakao37f2…` 반영 확인). 재발 방지는 `README.md` "Kakao 로그인 개발 설정".
  - **수출 규정 재발 방지**: `ITSAppUsesNonExemptEncryption=false` 를 `Info.plist` 에 박았다 — 없으면 업로드마다 묻고, **자동화하면 그 대화상자에서 파이프라인이 멈춘다.**
  - **남은 것**: EU 거래자 자격 신고 · 연령 등급 · App Store 표시 이름(`modi-modi` 그대로다) · 스크린샷 · 실기기 확인 3건 · 심사자용 데모 방 · `1.0.0 (2)` 업로드 — 상세는 `docs/ios-submission-handoff.md`.(2026-08-13 확정, 안드로이드 릴리스 경로 없음). Apple Developer 계정 구매 → 번들 ID 확인 → `docs/ios-release.md` 절차. 크래시·분석(Sentry)은 미착수
- [ ] 앱 release용 `app/env/prod.json` 생성 (`API_BASE_URL=https://api.maramodi.cloud`) — 서버 배포가 실제로 뜬 뒤에

### Phase 5 — 나중 확장 (별도)
- [ ] 위젯(home_widget), Live Activity(live_activities), 배경화면 자동화(Android 직접 / iOS 단축어)

---

## 5. 핵심 리스크

1. **인증 하이브리드 복잡도** — Firebase↔Spring Boot ID 토큰 검증. → Phase 0 스파이크로 선검증.
2. **전체 범위 = 큰 스코프(1인)** — 냉정한 Phase 순서·작은 PR·후순위 표시로 관리.
3. **Dart의 AI 지원** — 촘촘한 스펙·하네스로 상쇄.
4. **AI 기능 비용/인젝션** — 서버 경유, 입력 검증, 캐싱/중복 방지.
5. **권한 모델(방장 없음, 누구나 삭제 가능)** — 서버 인가 설계에서 명시적으로 반영, 파괴적 액션 확인 모달.

---

## 6. 지금 생성된 산출물

**스펙 (`specs/`)**
- `full_spec.md` — 전체 IA 원본(2026-07-27 추가) — **최상위 진실**, 수정 금지
- `design.md` — 디자인 시스템 ✓ — **디자인에 관한 유일한 진실** (v4: Airbnb 철학 적용 + `design_full_spec.md` S-00·`DESIGN-airbnb.md` 통합·삭제, 2026-07-27)
- `0001-architecture.md` — 아키텍처·인증(Redis 초대코드 포함) ✓
- `0002-data-model.md` — 관계형 스키마(ERD) ✓
- `0003-navigation.md` — 라우트 트리·전이표 ✓
- `0004-방-생성-참여.md` — 방 생성/참여, 온보딩 게이팅 ✓
- `0005-홈-대시보드.md` — 홈 대시보드(S-04) ✓ (2026-07-27 패치: 아바타 정렬·오늘투두 로직)
- `0006-투두-탭.md` — 투두 탭(S-15~18) ✓ (2026-07-27 패치: todo/done·필터·용어)
- `0007-온보딩.md` — 온보딩 S-01/S-02/S-02-A ✓ (2026-07-27 신규, 미구현)
- `0008-방-전환.md` — 방 전환 바텀시트 S-07 ✓ (2026-07-27 신규, 구현 완료)
- `0009-일정-탭.md` — 일정 탭 S-20 ✓ (2026-07-27 신규, 구현 완료)
- `0010-아카이브-탭.md` — 아카이브 탭 S-25 ✓ (2026-07-31, 폴더 CRUD + S-25-A 항목 목록 + S-25-B 항목 상세 + S-25-C 자료 등록/AI 태깅 + S-25-D 외부 공유 등록(Android·iOS 네이티브 코드) 구현 — iOS 실기기 E2E는 출시 검증)
- `0011-멤버-투두-콕찌르기.md` — 멤버 투두 S-30-M + 콕찌르기/재촉 ✓ (2026-07-27 신규, 미구현)
- `0012-설정.md` — 설정 페이지 S-40 ✓ (2026-08-03 앱·서버 흐름 구현 완료)
- `0013-종료된-방.md` — 종료된 방 요약 S-05 + 재시작 S-12 ✓ (2026-07-27 신규, 후순위·미구현)
- `0014-외부-공유-등록.md` — 외부 공유 등록 S-25-D(네이티브 공유 익스텐션) ✓ (2026-07-31, Android 구현+에뮬레이터 검증 및 iOS Share Extension 코드·Simulator 빌드/XCTest 완료, 실기기 E2E는 출시 검증)
- `0015-알림-트리거.md` — 알림 트리거 6종(콕찌르기/일정 전날·디데이/방 입장·퇴장/내 담당 투두 추가) 실제 발송 시점 ✓ (2026-08-05 신규, 백엔드 구현 완료 — `0012`가 유보해뒀던 부분)
- `0016-협업-캐릭터.md` — 협업 캐릭터(마이 탭) ✓ (2026-08-07 신규, 서버 구현 완료 — 프론트 UI는 후속 스코프)
- `0017-알림-내역.md` — 알림 내역(S-41 QA) ✓ (2026-08-09 신규, 서버+프론트 구현 완료)
- `OPEN.md` — 미확정 결정 트래커(방 자동 정리 배치, 약관, iOS 공유 익스텐션 등) — Kakao Firebase 연동 방식은 에서 확정

**서버 (`server/`)** — Spring Boot 3.5.x, PostgreSQL(Flyway `V1__init.sql`+`V2__drop_invite_codes.sql`+`V3__archive_crawl_status.sql`+`V4__add_fcm_token_to_users.sql`+`V5__add_summary_to_archive_items.sql`) + Redis(초대코드 전용) + MinIO(S3 호환, 이미지 업로드 전용). 패키지는 `domain/{도메인}/{controller,service,client,dto,entity,repository}` + `global/{config,security,notification,storage}` 구조(2026-07-29 재구조화)
- `global/security/` — Firebase ID 토큰 검증 필터
- `global/notification/` — `PushSender`(인터페이스) + `FirebaseCloudMessagingPushSender`, `FirebaseConfig`가 `firebase.credentials-path` 있을 때만 조건부 빈 생성(`AiTaggingClient`와 동일 패턴). **2026-08-05**: `PushNotifier`(+`PushType` enum)가 "설정 확인(`all_enabled && 개별플래그`) → 토큰 확인 → 발송 → 실패 삼킴" 게이팅을 공용화 — `PokeService`가 처음 만든 로직을 추출해 방/투두/일정 트리거까지 전부 이걸 쓴다. `global/config/SchedulingConfig`(`@EnableScheduling`, 이 프로젝트 최초의 배치 도입)도 함께 추가.
- `global/storage/` — `ObjectStorage`(인터페이스) + `MinioObjectStorage`. presigned PUT 업로드 URL 발급 + `profile/` 접두사만 공개 읽기(버킷 전체 아님, 리뷰 반영). `global/config/MinioConfig.java`가 `minio.endpoint` 있을 때만 조건부 빈 생성(부팅 시 버킷 자동 생성 + 공개 읽기 정책 적용까지 처리) — 없으면 업로드 API는 503
- `domain/auth/` — 카카오 로그인 교환(`POST /auth/kakao`) + **이메일 자체가입 인증코드(2026-07-31)**: `POST /auth/email/code`(발송, 409=이미 가입)·`POST /auth/email/code/verify`(검증, 400=불일치, 410=만료/5회 오답 무효화). `EmailVerificationService`가 Redis 단독 저장(초대코드와 동일 패턴, 이메일은 소문자 정규화). 이미 가입 여부는 로컬에 이메일 컬럼이 없어 `FirebaseEmailAvailabilityChecker`가 Firebase Admin SDK `getUserByEmail`로 확인(이 프로젝트 첫 사용례). 발송은 `EmailConfig`가 `spring.mail.host` 있을 때만 조건부로 만드는 SMTP `EmailSender` — 없으면 발송 API만 503.
- `domain/room/` — 방 생성/초대코드 미리보기·참여/내 방 목록, `InviteCodeService`(Redis). `GET /rooms/{roomId}/members/progress`(멤버별 진행률 조회) — 완료/담당 집계를 진행률 내림차순으로 반환, `MemberProgress` DTO는 `domain/room/dto/`가 정본. **4-3 방 자동 종료(2026-07-30)**: `RoomService.refreshStatus(Room)` — 배치 없이 요청 시점 lazy 체크, 초대 미리보기·참여·내 방 목록·지난 방 목록이 전부 이 메서드를 거친다. **2026-08-05**: 방 입장(신규 참여일 때만, 참여자 본인 제외)·퇴장(남은 멤버, 본인 제외) 시 `PushNotifier`로 다른 멤버에게 알림 발송 추가
- `domain/dashboard/` — 홈 대시보드 응답 조립. 멤버 진행률 부분은 `RoomService.listMemberProgress`를 그대로 재사용(중복 제거). 홈 응답의 방 상태도 `RoomService.refreshStatus`를 거쳐 4-3 자동 종료가 반영된다(2026-07-30). **2026-08-06**: `activities[]`(홈 활동 피드) 추가 — `ActivityService.getRecentActivities`가 채운다.
- `domain/activity/` — 홈 활동 피드(2026-08-06 신규, `docs/backend/home-activity-feed.md`). `Activity`/`ActivityType`(entity) + `ActivityRepository` + `ActivityService`(적재형 9종 `record` + 파생형 4종 포함 `getRecentActivities`). `TodoService`·`ScheduleService`·`ArchiveItemService`·`PokeService`·`RoomService`가 각 액션 시점에 기록을 호출한다. 기록 실패는 예외를 삼키고 로그만 남긴다(핵심 동작을 막지 않음).
- `domain/category/`, `domain/todo/` — 카테고리·투두 CRUD, 담당자 다대다. `GET /rooms/{roomId}/members/{userId}/todos`(S-30-M 멤버 투두 조회) — 조회 전용, 체크 토글 없음. **2026-08-05**: 투두 생성/수정 시 신규로 배정된 담당자(요청자 본인 제외)에게 `PushNotifier`로 알림 — 수정은 담당자를 전부 지우고 재생성하는 구조라, 재배정 직전에 기존 담당자를 스냅샷해 "신규만" 걸러낸다. **2026-08-06**: `V16`이 날짜·위치·중요 표시·사진 URL과 `todo_tags`를 추가하고, 새 클라이언트의 중첩 `reminder` 객체가 이를 명시적으로 갱신한다. 레거시 클라이언트가 이 객체 없이 수정하면 기존 메타데이터는 보존한다.
- `domain/schedule/` — 일정 CRUD(월 범위 조회, 시간 미지정 우선 정렬). **2026-08-05**: `ScheduleReminderService`(`@Scheduled`, 매일 08:00 KST)가 시작일이 오늘/내일인 일정의 방 멤버 전원에게 디데이/전날 알림 발송 — 방 활성 여부는 `rooms.status`가 아니라 `rooms.end_date`로 직접 판정(status는 lazy 갱신이라 아무도 안 연 종료된 방은 여전히 ACTIVE일 수 있음), Redis로 같은 날 중복 발송 방지
- `domain/poke/` — 콕찌르기 발송 `POST /rooms/{roomId}/members/{userId}/pokes`(S-30-M). POKE/KNOCK은 같은 기능으로 통일(항상 POKE, `specs/OPEN.md` 확정), `notification_settings.all_enabled`+`poke_enabled` 둘 다로 발송 게이팅(행 없으면 기본 허용, 로 all_enabled 반영), `users.fcm_token` 있고 `PushSender` 빈이 있을 때만 실제 발송 — 실패해도 `pokes` 행 저장은 항상 성공
- `domain/notification/` — `GET`/`PUT /me/notification-settings`(전체/개별 알림 설정). GET은 행 없으면 기본값(전부 true) 계산 반환(행 생성 안 함), PUT은 upsert. **2026-08-05**: `V13`의 8개 채널 전부(콕찌르기·일정 전날·일정 디데이·방 입장·방 퇴장·내 담당 투두 추가 + 미사용 재촉)가 이제 실제 트리거에서 읽힌다(`specs/0015-알림-트리거.md`). **2026-08-09 알림 내역 추가**(S-41, `specs/0017`): `Notification` 엔티티(`V24`)·`NotificationRepository`·`NotificationHistoryService`·`NotificationHistoryController`(`GET /me/notifications`·`GET /me/notifications/unread-count`·`POST /me/notifications/read-all`)·`NotificationRetentionScheduler`(90일 보존). 기록은 `PushNotifier.notify/notifyEach` 한 곳에서 발송 트리거 7종 전부를 커버.
- `domain/user/` — `PUT /me/fcm-token`으로 FCM 토큰 upsert 등록. `GET`/`PATCH /me/profile`(닉네임/사진 URL, upsert, 없으면 GET 404) + `POST /me/profile/photo/upload-url`(MinIO presigned PUT 발급, `ObjectStorage` 없으면 503). **2026-08-07**: 마이 탭 프로필 헤더용 `createdAt`/`loginProvider` 필드 추가(`V22`) — 카카오는 진입점에서 고정, 구글/이메일은 `FirebaseAuthFilter`가 새로 노출한 `sign_in_provider` 클레임으로 매핑, 최초 가입 시점에만 기록.
- `domain/archive/` — 아카이브 폴더 CRUD + 폴더 내 항목 목록 조회 + 항목 상세(`GET .../archive/items/{itemId}`)·핀/좋아요 토글(PATCH)·폴더 이동(PATCH)·태그 전체교체(PATCH)·삭제(DELETE) + 자료 등록(`POST .../archive/folders/{folderId}/items`, `JsoupUrlCrawler`로 크롤링 + `OpenAiTaggingClient`로 AI 태깅, `global/config/OpenAiConfig.java`가 `FirebaseConfig`와 동일 패턴으로 키 없으면 AI 클라이언트 빈 자체를 생성하지 않음) + 외부 공유 등록(`POST .../archive/folders/{folderId}/items/pending`, S-25-D 전용 신규 엔드포인트 — **URL·텍스트 둘 다** `ArchiveItem.CrawlStatus.PENDING`으로 즉시 저장 후 `ArchiveCrawlProcessor`가 `@Async`로 처리한다. 2026-08-05 이전에는 텍스트만 동기 `DONE`이었는데, 크롤링이 없어도 태깅·요약·임베딩(LLM 3회)이 5초 클라이언트 타임아웃을 넘겨 "서버는 성공, 앱은 실패"를 냈다. 프로세서가 `url == null`이면 크롤링만 건너뛴다). **AI 요약**: `AiSummaryClient`/`OpenAiSummaryClient`가 태깅과 **별도 호출**로 본문을 요약해 `archive_items.summary`에 저장한다 — 태깅 프롬프트를 건드리지 않기 위해 분리했고(JSON 파싱이 깨지면 태그까지 날아간다) 모델도 다르다(요약 `gpt-5.4-nano` / 태깅 `gpt-5-nano`, 근거는 `ai/docs/EXPERIMENTS.md` #14). 요약 실패는 태깅 실패와 같은 폴백 — 요약 없이 등록을 진행한다
- `domain/character/` — 협업 캐릭터(2026-08-07 신규). `UserActivity`/`UserActivityKind`(entity, `V21`) + `UserActivityRepository` + `UserActivityRecorder`(조회 로그 기록, `REQUIRES_NEW` 트랜잭션 — 읽기 전용 호출자 트랜잭션에 얹으면 rollback-only로 오염돼 반드시 별도 트랜잭션이어야 함) + `UserActivityRetentionScheduler`(매일 04:00 KST, 90일 지난 행 삭제) + `CharacterService`(완료 타이밍·완료율·스트릭·좋아요 준 횟수로 10종 캐릭터 결정 트리 판정, 진화 경로 포함) + `CharacterController`(`GET /me/character`, `GET /rooms/{roomId}/members/{userId}/character`, `POST /me/activity/app-open`). `DashboardService`·`ArchiveItemService`·`TodoService`의 기존 조회 메서드가 각각 `ROOM_VIEW`/`ARCHIVE_ITEM_VIEW`/`TODO_VIEW` 로그를 자동 기록(프론트 변경 없음). 프론트는 이번 스코프 밖(문서가 "프론트는 UI만 담당"이라 명시) — 마이 탭 화면은 후속 작업.
- `docs/api/openapi.json` — springdoc-openapi 자동 생성(테스트 실행 시 재생성, CI 가 드리프트 검사)

**AI 서버 (`ai/`)** — FastAPI + uv(Python 3.12), 투두 추천(S-16-B) 전용. 외부에 포트를 열지 않고 Spring만 호출하며 도메인 DB를 직접 보지 않는다
- `src/modi_ai/` — `suggest.py`(후보 생성 파이프라인, 순수함수와 LLM 호출 분리) · `prompts.py`(프롬프트 원본, git이 진실) · `schemas.py` · `security.py`(`X-Internal-Key`) · `config.py` · `tracing.py`(LangSmith 배선 — pydantic-settings가 `.env`를 `os.environ`에 넣지 않아 별도 배선이 필요했다, 운영 기본값 꺼짐)
- `POST /v1/todo-suggestions` — Spring의 `TodoSuggestionService`만 호출한다. `langchain-core` + `langchain-openai`로 게이트웨이 경유 구조화 출력, 파이프라인은 `langgraph` `StateGraph`(2026-08-01 추가 — 기능 이득 0, 팀 역량 판단). 풀 `langchain`은 필요 없는 것까지 끌고 와 여전히 금지
- `evals/` — **평가 하네스(런타임 아님, 실제 LLM을 호출해 크레딧을 쓴다)**. 자료 15건(`data/archives/`, 실제 인스타/블로그 캡션 — **정답 라벨 없음**) + 러너(`run_suggest_eval.py`, 후보 전문·토큰·응답시간·제목 길이 분포) + 동결 스냅샷(`data/snapshots/`, 요약 도입 **전/후** 기준선 한 쌍). **자동 채점층은 없다** — 프로토타입(Qwen)에서 이식했다가 지표가 전부 무효로 판명돼 2026-07-30에 전면 삭제했다(`ai/docs/EXPERIMENTS.md` "다시 하지 말 것"). `pytest`의 `testpaths` 밖이라 CI에서 돌지 않는다. **2026-08-01**: 러너 입력을 운영과 같은 **요약**으로 바꿨다(`summarize_fixtures.py`가 Spring 프롬프트로 만든 요약을 `data/summaries/`에 동결·커밋, 프롬프트 드리프트는 `tests/test_summary_prompt_drift.py`가 잡는다). 조건 A/B 플래그(`--input`/`--categories`/`--no-categories`/`--no-schema-hint`)와 결과를 커밋 파일로 얼리는 `freeze_snapshot.py`를 추가했고, **문서가 인용하는 수치는 `tests/test_snapshot_2026_08_01.py`가 크레딧 0으로 단언한다** — 이게 없어 초안에 커밋 파일 어디에도 없는 값이 들어갔고 리뷰에서 잡혔다. 순수 로직은 `tests/`가 크레딧 0으로 잰다. **2026-08-03: 러너가 둘이 됐다** — `run_room_eval.py`는 **방 하나를 통째로** 넣고(캡처 프록시로 받아 적은 실제 운영 요청을 `data/rooms/`에 동결) **운영 `suggest()` 그래프 전부**를 돌리며, `--rounds`로 추천 버튼 연속 클릭을, `--repeats`로 반복 평균·표준편차를 낸다. 자료 1건씩 넣는 기존 러너로는 "여러 자료가 섞일 때 나오는 문제"와 "제외 목록이 쌓여야 나오는 마름"을 **재현조차 못 했다**. 여기서도 **자동 채점은 없다** — 기준선(`docs/EXPERIMENTS.md` #26)은 여행 방 후보의 85%가 "확인·계획" 동사이고 학습 방은 61%라는 대비를 보여주지만, 그 경계를 코드로 박기 전에 눈으로 분류하는 단계를 먼저 둔다 **2026-08-03: 후보 자기 검수(critique) 노드 추가** — 생성 뒤 LLM 을 한 번 더 불러 `동선 짜기`·`코스로 묶기`·`10곳 중 1곳 고르기` 같은 후보를 반려한다. 부산 여행 방의 메타 행위 비율 **40% → 0~6%**(두 실행 재현, `docs/EXPERIMENTS.md` #28). 대가는 후보가 절반 이하로 주는 것과 **13%의 라운드가 7초를 넘는 것**이고 사용자가 둘 다 알고 받았다. `critique → generate` 되돌아가는 엣지도 만들었으나 **재보고 껐다** — 재생성이 더 낫다는 근거가 안 나왔다(`critique_max_attempts=1`). ⚠️ **LangGraph 덕분에 좋아진 것이 아니다** — 품질을 올린 것은 검수라는 아이디어이고, 검수 노드도 조건부 종료도 직선 코드로 된다(#28 ④-1). LangGraph Studio 를 개발 도구로 붙였다(`PYTHONUTF8=1 uv run --group studio langgraph dev`). 이 티켓에서 **지표가 한 번 무너졌다** — 좋은 동사를 열거하는 방식이 조건이 바뀌자 무효가 됐고, 사용자가 지목한 나쁜 모양을 세는 쪽으로 바꿔서야 재현됐다. **2026-08-03: 부분 반려분 재생성 — 만들어서 재고 기각했다.** 235의 되돌아가는 엣지가 "전부 반려"에만 걸려 운영에서 안 타길래 **부분 반려("3개 통과·4개 반려")에도 타게** 만들었다. 목적이던 후보 회복은 됐지만(부산 41·42 → 109·88개) **지연이 상한 10초를 37%·40% 깼다**(기준선 0%·3%, 최악 15.46초 — 라운드당 LLM이 2회→4회). 게다가 **방마다 방향이 반대였다**: 회화 스터디는 메타 행위가 줄었는데(6.6~7.7% → 3.0~4.3%) **부산은 늘었다**(0~5.9% → 7.6~12.7%) — 하필 사용자가 불만을 제기한 방이다. 코드는 스위치 OFF(`regenerate_on_partial_reject=False`)로 남겼다(`ai/docs/EXPERIMENTS.md` #29). 🔴 **곁가지로 235의 결함을 발견했다**: 검수가 판정을 하나 빠뜨린 응답을 받으면 fail-open으로 후보 전부를 통과시키는데 그게 **부산 방에서 반복해서 나왔다**(콘솔 기준 15라운드 중 6·4회 — ⚠️ 라운드 레코드에 필드가 없어 커밋된 근거는 없었다. 러너 계수기를 이 티켓에서 붙였다). **2026-08-03 해결**: 원인은 후보를 **`0.` 부터 번호 매긴 것**이었다. 검수만 떼어 60회를 재보니 첫 번호가 `0` 이면 30회 중 **10회(33%)**가 판정을 하나 빠뜨렸고 `1` 이면 **30/30**이 맞았다(`ai/evals/probe_critique.py`, `ai/docs/EXPERIMENTS.md` #30). 고친 것은 `suggest.CRITIQUE_INDEX_BASE = 1` 한 줄이고 **프롬프트·스키마 설명은 안 건드렸다**(측정이 그것들을 그대로 둔 채 얻은 값이라 함께 고치면 재지 않은 조건을 배포하게 된다). **개수가 아니라 짝이 문제였다** — 반려 사유를 후보와 대조하면 `0`에서 밀린 판정이 **12건**, `1`에서는 **0건**이다(개수가 우연히 맞았다면 fail-open이 안 걸리고 엉뚱한 후보가 반려됐을 것이다). ⚠️ **단 "꼬리가 빠진다"고 쓰면 안 된다**(2026-08-03 리뷰에서 잡힌 오독) — 빠진 **번호 라벨**은 응답이 `0..n-2`로 연속이라 산수로 따라오는 것이고 후보와 다른 양이다. 밀림 시작점이 묶음마다 달라(`opic_8` `index=6`, `busan_8c` `index=4`) 어느 후보가 건너뛰어졌는지는 확정할 수 없고, `OPEN.md`의 가설("0번을 빼고 답한다")도 기각·확증 둘 다 못 한다. 방 평가 2회에서 fail-open **0/30·0/30**, 후보 수 유지(41·38 / 58·64), 상한 초과 1/30(기준선과 같음). ⚠️ **품질이 좋아졌다고는 말할 수 없다** — 방마다 기준선보다 나쁜 실행이 하나씩 있어 "구분되지 않는다"까지다. ⚠️ #28의 대표 수치(`40% → 0~6%`)는 여전히 "검수가 매번 걸렸다"는 전제 위에 있고 그 스냅샷에는 계수기가 없어 사후 확인이 안 된다(오염 가능성은 낮다 — 부산 방 후보 수에 8이 하나도 없다).
- **2026-08-04 AI 카테고리 배정을 통째로 걷어냈다.** 추천 후보가 카테고리 없이 나가고(= 기타/미분류) 사용자가 투두 탭에서 직접 분류한다 — 제품 결정. ⚠️ 오늘 있는 경로는 **채택 → 기타 → 편집 시트에서 카테고리 선택**이고, 드래그 UI 는 앱에 없다. **세 방법을 만들어보고 셋 다 실패했다**: ① 프롬프트로 시키기(#21, 새 카테고리 0개·100% 한 곳으로 몰림) ② 임베딩 코사인(실사용 10/10 실패, 게이트를 뒤집어도 중앙값 차 ±0.03 — 한 방 안의 텍스트가 전부 같은 도메인이라 코사인이 좁은 띠에 몰린다) ③ LLM 별도 호출(#31, 후보당 카테고리 종수 0.81 → **0.96 악화**. 0.97 은 후보 하나당 카테고리 하나라는 뜻이다). ⚠️ **③은 비싸서가 아니라 효과가 없어서 기각이다** — LLM 호출이 3회가 됐는데도 10초 초과 0/30이었다(#29와 사유가 다르다). 걷어낸 것: `match` 노드 · `map_categories` · `CATEGORY_MATCH_THRESHOLD` · `SYSTEM_PROMPT` 규칙 4 · `TodoCandidate.category` · `SuggestionRequest.categories` · `calibrate_category_match.py` · 테스트 61개(-1,583줄). **Spring·앱은 한 줄도 안 고쳤다** — `AiCandidate`가 Java record라 키가 없으면 null이고 앱 `_resolveCategoryId`가 빈 이름을 기타로 해석한다. 제거를 **단일 커밋으로 묶어** `git revert` 한 번이면 돌아오며, 되살리기 전 읽을 것은 `ai/docs/RESTORE-category-assignment.md`(그냥 되돌리면 실패한 상태로 돌아간다). 측정 근거 스냅샷 5개는 커밋했고 `ai/tests/test_snapshot_category_removal.py`가 수치를 단언한다.
- **2026-08-01 카테고리 편향 수정 + LangGraph 전환**: `full_spec.md` §S-16-B가 요구하는 "새 카테고리 + 투두"가 안 나오던 스펙 위반을 고쳤다. **프롬프트 문구로는 안 됐다**(판정 기준·스키마 동시 수정·구체적 예시 등 네 가지 후보안 전부 새 카테고리 0개, 후보의 100%가 기존 카테고리 하나로 몰림) → **판단을 코드로 옮겼다**: 모델은 "이 후보가 속할 분야"만 쓰고(프롬프트에서 기존 카테고리 목록을 뺐다), 기존 카테고리와 같은 분야인지는 `suggest.map_categories`가 임베딩 코사인(0.50, 오탐 0 기준으로 캘리브레이션)으로 판정한다. **앱·서버·API 계약 무변경** — `ai_suggest_sheet.dart`가 이미 `"(새 카테고리)"`로 표시하고 채택 시 앱(`_resolveCategoryId`)이 이름으로 찾아 없으면 만든다(처음부터 준비돼 있었고 AI만 못 채우고 있었다). 함께 추천 파이프라인을 **LangGraph `StateGraph`**(generate→prewarm→dedupe→match)로 옮겼다 — ⚠️ **기능 이득은 0이고 팀 역량·포트폴리오 판단이다**(`langchain-core` 채택 때와 같은 근거). 합격 기준이던 "계약 무변경"은 기존 테스트가 하나도 안 바뀌고 통과하는 것으로 확인했다. 상세는 `ai/docs/EXPERIMENTS.md` #21
- `docs/DECISIONS.md` — AI 결정 트래커(확정/미확정/폐기), `docs/EXPERIMENTS.md` — 측정 기록으로 같은 시행착오 반복 방지
- **자료 AI 요약은 Spring에 있다**(`domain/archive/client/AiSummaryClient`) — 이 서버가 아니다. 요약은 자료 등록 시점에 만들어져 `archive_items.summary`에 저장되고, 추천 요청에 `ArchiveItemInput.content`로 실려 온다(요약·본문 중 짧은 쪽, Spring이 고른다). 2026-07-30 완료
- **자료 임베딩도 Spring에 있다**(`domain/archive/client/AiEmbeddingClient`) — 요약과 같은 이유다. 등록 시점에 만들어져 `archive_items.embedding`(`real[]`, `V7`)에 저장된다. 2026-08-01 완료. **추천 요청에는 벡터째로 실려 나가고**(자료 18건에 약 390KB — 내부망 1회라 수용) AI 서버가 방 목표와의 코사인을 계산해 순위를 매긴다(2026-08-02). ⚠️ 임베딩 모델은 양쪽이 **같아야 한다**(`ARCHIVE_EMBEDDING_MODEL` ↔ `OPENAI_EMBEDDING_MODEL`, 둘 다 `text-embedding-3-small`) — 다른 모델의 벡터끼리는 코사인이 의미를 갖지 않는다
- **자료 하이브리드 선별 완료**(2026-08-02) — 자료를 네 축(좋아요 3.0 > 핀 2.5 > 유사도 2.0 > 최근성 1.0)으로 줄 세워 프롬프트에 싣는다(`ai/src/modi_ai/ranking.py`, 그래프에 `select` 노드). 동점은 같은 등수(competition ranking)라 신호가 없는 축은 조용히 빠진다. ⚠️ **확정 설계였던 가중 RRF(k=60)를 실측으로 기각했다** — 좋아요·핀은 사실상 2단계 축이고 유사도·최근성은 17단계라, RRF의 변별력이 단계 수에 비례해 **가중치 순서가 정반대로 뒤집혔다**(k=0까지 낮춰도 마찬가지). 대신 등수를 축마다 [0,1]로 펴서 가중합한다 — 변별력이 곧 가중치가 되고 RRF를 고른 원래 근거(스케일 차이)는 그대로 지켜진다(`ai/docs/EXPERIMENTS.md` #22, 사용자 확정). 라이브 검증: 자료 18건에서 좋아요 5건이 상위 5위 독점. ~~pgvector~~는 2026-08-01에 `real[]`로 뒤집혔다
- **선별의 이득은 토큰이 아니다** — 프롬프트의 80%가 고정비라 자료를 절반으로 줄여도 25%밖에 안 준다(#20 ⑥). 실제 이득은 **본문이 32~72자뿐인 URL 자료가 프롬프트 앞자리를 차지하지 않는 것**이었다(#22 ④). 예산(12,000자)은 튜닝값이 아니라 안전밸브다 — 현재 규모(18건 = 3,709자)에서는 발동하지 않는다

**앱 (`app/`)** — go_router, 디자인 토큰(`design/tokens.dart`)/테마 코드화. Riverpod는 부트스트랩만 돼 있고 화면은 아직 StatefulWidget+setState 패턴
- `features/auth/` — 구글·카카오·이메일 로그인/가입, Firebase ID 토큰 발급, 보호 API 공통 Bearer 주입과 최초 401 강제 갱신·1회 재시도(`AuthenticatedHttpClient`). Access/Refresh Token은 앱이 따로 저장하지 않고 Firebase SDK 세션을 사용한다.
- `features/room/` — S-03/S-10/S-10-A/S-11 화면, `AppSession`(온보딩 게이팅 redirect), `RoomApi`, `RoomSession`+`RoomSwitcherSheet`(S-07, "마지막 본 방" 영속화), `RoomSwitchHintOverlay`(홈 버튼 롱프레스 코치마크)
- `features/home/` — S-04 홈 대시보드
- `features/todos/` — S-15~18 투두 탭(카테고리/독립 투두, 담당자 다중 선택, 미지정 처리 시트). **2026-08-07 롤백 기준**: 생성(S-16)은 점선 원 → `TodoFormSheet` 바텀시트, 수정(S-18)은 전체화면(`todo_detail_screen.dart`, `/todos/item/:id`)에 같은 폼을 올린다. 입력 항목은 제목·상세·카테고리·마감일·담당자이며, `TodoInlineComposer`와 위치·태그·중요표시·사진은 삭제됐다.
- `features/schedule/` — S-20 일정 탭(월간 그리드, 생성/수정/삭제 시트)
- `features/archive/` — S-25 아카이브 폴더 목록(CRUD) + S-25-A 폴더 내 항목 목록(검색/정렬, `crawl_status` 배지) + S-25-B 항목 상세(좋아요/핀 낙관적 토글, 폴더 이동, 태그 일괄편집, 삭제, nullable `bodyText`/배지) + S-25-C 자료 등록 시트(`ArchiveItemRegisterSheet`, 링크/텍스트 토글 + 폴더 선택 + AI 태깅 로딩 문구) — 외부 공유 등록(S-25-D)은 앱 쪽엔 별도 등록 UI 없음(네이티브에서만 발생), 모델(`ArchiveItem`/`ArchiveItemDetail`)에 `crawlStatus` 필드만 추가
- `features/shell/`, `features/splash/` — 하단 탭 셸, 스플래시
- `android/app/src/main/kotlin/.../ShareActivity.kt` — S-25-D Android 네이티브 공유 화면(Flutter 엔진 미기동). `AlertDialog` 순차 시퀀스(방 선택→폴더 선택→제목/등록), `FirebaseAuth.getInstance().currentUser`로 네이티브 세션 직접 재사용, `HttpURLConnection`+`org.json`으로 서버 호출(코루틴/OkHttp 의존성 없음).
- `ios/ShareExtension/ShareViewController.swift` + `Shared/ShareSessionStore.swift` — S-25-D iOS 네이티브 공유 화면(Flutter 엔진 미기동). URL/텍스트 수신, 방/폴더 선택, 등록 API 호출, 메인 앱의 Firebase ID 토큰을 App Group Keychain으로 공유.
- `config/env.dart` — `--dart-define=API_BASE_URL`로 에뮬레이터/실기기 전환

**공통**
- `CLAUDE.md` — 하네스 헌법 ✓
- `.claude/agents/*`, `.claude/hooks/*`, `.claude/skills/spec/*` ✓
- `.github/workflows/ci.yml` — GitHub Actions CI/CD(app/server/ai 게이트 + openapi 드리프트 검사 + `dev` push 시 배포) ✓ *(2026-08-13, 497줄 `Jenkinsfile` 대체)*
- `docs/TEAM.md` · `docs/ios-release.md` — 팀·권한 소유 현황 / iOS 수동 릴리스 절차 ✓ *(2026-08-13 신설)*
- `deploy/` — 운영 배포 일체: `docker-compose.infra.yml`(Caddy) / `docker-compose.app.yml`(spring·ai·postgres·redis) / `Caddyfile` / `deploy.sh` / `.env.example` ✓ *(EC2 적용은 미완 — Phase 4 참고)*
- `server/Dockerfile`, `ai/Dockerfile` (+ 각 `.dockerignore`) — 운영 이미지. 둘 다 로컬 빌드·기동 검증 완료 ✓
- `.gitattributes` — `*.sh`/`Dockerfile`/`Caddyfile`을 LF로 고정(윈도우에서 커밋한 셸 스크립트가 CRLF로 나가면 리눅스 컨테이너에서 실행 자체가 안 된다) ✓
- `server/.env.example` + `build.gradle`의 `bootRun` 전용 `.env` 로더 — 로컬 필수 환경변수 3개(`FIREBASE_CREDENTIALS_PATH`/`MINIO_ENDPOINT`/`OPENAI_API_KEY`)를 한 파일로. `./gradlew test`는 읽지 않는다(키 유출 방지) ✓
- `.vscode/launch.json` — `app (debug/profile/release)` + `app (real device via USB)` 실행 구성

기능별 상세 스펙(일정·아카이브 등)은 개발하며 `/spec` 루프로 추가하고, 완료 시 이 문서(4·6절)를 함께 갱신한다(CLAUDE.md DoD 참고).
