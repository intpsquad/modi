# 협업 규칙

이 파일이 **git 협업 규칙의 단일 진실**이다. 브랜치·커밋·이슈·PR·테스트·버저닝은 전부 여기를 본다.
(코드·스펙 규칙은 `CLAUDE.md` 와 `specs/` 가 따로 있다. 완료 정의는 `CLAUDE.md`.)

---

## 1. 브랜치

### 세 계층과 **각 브랜치가 실제로 무엇을 일으키는가**

| 브랜치 | 역할 | push/머지 되면 실제로 일어나는 일 |
|---|---|---|
| `main` | **운영** | 🔴 **오라클 서버에 blue/green 무중단 배포가 돈다.** `api.maramodi.cloud` 가 바뀐다 |
| `dev` | 통합 · 테스트 | CI 가 테스트를 돌린다. **배포는 하지 않는다** |
| `feat/*` | 기능 개발 | PR 을 열면 CI 가 테스트를 돌린다 |

> 🔴 **`dev` 에 머지했다고 서버가 바뀌지 않는다** (2026-08-15 변경).
> 서버가 1대라 그전까지는 `dev` = 운영 = 데모였다. 지금은 **`main` 까지 머지해야** 서버에 반영된다.
> 자기 기능을 실제 서버에서 확인하려면 `dev` → `main` PR 이 필요하다.

**기본 브랜치는 `dev` 로 둔다.** PR 대부분이 `feat/*` → `dev` 이기 때문이다. 기본이 `main` 이면
`gh pr create` 나 웹 UI 가 매번 `main` 을 base 로 잡아, 테스트를 건너뛴 채 운영에 바로 머지될 위험이 있다.

### 브랜치 이름

```
feat/이슈번호-내용            예) feat/12-login, feat/15-todo-recommend
feat/이슈번호-hotfix-내용     예) feat/20-hotfix-null-response
```

영어 소문자 + 하이픈. **이슈 번호를 맨 앞에** 붙여 어느 이슈에서 나왔는지 바로 보이게 한다.

### 표준 흐름

```
1. 이슈를 판다
2. dev 에서 feat/<이슈번호>-<내용> 을 딴다          ← main 아님
3. 개발한다. 단위 테스트로 직접 확인한다
4. dev 로 PR 을 연다 → CI 통과 → 작성자 본인이 머지
5. 운영에 내보낼 때가 되면 dev → main PR 을 연다   ← 다 같이 확인 (§8 릴리스 절차)
6. main 머지 → 서버 배포
7. feat 브랜치는 머지와 함께 자동 삭제된다
```

### 매일 치는 명령 — 이슈 12번 "로그인 API" 예시

```bash
# ① 최신 dev 를 받는다. 항상 여기서 시작한다
git switch dev && git pull

# ② 이슈 번호로 브랜치를 판다
git switch -c feat/12-login

# ③ 작업하고 커밋한다 (타입은 소문자, §2)
git add -A && git commit -m "feat: 로그인 API 구현"

# ④ 🔴 push 전에 최신 dev 를 병합한다. 건너뛰지 말 것 (아래 사고 기록)
git fetch origin && git merge origin/dev

# ⑤ push
git push -u origin feat/12-login

# ⑥ PR 을 연다. base 는 안 적어도 dev 다 (기본 브랜치)
gh pr create --base dev

# ⑦ CI 가 초록이면 본인이 머지한다. 브랜치는 자동 삭제된다
gh pr merge --merge

# ⑧ 로컬 정리
git switch dev && git pull && git branch -d feat/12-login
```

> ⑥ 대신 **GitHub 저장소 첫 화면의 노란 배너**(`Compare & pull request`)를 눌러도 된다.
> base 가 기본 브랜치인 `dev` 로 알아서 잡힌다.
>
> ⚠️ 단 **`main` 브랜치에 뜬 배너는 누르지 않는다.** 릴리스 직후에 뜨는데, 그걸 누르면
> `main` → `dev` 방향 PR 이 열려 머지 커밋을 거꾸로 밀어 넣게 된다. 그냥 두면 사라진다.

### 핫픽스

운영에 급한 버그가 터져도 **순서를 건너뛰지 않는다.** `dev` 에서 `feat/<번호>-hotfix-xxx` 를 따고,
`dev` PR → 테스트 통과 → `main` PR → 배포. `main` 에 직접 밀어넣지 않는다.

### 🔴 push 전에 반드시 최신 `dev` 를 병합한다

```bash
git fetch origin && git merge origin/dev
```

원격 브랜치가 앞서갔는지만 확인하는 걸로는 부족하다. `dev` 보다 여러 커밋 뒤처진 채 push 하면
다른 사람이 이미 올린 변경을 놓치고, 나중에 머지할 때 충돌이 커진다.

> **2026-07-30 실제 사고**: `dev` merge 커밋을 revert 한 것이 대상 브랜치가 아니라 **`dev` 자체에**
> 병합되면서 배포 설정이 통째로 사라졌다. `dev`/`main` 에 직접 push 할 때도 fetch/pull 은 똑같이 먼저 한다.

---

## 2. 커밋 메시지

형식은 `타입: 작업 내용` — **타입은 소문자.**

| 타입 | 설명 |
|---|---|
| `feat` | 새 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 수정 |
| `refactor` | 기능 변화 없는 코드 구조 개선 |
| `test` | 테스트 코드 추가/수정 |
| `chore` | 빌드, 설정, 패키지 등 자잘한 작업 |
| `revert` | 이전 커밋 되돌리기 |
| `design` | UI/디자인 변경 |

- 타입 뒤 콜론 + 띄어쓰기 한 칸
- 제목은 짧게, 명사형 마무리 ("구현", "수정")
- **한 커밋에는 한 가지 작업만**
- 본문은 자유. "왜" 가 코드에서 안 읽히면 본문에 남긴다

```
feat: 로그인 API 구현
fix: 토큰 만료 오류 수정
design: 메인 페이지 버튼 스타일 변경
```

> 2026-08-15 이전 커밋은 `Feat(ai):` 처럼 **대문자 + 스코프**였다. 과거 커밋은 그대로 두고
> 앞으로만 소문자로 쓴다. 라벨 8개도 같은 이름으로 맞춰 뒀다.

---

## 3. 이슈

제목은 `[타입] 설명` — 예) `[feat] 로그인 API 구현`.
새 이슈를 열면 **템플릿 하나**(「이슈」)가 뜬다. 타입은 그 안의 드롭다운에서 8개 중 고른다.

라벨은 커밋 타입과 **같은 8개**를 쓴다.

⚠️ **제목의 `[타입]` 과 라벨은 직접 넣어야 한다.** GitHub 이슈 폼은 드롭다운 값으로 제목이나
라벨을 바꾸지 못한다 — `labels:` 는 폼이 그려지기 **전에** 정해지는 고정값이라, 사용자가 무엇을
고를지 알 수 없기 때문이다. 템플릿 체크리스트가 이 두 가지를 묻는 이유다.

> **왜 하나로 합쳤나** (2026-08-19)
> 원래 「기능」·「버그」·「작업」 3종이었는데, `New issue` 목록에 **「작업」만 떴다.** 앞의 둘은
> GitHub 이 조용히 거부하고 있었고 아무런 오류도 보여주지 않았다. 그래서 버그 이슈(#38·#43)가
> 폼이 아니라 손으로 쓴 마크다운으로 올라왔다 — 폼이 만든 본문은 `### 제목` 인데 그 둘은 `## 제목`
> 이었던 게 증거다. 원인을 캐는 대신 **살아 있던 「작업」 하나로 합치고 나머지를 지웠다.**
> 이 프로젝트에서 세 번째로 만난 **「조용히 죽는」 실패**다(빈 `catch`, 서명 없는 아카이브에 이어).

---

## 4. PR

| 방향 | 머지 규칙 |
|---|---|
| `feat/*` → `dev` | **작성자 본인이 알아서 머지.** CI 통과가 조건 |
| `dev` → `main` | **다 같이 모여서 확인 후 머지.** 배포가 걸려 있다 |

**PR 은 GitHub 웹에서 연다** — push 하면 저장소 첫 화면에 뜨는 노란 배너
`Compare & pull request` 를 누르면 템플릿이 채워진 폼이 열리고 base 도 `dev` 로 잡힌다.
터미널에서 시작하려면 `gh pr create --web`.

⚠️ **`gh pr create --body "..."` 로 본문을 직접 주면 템플릿이 덮인다.** 그러면 「확인한 것」이
빠진 채로 올라간다.

템플릿의 **「확인한 것」을 비워 두지 않는다** — 테스트 출력이나 스크린샷을 붙인다.
CI 가 초록인 것은 "안 깨졌다"는 뜻이지 "요구한 대로 동작한다"는 증거가 아니다.

환경변수·마이그레이션·인프라 설정을 건드렸으면 **§9** 를 함께 본다.

---

## 5. 테스트

| 시점 | 무엇을 | 왜 |
|---|---|---|
| `feat/*` → `dev` PR | CI 가 단위 테스트 자동 실행 (통과해야 머지) | 내가 짠 코드가 안 깨졌는지 |
| `dev` 머지 후 | 여러 기능이 합쳐진 상태에서 실제 의존성(DB·Redis) 붙여 실행 | 다른 코드와 붙었을 때 안 깨지는지 |

CI 는 `.github/workflows/ci.yml` 한 파일이다. 경로 필터로 바뀐 영역(`app`/`server`/`ai`)만 돌고,
`gate` 잡이 "실패한 것이 없다"를 한 줄로 모아 준다 — 필수 상태 체크는 **`gate` 하나만** 건다.

로컬에서 돌리는 법은 `CLAUDE.md` "명령" 절 참고.

---

## 6. 버저닝

**시맨틱 버저닝** `메이저.마이너.패치` — 예) `1.3.12`

| 자리 | 언제 올리나 | 예 |
|---|---|---|
| 메이저 | 기존 API 와 호환되지 않는 변경 | 함수 이름 변경·동작 변경·제거 |
| 마이너 | 호환되는 기능 추가 | 새 함수, 기존 함수에 옵션 추가 |
| 패치 | API 에 영향 없는 버그 수정·안정성 개선 | |

릴리스 노트는 GitHub Releases 에 남긴다.

> **언제 · 누가 올리는가는 §8 릴리스 절차**가 단일 진실이다. 요약하면 — 다 같이 모여서
> `dev` → `main` 을 올리기로 정한 자리에서, **`main` PR 을 열기 전에 `dev` 에 먼저** 넣는다.

### iOS 빌드 번호는 별개다

`app/pubspec.yaml` 의 `version: 1.0.0+4` 에서 **`+` 뒤가 빌드 번호**다.

> 🔴 **App Store 에 재업로드할 때마다 빌드 번호를 올린다.** 같은 번호는 거절된다.
> **거절당한 업로드도 번호를 소모한다** (2026-08-14 `-19232` 실측).

---

## 7. 기술적으로 강제되는 것

무료 플랜 + 프라이빗 저장소에서는 브랜치 보호 API 가 막혀 있어(`403`) **규칙이 규칙일 뿐이었다.**
2026-08-15 에 저장소를 공개로 전환하면서 보호를 켰다:

| 브랜치 | 직접 push | 승인 | 필수 상태 체크 | 강제 push · 삭제 | 관리자도 적용 |
|---|---|---|---|---|---|
| `main` | 금지 (PR 필수) | **1명** | `gate` | 금지 | ❌ **아니오** |
| `dev` | 금지 (PR 필수) | 0명 | `gate` | 금지 | ✅ 예 |

**`main` 만 관리자 우회를 열어둔 이유**: 승인 1명을 요구하는데 **GitHub 은 자기 PR 을 자기가
승인하지 못한다.** 관리자에게도 강제하면 혼자 일할 때 **운영 배포가 통째로 막힌다.** `dev` 는
승인이 0명이라 강제해도 잠기지 않는다 — PR 만 열면 된다.

> ⚠️ 즉 `main` 은 관리자가 마음먹으면 우회할 수 있다. **팀원 넷이 실제로 돌기 시작하면
> `main` 도 관리자 강제로 바꾼다** (`Settings → Branches → main → Do not allow bypassing`).

필수 상태 체크가 **`gate` 하나뿐인 이유**는 `.github/workflows/ci.yml` 의 `gate` 잡 주석에 있다 —
경로 필터로 스킵된 잡을 필수로 걸면 문서만 고친 PR 이 영구히 머지 대기에 걸린다.

> 🔴 **`main` 에 머지할 수 있는 사람 = 서버에서 임의 코드를 돌릴 수 있는 사람이다.**
> 배포 잡이 SSH 로 들어가 `deploy/deploy.sh` 를 그대로 실행한다. 쓰기 권한을 줄 때 이 점을 기억한다.
> 저장소가 공개인 것은 **읽기**일 뿐이라 머지 권한과 무관하고, 포크에서 온 PR 은 `deploy` 잡의
> `github.event_name == 'push'` 게이트에 걸려 배포를 태울 수 없다.

---

## 8. 릴리스 절차 (`dev` → `main`)

**다 같이 모였을 때만 한다.** "여기까지 됐으니 내보내자"를 합의하는 자리다.

### 🔴 서버와 앱은 따로 나간다

| | 언제 | 무엇이 나가나 |
|---|---|---|
| `main` 머지 | 머지하면 **자동** (10분쯤) | **서버**가 배포된다 (`api.maramodi.cloud`) |
| 앱 릴리스 | Mac 에서 **사람이 수동** | IPA → App Store → **심사 며칠** (`docs/ios-release.md`) |

`main` 에 머지해도 **사용자 손의 앱은 그대로다.** 서버만 바뀐다. 그래서 **옛 앱이 새 서버와
말이 통해야 한다** — 응답에서 필드를 빼거나 이름을 바꾸는 변경은 앱이 다 갈아탈 때까지 미룬다.

### 순서

**① 모이기 전에 확인한다**

```bash
git switch dev && git pull
gh pr list                 # 안 끝난 PR 이 없어야 한다
gh run list --branch dev --limit 1   # 마지막 CI 가 초록이어야 한다
```

**② 버전을 정한다** — `app/pubspec.yaml` 의 `version: 1.0.0+4`

| 무엇이 바뀌었나 | 어떻게 | 예 |
|---|---|---|
| 기능이 추가됐다 | 마이너 | `1.0.0` → `1.1.0` |
| 버그 수정·다듬기만 | 패치 | `1.0.0` → `1.0.1` |
| 기존 동작이 깨진다 | 메이저 | `1.0.0` → `2.0.0` |

> 🔴 **`+` 뒤 빌드 번호는 무조건 올린다** — `1.0.0+4` → `1.1.0+5`. 같은 번호는 App Store 가
> 거절하고, **거절당한 업로드도 번호를 소모한다**(2026-08-14 `-19232` 실측).

**③ 버전 올리는 PR 을 `dev` 에 먼저 넣는다** — `main` PR 을 연 뒤에 올리면 PR 을 다시 만들어야 한다

```bash
git switch -c feat/<이슈번호>-release-1-1-0
# app/pubspec.yaml 의 version 을 고친다
git commit -am "chore: 버전 1.1.0+5"
git push -u origin feat/<이슈번호>-release-1-1-0
gh pr create --base dev && gh pr merge --merge
```

**④ `dev` → `main` PR** — 여기서만 base 를 명시한다

```bash
git switch dev && git pull
gh pr create --base main --head dev --title "release: 1.1.0"
```

PR 본문에 **이번에 들어가는 기능 목록**과 **서버·DB 주의사항**(새 환경변수, Flyway 마이그레이션)을 적는다.

**⑤ 머지 → 배포 확인**

```bash
gh pr merge --merge
gh run list --branch main --limit 1          # deploy 가 success 여야 한다
curl -s -o /dev/null -w '%{http_code}\n' https://api.maramodi.cloud/rooms   # 401 이면 정상
```

> `/actuator/health` 는 Caddy 가 **일부러 404** 로 막는다(내부 전용). 살아있는지는 실제 API 로 본다.

**⑥ 태그와 릴리스 노트**

```bash
gh release create v1.1.0 --target main --title "v1.1.0" --generate-notes
```

`--generate-notes` 가 지난 릴리스 이후 PR 목록을 자동으로 채운다. 거기에 **사용자가 체감하는
변화**를 한국어로 두세 줄 얹는다.

**⑦ 앱은 여기서부터 따로** — Mac 에서 `docs/ios-release.md` 를 따라 IPA 빌드 → Transporter 업로드
→ App Store Connect 에서 심사 제출. **며칠 걸린다.**

---

## 9. 배포에 영향이 있는 변경 — 머지 전에 확인

아래 셋 중 하나라도 건드렸으면 여기를 읽는다. **전부 "배포는 성공하는데 기능만 조용히 죽는"
종류**라 나중에 발견하기가 어렵다. (PR 템플릿에 길게 적어 뒀다가 2026-08-19 에 이리로 옮겼다 —
매번 보이면 아무도 안 읽는다.)

### 새 환경변수를 추가했다

🔴 **머지 전에 서버 `.env` 를 먼저 채운다.**

`deploy/.env.example` 만 고쳐도 서버의 `/home/ubuntu/maramodi/.env` 는 이미 있어서 덮이지 않는다.
`deploy.sh` 는 필수 변수를 검사하지 않는다 → **그 기능만 죽은 채로 뜬다.**

`deploy/docker-compose.app.yml` 에 그 변수를 서비스로 통과시키는 줄이 있는지도 본다. 거기 없으면
서버 `.env` 에 넣어도 컨테이너 안으로 안 들어간다.

### Flyway 마이그레이션을 추가했다

🔴 **파괴적 변경을 한 배포에 담지 않는다** — `DROP COLUMN` · 컬럼 이름 변경 · `NOT NULL` 추가.

배포가 blue/green 이라 **새 컨테이너가 마이그레이션을 돌린 뒤에도 옛 컨테이너가 몇십 초 동안
트래픽을 받는다.** 그 동안 옛 코드가 새 스키마 위에서 돈다.

파괴적 변경은 배포 두 번으로 쪼갠다 — ① 추가만 → ② 코드 배포 → ③ 다음 배포에서 제거.

새 파일을 추가했으면 `./scripts/regen-migration-checksums.sh` 도 돌린다.

🔴 **이미 적용된 마이그레이션 파일은 고치지 않는다 — 주석 한 글자도.** 체크섬이 달라져
운영이 `Validate failed` 로 **부팅부터 실패한다.** 2026-08-13 실제 사고: 주석의 티켓 번호만
지웠는데 배포가 멈췄고, 그때 서버 테스트 774개는 전부 통과했다.
`FlywayMigrationImmutabilityTest` 가 잡지만 눈으로도 확인할 것.

### `Caddyfile` 또는 `docker-compose.infra.yml` 을 바꿨다

**배포가 반영해 주지 않는다.** 서버에서 인프라 compose 를 수동으로 다시 올려야 한다.
PR 설명에 그 사실을 적어 다음 사람이 알게 한다.
