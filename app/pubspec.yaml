name: app
description: "팀 목표 협업 앱"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+5

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # 날짜 피커 등 Material 위젯을 한국어로 표시.
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  go_router: ^17.3.0
  flutter_riverpod: ^3.3.2
  firebase_core: ^3.15.0
  firebase_auth: ^5.7.0
  firebase_messaging: ^15.2.10
  google_sign_in: ^6.3.0
  kakao_flutter_sdk_user: ^2.0.0+1
  kakao_flutter_sdk_share: ^2.0.0+1
  flutter_svg: ^2.3.0
  http: ^1.2.2
  shared_preferences: ^2.5.5
  image_picker: ^1.1.2
  url_launcher: ^6.3.2
  share_plus: ^10.1.4
  widgetation: ^0.2.2
  flutter_slidable: ^3.1.0
  flutter_local_notifications: ^22.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  fake_async: ^1.3.3
  widgetbook: ^3.25.0
  flutter_launcher_icons: ^0.14.4

# 앱 아이콘 생성 — `dart run flutter_launcher_icons` 로 iOS/안드로이드 전 사이즈 재생성.
# 소스: assets/icons/modi_app_icon.png(정사각 워드마크). 안드로이드 어댑티브는
# 원형 마스크 잘림 방지를 위해 여백 버전(modi_app_icon_adaptive_fg.png)을 전경으로 쓴다.
flutter_launcher_icons:
  image_path: "assets/icons/modi_app_icon.png"
  android: true
  ios: true
  remove_alpha_ios: true
  background_color_ios: "#FFFFFF"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icons/modi_app_icon_adaptive_fg.png"
  min_sdk_android: 21

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Xcode Cloud에서 공개 Swift Package 저장소마다 별도 GitHub App 설치를 요구하지
  # 않도록, 기존 CocoaPods 기반 iOS 의존성 해석을 명시적으로 유지한다.
  config:
    enable-swift-package-manager: false

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/icons/
    # 방 대표 이미지를 설정하지 않았을 때 쓰는 기본 배경 5종(features/room/default_cover.dart).
    - assets/images/covers/
    # S-01 온보딩 인트로 캐러셀 4장(390×844 프레임, features/onboarding/intro_screen.dart).
    - assets/images/onboarding/
    # 협업 캐릭터 일러스트(마이 탭 활동 카드, features/settings/my_activity_card.dart · specs/0016).
    - assets/images/characters/
    # (2026-08-13: shorebird.yaml 에셋 등록을 제거했다. Shorebird OTA 는 안드로이드 전용
    #  자동화였고 iOS 전용 출시로 좁히면서 걷어냈다 — app_id 는 끝까지 placeholder 였다.)

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # 앱 전역 폰트 = Pretendard (SIL OFL). specs/design.md §3 · app/lib/design/tokens.dart 참조.
  # 정적 웨이트 4종: Regular(400)/Medium(500)/SemiBold(600)/Bold(700).
  fonts:
    - family: Pretendard
      fonts:
        - asset: fonts/Pretendard-Regular.otf
          weight: 400
        - asset: fonts/Pretendard-Medium.otf
          weight: 500
        - asset: fonts/Pretendard-SemiBold.otf
          weight: 600
        - asset: fonts/Pretendard-Bold.otf
          weight: 700
    # Jalnan 2 (여기어때 잘난체 2.0, 무료 상업용) — 초대 코드 표시 전용 디스플레이 폰트.
    - family: Jalnan2
      fonts:
        - asset: fonts/Jalnan2.otf

  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
